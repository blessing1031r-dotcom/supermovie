# SuperMovie Observability Contract

本 doc は SuperMovie pipeline の observability に関する最小規約を固定する。Codex 19:50 consult verdict (Tech 改善 Medium #3) を起点に PR #1-#14 (PR-A..K) を経て確立、最新 fork/main = `db4cb74` 時点で全 surface (json tail / human stdout / stderr / error message / artifact path) で redaction default strict + unified knob (`--unsafe-keep-abs-path`) + 4 sensitive class (secret / user_content / abs_path / provider_response_body) helper 化が完了。env var rename (Anthropic rate v0→v1 alias) は PR-D 実装済、distributed tracing run_id active emission は PR-E 実装済、cost abort threshold は PR-F 実装済、error path tail emit consistency は PR-G 実装済、helper-level secret redaction は PR-H 実装済、human stdout / stderr path leak audit は PR-I/J 実装済、redact_error_message regex 強化 (Windows path / IPv6 / data URI 対応) は PR-K 実装済。provider price 記載 / external SaaS 前提は本 doc では扱わない。

## Scope And Non-Goals

### Scope (本 doc が固定する)
- status JSON emission の canonical な仕様 (`--json-log` 動作、stdout/stderr 区別、common fields)
- log redaction の対象種別と適用 rule
- cost telemetry の単位・rate env var convention・missing rate 時の挙動
- migration policy (legacy v0 → current v1、完了履歴と新規 script 追加時の reference)
- test 要件 (regression 防止)

### Non-Goals (本 doc では扱わない)
- provider 個別 pricing 記載 (network 制約下で未検証、`docs/api-operation-guard` 系参照)
- external SaaS / GCP / credential 前提 (本 PR では required にしない)
- metrics export / 外部 collector (OTel / Datadog 等) 実装 (本 doc は env-driven trace context のみ規定)
- 多 provider への v0 alias 拡張 (Gemini / Kling 等は v1 canonical のみ、Anthropic 限定の後方互換)

(Resolved Non-Goals: script v1 migration → PR-A/B/C で完了。env var rename → PR-D で v0→v1 alias 実装済。distributed tracing run_id active emission → PR-E で 7 script propagate 完了。)

## Current Surface

実装済の observability surface (PR #1-#14 merged 後、最新 fork/main = `db4cb74` 時点):

| script | --json-log | cost guard | dry-run | redaction status |
|---|---|---|---|---|
| `generate_slide_plan.py` | ✓ helper 経由 v1 (PR #3 merged) | ✓ ([:161,297](../template/scripts/generate_slide_plan.py)) | ✓ ([:159](../template/scripts/generate_slide_plan.py)) | helper 経由 (HTTP body / LLM raw text default redact、output path safe) |
| `voicevox_narration.py` | ✓ helper 経由 v1 (PR #3 merged) | minimal | — | helper 経由 (chunk text default redact、summary path safe) |
| `compare_telop_split.py` | ✓ helper 経由 v1 + category_override=`kpi-comparison` (PR-A) | — | — | helper 経由 (artifact path safe、`/tmp/` 等 system tmpdir も placeholder) |
| `visual_smoke.py` | ✓ helper 経由 v1 + category_override=`dimension-regression` (PR-A)、summary JSON artifact ([:365](../template/scripts/visual_smoke.py)) は維持 | — | — | helper 経由 (summary / grid path safe) |
| `preflight_video.py` | ✓ helper 経由 v1 (PR-B、既存 stdout source JSON 維持 + --json-log で末尾 v1 tail emit、success category=`preflight-source-meta` / error は STATUS_MAP 詳細 category) | — | — | helper 経由 (write-config path safe) |
| `timeline.py` | — (library 性質、Codex 21:01 step 3 S3-2 で migration 対象外) | — | — | — |
| `build_slide_data.py` | ✓ helper 経由 v1 (PR-C、category_override=`slide-build`、`--unsafe-show-user-content` で raw title) | — | — | helper 経由 (slide title default redact via `user_content_meta`、artifact path safe) |
| `build_telop_data.py` | ✓ helper 経由 v1 (PR-C、category_override=`telop-build`、`--unsafe-show-user-content` で raw text) | — | — | helper 経由 (telop text default redact via `user_content_meta`、artifact path safe) |

v1 helper 経由は generate_slide_plan / voicevox_narration / compare_telop_split / visual_smoke / preflight_video / build_slide_data / build_telop_data の **7 script** が適用済 (PR #3 + PR-A + PR-B + PR-C merged 後)。`timeline.py` は library 性質で migration 対象外、呼び出し元 script で status emit される設計 (Codex 21:01 step 3 S3-2)。Step 3 完了。

### v0 JSON tail / output gap (legacy 履歴、PR #3 で全解消済)

旧 v0 emission に schema v1 と不整合だった点 3 つの解消履歴:
- `generate_slide_plan.py` の output JSON `path` 絶対 path 漏れ → PR #3 で `safe_artifact_path` (`<HOME>` / `<TMP>` / `<ABS>` placeholder) 適用、`redaction.applied_rules=[abs_path]` 反映済。
- `voicevox_narration.py` の summary JSON `path` 絶対 path 漏れ → PR #3 で同 helper 適用済。
- `generate_slide_plan.py` の dry-run JSON は `--json-log` 強制なしで stdout に出す legacy 動作を維持 (cost estimate 即時 emit 用途)。`--json-log` 指定時は別途 helper 経由で v1 status JSON を末尾 1 行 emit する 2-emission pattern として canonical 化 (PR #3 確定)。

## Status JSON Contract

### Emission Contract

- `--json-log` flag が指定された時のみ、stdout の **末尾 1 行** に純 JSON (改行で終端) を emit する。
- human-readable stdout (進捗 message 等) は `--json-log` 有無に関係なく維持する。
- error / warning は `stderr` に出す。`--json-log` 時も stderr 経路は変えない。
- 1 invocation 1 emission を default。multiple emission を採る場合は `schema_version` で variant を区別する。

既存の test 契約 (`test_timeline_integration.py:1167`, `:2173`) は本 contract の正規化版。

**helper module type annotation contract**: `_observability.py` の全 module-level function は return type annotation と全 parameter の type annotation を持たなければならない。
annotation 追加・変更時の型 drift を AST audit で fail-loud 化。
(`test_observability_helper_type_annotation_completeness_lint`、PR-U)

**Local emit_json wrapper type contract** (`generate_slide_plan.py` / `voicevox_narration.py`):
scripts that define a local `emit_json(status: str, exit_code: int, ...)` wrapper call it with
- `status` as a str literal (v0 status string)
- `exit_code` as an int literal (not bool: `True`/`False` are bool subclass, rejected)
at every call site (`test_observability_emit_json_local_wrapper_positional_type_contract_lint`、PR-S1)。

### Common Fields (schema v1)

```json
{
  "schema_version": 1,
  "script": "generate_slide_plan",
  "status": "ok|skipped|error|dry_run",
  "ok": true,
  "exit_code": 0,
  "category": "phase3-slide|phase3-narration|...",
  "duration_ms": 1234,
  "counts": { "<domain-specific>": 0 },
  "artifacts": [
    { "path": "<relative or redacted absolute>", "kind": "json|wav|ts|png|..." }
  ],
  "cost": {
    "currency": "USD",
    "estimate": 0.0,
    "rate_source": "env:SUPERMOVIE_RATE_..._PER_MTOK"
  },
  "redaction": {
    "applied_rules": ["api_key", "abs_path", "user_content"],
    "version": 1
  },
  "run_id": "<32-char hex UUID v4、env SUPERMOVIE_RUN_ID 経由 or auto-generate、PR-E で active emission 化>",
  "parent_run_id": "<optional、env SUPERMOVIE_PARENT_RUN_ID 経由のみ、auto-generate なし>",
  "step_id": "<optional、env SUPERMOVIE_STEP_ID 経由のみ、auto-generate なし>"
}
```

### Status Naming (canonical 値)

| value | 用途 |
|---|---|
| `ok` | 成功完走 |
| `skipped` | precondition 不在等で no-op skip (e.g., engine 不在で voicevox skip、idempotent skip) |
| `error` | 異常終了 (exit_code ≠ 0)、`category` で詳細 |
| `dry_run` | API call せず estimate のみ出力 |

**v0_status enumeration completeness** (`STATUS_MAP` call-site coverage):
全 7 V1_CALLER_SCRIPTS の emit helper call site で使われる v0_status 文字列は必ず `STATUS_MAP` に登録済でなければならない。
未登録 v0_status は `map_status()` defensive fallback `("error", v0_status)` を通るが category 精度が劣化するため fail-loud 化。
(`test_observability_v0_status_enumeration_completeness_lint`、PR-S3)

### Stdout And Stderr

| stream | 内容 |
|---|---|
| stdout (human) | 進捗 message、preflight output、最終 summary。`--json-log` 時も維持。 |
| stdout (json tail) | 末尾 1 行に schema v1 JSON。`--json-log` 指定時のみ。 |
| stderr | error message、warning、stack trace、retry attempt。 |

`--json-log` consumer は **stdout の末尾 1 行のみを JSON parse** する想定。途中の human message を JSON parse させない。

### Script Coverage Matrix

post-migration (v1) では `generate_slide_plan.py` / `voicevox_narration.py` / `compare_telop_split.py` / `visual_smoke.py` / `preflight_video.py` / `build_slide_data.py` / `build_telop_data.py` の **7 script** が helper (`_observability.py`) 経由で schema v1 を emit する (PR #3 + PR-A/B/C merged 後)。`timeline.py` は library 性質で migration 対象外。Current Surface table 参照。

**_observability import order contract**: 各 7 script の `from _observability import (...)` block は alphabetical order (原名 / `as` alias 前の名前でソート) でなければならない。
import order drift は PR merge ごとに 1 名ずつ加わる中での順番混在を防ぐ。
(`test_observability_caller_import_order_canonical_lint`、PR-S2)

## Log Redaction Contract

### Sensitive Classes

| class | 例 | source |
|---|---|---|
| secret | `ANTHROPIC_API_KEY` 等 API key、token、credential | env / config / argv |
| user_content | transcript text、segments、telop raw text、prompt body | input file / API request body |
| abs_path | `/Users/<name>/...` machine-local 絶対 path | argparse / sys.argv / output file path |
| provider_response_body | LLM API の response 全文 | network response |

### Redaction Rules (Codex 20:08 review P1 #2 で strict 化)

- secret: 値の最後 4 文字以外をマスク (`sk-...XXXX`)。env name / config key 名は出して可。stderr / json tail / human stdout 全て同 rule。helper `redact_secret(value, *, last_n=4, mask_char="*")` で実装 (PR-H、`_observability.py`)。短い value (last_n+1 char 以下) は partial leak 回避のため全 mask、non-string は passthrough。現 codebase に secret 直接 emit 経路はなく、本 helper は将来の secret-bearing emission の contract enforcement として置く。
- user_content (transcript / segments / chunk text / telop raw):
  - human stdout: **default は `length` / `hash` のみ表示**、raw 出力は debug opt-in flag (`--unsafe-show-user-content` 等) 限定。`first-N-chars` 等の partial preview も default では出さない (raw partial も raw の subset とみなす)。`voicevox_narration.py` chunk text human log は PR #3 で `--unsafe-show-user-content` 化済み。
  - external structured log / json tail: `length` / `hash` のみ、raw 禁止 (default / debug 共通)。
  - debug opt-in 時も secret-bearing input (transcript 内に API key 等) は事前 detection + 削除。
- abs_path:
  - human stdout: **default redact** (PR-I で contract 改訂、`safe_artifact_path()` 経由で project-root 相対 / `<HOME>` / `<TMP>` / `<ABS>` placeholder)、`--unsafe-keep-abs-path` で raw 切替 (json tail と同 knob で一貫)。stdout を log capture / pipe / shared screenshot 等で意図せず外部公開する事故への defense-in-depth。
  - json tail / artifact path / external log: repo root or project root 相対 path に変換。`~` 展開後の絶対 path には `<HOME>` placeholder を適用。
- provider_response_body (LLM API の raw response):
  - **stderr であっても default は raw 禁止** (request_id / status_code / token_usage の structured summary のみ出す)。raw body は `--unsafe-dump-response` 等 debug opt-in flag 時に限定。
  - secret-bearing header (Authorization / x-api-key 等) は事前 strip。
  - `generate_slide_plan.py` の HTTP error response body / partial body / LLM raw text 経路 (旧 `:347` / `:351` / `:366`) は PR #3 で provider_response_body redaction 適用済 (`--unsafe-dump-response` opt-in 時のみ raw 出力)。
  - regression test 既存: HTTP body / LLM raw text を含む test fixture で default emission に raw が現れないことを `test_timeline_integration.py` の `test_observability_provider_body_stderr_default_redact` で検証済 (PR #3)。

### Path Policy

artifact `path` field は repo root or project root からの **相対 path** で記録する。絶対 path は `redaction.applied_rules` に `abs_path` を含めて redacted variant を記録する (PR #3 helper `safe_artifact_path` で `<HOME>` / `<TMP>` / `<ABS>` placeholder を機械置換、全 7 script 適用済)。明示 opt-in flag `--unsafe-keep-abs-path` は PR #3 で json tail / artifact path 用に実装、PR-I で human stdout (各 script の `print(f"... {path}")` 経路 / voicevox summary JSON) にも適用範囲拡大、両経路で同 knob 制御 (一貫した default redact / opt-in raw)。

**root priority policy** (`safe_artifact_path` の root 解決順序):
1. `project_root` を先に試みる (supermovie pipeline の primary root は project dir)
2. `project_root` に含まれない場合 `repo_root` を fallback として試みる
3. どちらにも含まれない abs path は `_lexical_redact` で `<HOME>` / `<TMP>` / `<ABS>` placeholder 化
4. 相対 path 入力は root 解決せずそのまま passthrough

`project_root > repo_root > lexical_redact placeholder` の優先順は `safe_artifact_path` の `for root in (project_root, repo_root)` ループで実装済 (PR-V で正式仕様化)。
supermovie pipeline では project root dir を `project_root` 引数として渡す運用。
(`test_observability_artifact_path_root_priority_policy_doc_lint`、PR-V)

### User Content Policy

`build_slide_data.py` / `build_telop_data.py` の raw title/telop text 経路は PR-C で `--unsafe-show-user-content` opt-in 化済。default は json tail に `user_content_meta` (length / sha256 hash) のみ emit、raw text は flag 指定時のみ stdout/json に出す。

## Cost Telemetry Contract

### Cost JSON Shape

設計上の future canonical schema (nested `cost` object):

```json
{
  "currency": "USD",
  "estimate": 0.0,
  "rate_source": "env:SUPERMOVIE_RATE_<PROVIDER>_<DIR>_USD_PER_MTOK",
  "rate_input_usd_per_mtok": 1.0,
  "rate_output_usd_per_mtok": 5.0,
  "tokens_input": 1234,
  "tokens_output": 567,
  "rate_missing": false
}
```

**現 emission (PR-S 以降)** は dry-run --json-log v1 tail / cost_guard_aborted の v1 tail で **nested cost object + top-level discriminator (backward compat)** の **dual emission** で出す:

- nested `cost`: PR-S で実装。`build_cost_payload(estimate, rate_input, rate_output, ...)` helper 経由で payload 構築、上記 schema に対応。`build_status(cost=cost_dict)` で payload に nested 出力。
- `cost.currency`: uppercase 3-letter ASCII code (例: `USD`) に固定。lowercase / mixed-case / non-ASCII / 3文字以外は `build_cost_payload()` が reject。
- top-level extras (backward compat): `estimated_cost_usd_upper_bound` / `estimated_input_tokens` / `estimated_output_tokens_upper_bound` / `rate_input_per_mtok` / `rate_output_per_mtok` (legacy JSON のみ) / `rate_missing` (PR-N で導入)。downstream parser が nested cost.* に migrate するまでの猶予として残す。
- discriminator 整合性: `cost.rate_missing == top-level rate_missing`、`cost.estimate == top-level estimated_cost_usd_upper_bound` (PR-S 統合 test で verify)。

dry-run legacy JSON (`--json-log` なしの一次 stdout) は依然 top-level form のみ (cost estimate を即時 emit する legacy 動作維持、PR-N で `rate_missing` 追加済)。

下流 parser 推奨: 新規実装は **nested `cost.*` を一次 source** とする。top-level extras は legacy 互換のみのため、将来 PR で removal 予定 (migration 警告は別 PR で出す可能性)。

### Rate Env Var Convention

- v1 canonical: `SUPERMOVIE_RATE_<PROVIDER>_<DIRECTION>_USD_PER_MTOK`
  - 例: `SUPERMOVIE_RATE_ANTHROPIC_INPUT_USD_PER_MTOK`、`SUPERMOVIE_RATE_GEMINI_OUTPUT_USD_PER_MTOK`
- v0 (既存): `SUPERMOVIE_RATE_INPUT_PER_MTOK` / `SUPERMOVIE_RATE_OUTPUT_PER_MTOK` ([generate_slide_plan.py](../template/scripts/generate_slide_plan.py))
  - v0 alias は **`generate_slide_plan.py` の Anthropic input/output rate 専用** で定義 (Codex 20:08 review P2 #1)。Gemini / Kling 等 future provider には v1 canonical のみを使う。
  - **alias 動作実装済 (PR-D)**: `_resolve_decimal()` の precedence は `CLI > v1 canonical > v0 alias > None`。両方設定時は v1 が勝つ。後方互換維持で v0 のみ設定でも動作。test は `test_generate_slide_plan_rate_v0_v1_alias_precedence` で 4 case 検証 (v0 only / v1 only / v1+v0 both / CLI wins)。

### Provider Notes

| provider | unit | rate kind |
|---|---|---|
| Anthropic API (slide plan) | $/MTok input + $/MTok output | text generation、env 必須 |
| Gemini API (image gen) | $/image | image generation、別 env 系 (`GEMINI_API_KEY`、PR-LE で固定) |
| VOICEVOX (narration) | local engine、cost なし ([voicevox_narration.py:66](../template/scripts/voicevox_narration.py)) | telemetry 対象外 |
| Kling / 他 (将来) | provider 個別 | 別 doc 化 |

### Missing Rate Behavior

- rate env が未設定: 現 emission では top-level `estimated_cost_usd_upper_bound = null` + `rate_missing = true`、`status = ok` (cost 不明は error 扱いしない)。future nested schema では `cost.estimate = null` / `cost.rate_missing = true`。
- rate が設定されているが invalid (NaN / Inf / -Inf / 負値 / decimal parse 不能等): `_resolve_decimal()` が ValueError を raise、`generate_slide_plan.py` の `except ValueError` 経路で `status = error`、`category = "cost_guard_arg_invalid"`、`exit_code = 4` に正規化される (PR-CD で docs ↔ code drift 修正、`STATUS_MAP["cost_guard_arg_invalid"] = ("error", "cost_guard_arg_invalid")` を反映)。

### Cost Abort Threshold (PR-F、Anthropic API 限定)

- `SUPERMOVIE_COST_USD_ABORT_AT` env / `--cost-abort-at` CLI で USD 単位の上限値を設定すると、`generate_slide_plan.py` は API call 前に `estimated_cost_usd_upper_bound` と比較し、超過時は `status=error / category=cost_guard_aborted / exit_code=10` で abort。
- precedence: CLI > env > None (default 無効)。
- rate 未設定 (`estimated_cost_usd_upper_bound=None`) 時は閾値設定があっても skip (cost 不明で勝手に abort せず通常進行)。
- abort emission payload には `estimated_cost_usd_upper_bound` / `cost_abort_at` / `estimated_input_tokens` / `estimated_output_tokens_upper_bound` を含む。
- 関連 test (PR-F): `test_generate_slide_plan_cost_abort_blocks_api_when_estimate_exceeds` / `test_generate_slide_plan_cost_abort_skipped_when_rate_unset` / `test_generate_slide_plan_cost_abort_cli_overrides_env`。

## Trace Context Convention (PR-E、distributed tracing run_id active emission)

### env precedence

| env var | role | precedence |
|---|---|---|
| `SUPERMOVIE_RUN_ID` | invocation 全体を貫通する trace identifier | 設定 + 非空 → そのまま採用、未設定 → `uuid.uuid4().hex` (32 char) auto-generate |
| `SUPERMOVIE_PARENT_RUN_ID` | upstream (orchestrator) からの caller run_id | 設定 + 非空 → そのまま採用、未設定 → `null` (auto-generate しない) |
| `SUPERMOVIE_STEP_ID` | pipeline 内の論理 step 識別子 | 設定 + 非空 → そのまま採用、未設定 → `null` (auto-generate しない) |

cap: 全 3 field に `MAX_TRACE_CONTEXT_VALUE_LEN = 128` 適用、超過時は `TraceContextError` raise (truncation せず error)。

`parent_run_id` / `step_id` を auto-generate しない理由: 関係性情報なので、呼出元 (orchestrator) が明示注入していない時に偽値を作ると trace tree が破綻するため。

### 7 script propagate 方針 (PR-E 実装)

各 script の `main()` 冒頭で `resolve_run_context()` を 1 回呼び、emit closure / `build_status()` 全 path に `run_id / parent_run_id / step_id` を渡す。`generate_slide_plan.py --dry-run --json-log` は legacy stdout JSON + v1 tail の 2-emission pattern で、v1 tail にも run_id 反映 (Codex 22:47 PR-E review prerequisite)。

`timeline.py` は library 性質で CLI emission を持たないため対象外 (Codex 21:01 step 3 S3-2 維持)。

### 関連 test (PR-E、`test_timeline_integration.py`)

- `test_observability_resolve_run_context_uses_env`: env 設定時の pass-through
- `test_observability_resolve_run_context_generates_when_missing`: env 未設定時の uuid4 32-char hex auto-generate + 連続 call で重複しない
- `test_observability_resolve_run_context_no_generate`: `generate_if_missing=False` で env 未設定 → None
- `test_observability_resolve_run_context_empty_env_fallback`: env 空文字列 → 未設定扱い + auto-generate
- `test_observability_resolve_run_context_cap_exceeded`: cap 超過 → `TraceContextError` raise
- `test_observability_run_id_in_payload`: build_status が non-None で payload に乗せ、None で含めない
- `test_generate_slide_plan_run_id_propagation`: dry-run --json-log 経由 v1 tail に run_id 反映 (e2e import + main() pattern)
- `nan/inf` ガードは既存 `generate_slide_plan.py:120` 系の挙動を canonical とする。

## Migration Policy

(本節は v0 → v1 migration の **完了済 normative spec** として保持。新規 script 追加時 / regression test 修正時の reference。Migration 自体は PR #3 + PR-A/B/C + PR-D で完了済。)

| state | criteria | 適用状況 |
|---|---|---|
| v0 (legacy) | `status` / `exit_code` を出す最小 status JSON、redaction なし、cost rate env は v0 名 | 廃止 (PR #3 で 2 script、PR-A/B/C で 5 script を helper 経由 v1 へ refactor 完了) |
| v1 (current) | schema_version=1、common fields 全埋め (null OK)、redaction 適用、rate env は v1 名 + v0 alias (Anthropic 限定、PR-D 実装済) | 7 script 適用済 (Current Surface table 参照) |

### v0 → v1 status mapping (Codex 20:08 review P1 #1 で明文化、PR #3 + PR-A/B/C で適用済)

既存 v0 status 値を v1 canonical 値に migrate する mapping。新規 script 追加時はこの table を normative とする。

| v0 status (現行 script) | v1 canonical | v1 category | source |
|---|---|---|---|
| `success` (slide-plan) | `ok` | (空 or domain-specific) | [generate_slide_plan.py:374](../template/scripts/generate_slide_plan.py) |
| `success` (narration) | `ok` | (空 or domain-specific) | [voicevox_narration.py:788](../template/scripts/voicevox_narration.py) |
| `api_key_skipped` | `skipped` | `api_key_missing` | [generate_slide_plan.py:191](../template/scripts/generate_slide_plan.py) |
| `engine_skipped` | `skipped` | `engine_unavailable` | [voicevox_narration.py:551](../template/scripts/voicevox_narration.py) |
| `list_speakers` | `ok` | `list_speakers` | [voicevox_narration.py:559](../template/scripts/voicevox_narration.py) |
| `dry_run` | `dry_run` | (現状通り) | [generate_slide_plan.py:298](../template/scripts/generate_slide_plan.py) |
| `error` variants (`cost_guard_arg_invalid` / `inputs_missing` / `rate_limited` / `api_http_error` / `llm_json_invalid` 等) | `error` | error 種別を `category` に格納 (旧 status 名を category に流用可) | 各 error path ([generate_slide_plan.py:229,235,350,352,367](../template/scripts/generate_slide_plan.py) 等) |

**STATUS_MAP reverse direction coverage** (orphaned entry detection):
`STATUS_MAP` の全 key は V1_CALLER_SCRIPTS 7 scripts の何れかで str literal として出現しなければならない。
str literal 未出現の key は orphaned dead entry であり、mapping 膨張を防ぐため fail-loud 化。
(`test_observability_status_map_reverse_direction_coverage_lint`、PR-BM')

### v0 dry-run JSON legacy

`generate_slide_plan.py` の dry-run path は `--json-log` flag なしでも JSON を stdout に出す legacy 動作を維持する (cost estimate を即時出すため)。schema v1 contract では:
- v0 dry-run JSON は `--json-log` 強制を要さず legacy として正規化済とみなす。
- helper (`_observability.py`) は dry-run legacy JSON を wrap しない設計 (PR #3 で確定)。dry-run は cost estimate を即時 stdout に出す独立 path で、`--json-log` 指定時は別途 helper 経由で v1 status JSON を末尾 1 行に emit する 2-emission pattern。

### Migration steps (完了履歴)

| step | 内容 | 完了 PR |
|---|---|---|
| 1 | `template/scripts/_observability.py` helper 新規追加 (status mapping + redaction rule + safe_artifact_path 実装) | PR #3 (987c3d0) |
| 2 | `generate_slide_plan.py` / `voicevox_narration.py` を helper 経由 refactor、abs_path 漏れ + chunk text redaction 適用 | PR #3 (987c3d0) |
| 3 | 残 5 script (`compare_telop_split.py` / `visual_smoke.py` (PR-A) / `preflight_video.py` (PR-B) / `build_slide_data.py` / `build_telop_data.py` (PR-C)) を v1 化。`timeline.py` は library 性質で対象外 (Codex 21:01 step 3 S3-2) | PR-A (4) / PR-B (5) / PR-C (6) |
| 4 | `test_timeline_integration.py` に redaction + status v1 schema regression test 9 件追加 (abs_path / user_content / provider_response_body の raw 漏れ防止、build_status duration_ms / category_override、provider body stderr default redact 等。`secret` class 専用 test は PR-H で追加 (5 件)、§Test Requirements 参照) | PR #3 + PR-A/B/C |
| 5 | rate env v0 → v1 alias 実装 (Anthropic 限定後方互換) | PR-D |
| 6 | distributed tracing run_id active emission 実装 (`resolve_run_context()` helper + 7 script propagate + cap validation + 7 件 regression test) | PR-E |
| 7 | pre-API cost abort threshold 実装 (`SUPERMOVIE_COST_USD_ABORT_AT` env + `--cost-abort-at` CLI + `cost_guard_aborted` status_map 追加 + estimate 共通化 + 3 件 regression test) | PR-F |
| 8 | error path tail emit consistency audit (`compare_telop_split` の transcript / typo_dict / telop ts read failure を `_emit_early` 経由化、`preflight_video` の `--write-config` parse / write failure を `_emit` 経由化、`visual_smoke` の `out_dir.mkdir` / `videoConfig.ts read` failure を `_emit_early` 経由化、9 status 追加 + 4 件 regression test) | PR-G |
| 9 | helper-level secret redaction 実装 (`redact_secret()` で last-4 mask + short-value 全 mask + non-string passthrough、`docs/OBSERVABILITY.md:123` secret class contract enforcement、4 件 regression test) | PR-H |
| 10 | human stdout path leak audit (build_slide_data / build_telop_data / voicevox_narration / visual_smoke / preflight_video / generate_slide_plan の 9 `print(f"... {path}")` 経路を `safe_artifact_path()` 経由化、`--unsafe-keep-abs-path` で raw 切替 unified knob、abs_path contract 改訂 + 1 件 regression test) | PR-I |
| 11 | stderr path leak audit (preflight_video `input not found` / visual_smoke `MAIN_VIDEO` / `REMOTION_BIN` / `VIDEO_CONFIG` / png ffprobe / generate_slide_plan `PROJ` を `safe_artifact_path()` 経由化、`--unsafe-keep-abs-path` で raw、stderr も同 contract 適用 + 1 件 regression test) | PR-J |
| 12 | `redact_error_message()` regex 強化 (Windows path `C:\...` / `D:/...` 検出 + 置換、IPv6 / data: URI / mailto: の false-positive 防御維持、複数 path 同時 redact + 3 件 edge case test) | PR-K |
| 13 | `--unsafe-keep-abs-path` flag 7 script audit (`build_slide_data` / `build_telop_data` / `voicevox_narration` / `visual_smoke` / `compare_telop_split` / `preflight_video` / `generate_slide_plan` の argparse + `args.unsafe_keep_abs_path` 使用を verify、将来 script 追加時の漏れ防止 lint test 1 件追加) | PR-M |
| 14 | `rate_missing` discriminator 追加 (generate_slide_plan dry-run legacy JSON + v1 tail / cost_guard_aborted payload に `rate_missing: bool` 明示、`estimate is None` から推論する fragile downstream parse を回避、§Cost JSON Shape contract enforcement + 2 件 regression test) | PR-N |
| 15 | `compute_rate_missing()` helper sink (`estimate is None ⇔ rate_missing=true` 算出規約を helper に集約、caller (legacy JSON / v1 tail / cost_guard_aborted) の重複式 3 site を `compute_rate_missing(estimate)` 単一呼び出しに統一、single source of truth + 1 件 unit test) | PR-O |
| 16 | entry exit code propagation audit (`build_slide_data` / `build_telop_data` / `preflight_video` の `__main__` block を `main()` 直呼び → `sys.exit(main() or 0)` に統一、`_emit_error` 経由 return int を shell rc に propagate、PR-G fix iter で compare_telop_split で発見された pattern を全 7 script に展開 + 漏れ防止 lint test 1 件) | PR-P |
| 17 | `redaction.applied_rules` canonicalize (`build_status()` で `sorted(set(...))` 正規化、caller の dedup 漏れ / 順序差を helper 側で吸収、downstream diff / regression test 安定性確保 + 1 件 unit test) | PR-Q |
| 18 | `redact_error_message()` URL edge case lock-in test (port `:8080` / query string `?key=value` / fragment `#anchor` / git+ssh:// scheme / URL+abs_path 混在 path 7 case を preserve regression、PR-K の `_ABS_PATH_RE` URL 破壊回避ロジック未被覆 edge を閉じる + 1 件 regression test) | PR-R |
| 19 | nested `cost={...}` schema migration (`build_cost_payload()` helper 追加、`generate_slide_plan` dry-run + cost_guard_aborted で `build_status(cost=cost_dict)` 経由 nested cost emit、top-level extras は backward compat で残す dual emission、§Cost JSON Shape 改訂 + 2 件 regression test) | PR-S |
| 20 | STATUS_MAP static lint test 追加 (`test_observability_status_map_lint`)。AST parse で dict literal 重複 key を検出 (silent overwrite 防止)、value shape (2-tuple) / v1_status 4 値 set / category str-or-non-empty を一括 verify。`map_status()` の defensive fallback `("error", v0_status)` が unknown を silent fallback するため、emit site 追加漏れ系の `must_have` set diff (`test_observability_helper_status_map`) で拾えない内部 contract drift を early fail させる lint 層 + 1 件 regression test | PR-T |
| 21 | `safe_artifact_path()` path collision corner case test 追加 (`test_observability_safe_artifact_path_collision_corners`)。(1) project_root + repo_root 両方 match で project_root 優先、(2) path == project_root 完全一致 → "."、(3) `..` traversal escape は relative_to で root 配下偽装させず placeholder、(4) project_root の trailing slash 無関係性、(5) `proj` vs `proj_extra` 似た prefix non-collision (Path.relative_to の segment 単位判定依存)、(6) repo_root のみ fallback contract、(7) 相対 path 入力 as-is passthrough。出力先一意性 + 監査 trail 健全性 + caller 流儀差吸収を lock-in + 1 件 regression test | PR-U |
| 22 | `safe_artifact_path()` の `~/...` 展開 redaction contract gap fix。旧実装は `Path(s).is_absolute()` ガードで `~` を `expanduser` せず、Python 仕様で `Path("~/x").is_absolute() == False` のため後段 `_lexical_redact(s, home)` も `s.startswith(home)` 判定を通せず literal `~/...` がそのまま漏れていた。fix: `s.startswith("~")` 時のみ `os.path.expanduser` を早期適用して `s_expanded` を後段全経路に渡す。相対 path 入力 (`public/main.mp4` 等) の as-is passthrough invariant (PR-U test 7) は維持。`test_observability_safe_artifact_path_tilde_expansion` で 7 case (root 未指定 / 異 root / `~`-`~` mixed / `~`-abs mixed / unsafe_keep bypass / 非 tilde 相対 passthrough / abs HOME regression) を lock-in + 1 件 regression test | PR-V |
| 23 | `build_status()` 出力 top-level field 順序の deterministic lock-in test 追加 (`test_observability_build_status_top_level_field_order`)。Python 3.7+ の dict insertion order 保持 + json.dumps 順次出力により、status JSON tail の field 順は consumer 側 diff / log grep / regression test 安定性を支える contract の一部。reserved core 10 field の固定順 (schema_version → script → status → ok → exit_code → category → counts → artifacts → cost → redaction)、optional field (duration_ms / run_id / parent_run_id / step_id) の source 宣言順、extras (v0 compat) の caller 順、reserved key 同名 extras の filter (reserved 値が勝つ) を 6 case で lock-in + 1 件 regression test | PR-W |
| 24 | legacy top-level cost extras deprecation warning 追加 (env-gated)。`warn_legacy_cost_extras()` helper + `WARN_LEGACY_COST_EXTRAS_ENV = "SUPERMOVIE_OBSERVABILITY_WARN_LEGACY_COST_EXTRAS"` (default off)、`LEGACY_COST_EXTRAS_KEYS` 5 entry (estimated_input_tokens / estimated_output_tokens_upper_bound / estimated_cost_usd_upper_bound / cost_abort_at / rate_missing)。env="1" 時のみ payload に nested `cost` (truthy) と legacy keys が併存していたら stderr に 1 行 deprecation warning emit、stdout JSON contract は不変。`generate_slide_plan.emit_json` で payload 構築直後に呼び出し、downstream consumer に nested 形式 migration を促す。test 5 case (env unset / "0" / "1" + dual / "1" + nested only / "1" + cost None) で env-gate / dual 検出 / no-op 経路を lock-in + 1 件 regression test | PR-X |
| 25 | `emit_json()` stdout output format lint 追加 (`test_observability_emit_json_format_lint`)。`--json-log` の downstream parser が stdout 末尾を `splitlines()[-1]` → `json.loads()` する pattern を前提にしているため、format invariant を helper-level で lock-in: (1) enabled=True で exactly 1 line + final `\n`、(2) `json.loads` 可能で payload と semantically 等価、(3) embedded control char (`\n` / `\t` / `"` / `\\`) escape で single-line 維持、(4) non-ASCII (日本語) 文字は `ensure_ascii=False` literal 維持で行構造保持、(5) enabled=False で stdout 無音、(6) exit_code propagation。既存 `test_observability_emit_json_disabled_no_print` は parse 成功のみ、行数 / 末尾改行 / 制御文字 escape は本 test で初固定 + 1 件 regression test | PR-Y |
| 26 | `emit_json()` stderr 非混入 invariant lint test 追加 (`test_observability_emit_json_stderr_clean`)。`--json-log` の stdout/stderr 分離契約 (stdout = JSON tail + human v0 出力、stderr = error/warning/debug 専用) を helper-level で固定。`redirect_stderr(StringIO())` 下に enabled / disabled / error-status / non-ASCII / control-char の 5 case で `emit_json()` を呼び、stderr が空のまま、enabled=True で stdout に出力される (false-positive guard) ことを assert。emit_json の `print()` が `file=sys.stderr` 等にリファクタされた時の transport contract drift を early fail + 1 件 regression test | PR-Z |
| 27 | `build_cost_payload()` の NaN / Inf rate defense + `compute_rate_missing()` 拡張。`_coerce_finite_or_none(v)` helper 追加で estimate / rate_input / rate_output が non-finite (NaN / Inf / -Inf / 非数値型) 時に None 正規化。`compute_rate_missing(estimate)` も None | non-finite で True を返すよう拡張。CLI / env 経路は math.isfinite + ValueError reject で early guard 済だが、helper 独立呼び出し / 将来 caller への defense-in-depth。json.dumps default が `NaN` / `Infinity` non-standard token を出す経路 + `allow_nan=False` での突発 ValueError を完全 closure。test 7 case (estimate NaN/Inf/-Inf / rate_input NaN / rate_output Inf / 全 finite regression / 全 None / compute_rate_missing 直接 / 非数値型 str) で contract lock-in + 1 件 regression test | PR-AA |
| 28 | `warn_legacy_cost_extras()` env gate strict opt-in lint 追加 (`test_observability_warn_legacy_cost_extras_env_strict_opt_in`)。PR-X 既存 test は unset / "0" / "1" の 3 値しか被覆しないため、env が "2" / "true" / "TRUE" / "yes" / "on" / 空 string / whitespace / "1\n" / " 1" / "11" / "01" 等の「truthy 風だが厳密に "1" ではない」 値を渡した時に warning が emit されない strict opt-in 契約を 19 invalid value で lock-in + "1" exactly での positive control。リファクタで `bool()` / `.lower() in (...)` / 空文字弾き 等に変えた場合の noise regression を early fail + 1 件 regression test | PR-AB |
| 29 | `emit_json()` の `payload['exit_code']` int 限定 contract fix + lock-in。旧実装 `int(payload.get("exit_code", 0))` は str / float / bool を silent coerce、None / "abc" で uncaught TypeError/ValueError を投げる weak 動作で v1 schema drift を silent 通過させていた。新実装は missing → default 0 維持、bool は int subclass でも reject、それ以外の非 int (str / float / None / list / dict / object) を explicit TypeError で fail-loud、payload 構築側の責務として固定。`test_observability_emit_json_exit_code_int_contract` を追加 (98→99 件)、8 case (正常 int / missing default / bool reject / str reject / float reject / None reject / その他 type reject / reject 経路で stdout 空) で contract lock-in + 1 件 regression test | PR-AC |
| 30 | `build_status(redaction_rules=...)` の str 限定 contract fix + lock-in。旧実装 `sorted(set(redaction_rules)) if redaction_rules else []` は None/empty で `[]` を返すが、`[None]` / `[1]` を silent pass、`["a", None]` で「'<' not supported between instances of 'str' and 'NoneType'」の意味不明 TypeError、bare str (`"abs_path"`) を渡すと iter で char 分解されて `["_","a","b","h","p","s","t"]` という schema drift を起こしていた。新 `_normalize_redaction_rules()` helper を追加: None → []、list/tuple of str → sorted unique、bare str / 非 str entry / dict-set-int 等 → explicit TypeError fail-loud。`test_observability_build_status_redaction_rules_strict` を追加 (99→100 件)、9 case (None / empty list/tuple / dedup / tuple accept / bare str reject / [None] reject / [int] reject / mixed reject / dict-set-int reject) で contract lock-in + 1 件 regression test | PR-AD |
| 31 | `build_status()` の reserved key collision invariant lint 拡張。PR-W (`test_observability_build_status_top_level_field_order` case 5) で `status` 1 件のみ被覆していた `**extras` 経由の reserved key override filter を全 non-signature reserved key (5 件: `schema_version` / `status` / `ok` / `category` / `redaction`) に拡張。signature kwargs (script / v0_status / exit_code / counts / artifacts / cost / redaction_rules / duration_ms / category_override / run_id / parent_run_id / step_id) は Python routing で `**extras` に流れないため対象外。`test_observability_build_status_reserved_key_collision` を追加 (100→101 件)、4 case (各 reserved key 単独 inject / 5 件まとめ inject / unrelated extras passthrough / payload key 順序 invariant) で reserved-wins 契約を全 reserved key に対して lock-in + 1 件 regression test | PR-AE |
| 32 | `build_status(cost=...)` の dict-or-None 限定 contract fix + lock-in。旧実装は cost 引数の型 validation がなく list / str / int / bool / tuple / set 等を payload に素通り、§Cost JSON Shape canonical nested object 契約 + `warn_legacy_cost_extras()` の `payload.get("cost")` truthiness 判定の両方を drift させていた。fix: build_status 入口で `cost is not None and not isinstance(cost, dict)` を explicit TypeError で fail-loud。`test_observability_build_status_cost_dict_strict` を追加 (101→102 件)、8 case (None / 通常 dict / empty dict / list reject / str reject / int+float reject / bool reject / tuple+set+object reject) で contract lock-in。既存 callers (build_cost_payload 経由 dict / None) は backward compatible + 1 件 regression test | PR-AF |
| 33 | `resolve_run_context()` env value cap (`MAX_TRACE_CONTEXT_VALUE_LEN = 128`) の境界値 inclusive lock-in lint 追加 (`test_observability_resolve_run_context_cap_boundary`)。既存 PR-E `test_observability_resolve_run_context_cap_exceeded` は 129 char の reject のみ被覆、127 / 128 char の accept (boundary inclusive contract) は lock されていなかった。`_validate_trace_value` の cap 比較 `> MAX_TRACE_CONTEXT_VALUE_LEN` が `>= ...` 等にリファクタされて 128 char 突然 reject の silent regression を early fail させるため、TRACE_RUN_ID_ENV / TRACE_PARENT_RUN_ID_ENV / TRACE_STEP_ID_ENV 全 3 env の 127 accept / 128 accept / 129 reject の 3 boundary を lock-in (3 env × 3 boundary = 9 case) + 1 件 regression test | PR-AG |
| 34 | `build_status()` の `schema_version: 1` invariant + header hash snapshot lock-in lint 追加 (`test_observability_build_status_schema_version_invariant`)。`schema_version` は v1 schema の root identifier で downstream consumer / log analyzer が「v1 payload」と認識する最上流契約。4 層 lock-in: (1) `SCHEMA_VERSION` constant が int 1 (型 + 値、bool subclass reject)、(2) build_status 出力で全 7 v0 status (success/rate_limited/api_key_skipped/dry_run/cost_guard_aborted/ffprobe_failed/smoke_ok) で schema_version=1 維持、(3) extras inject (schema_version=999) reserved-wins、(4) header 6 field (schema_version/script/status/ok/exit_code/category) の SHA-256 16char snapshot hash (`4e1cd359d000dd2b`)。PR-W (field order) / PR-AE (reserved key) と役割分離、root identifier 単独の独立 lock-in + 1 件 regression test | PR-AH |
| 35 | `redact_provider_body()` の `preview_length` boundary fix + lock-in。旧実装 `min(len(body), max_preview)` は `max_preview=-1` で `preview_length=-1` の semantic violation (range invariant `[0, len(body)]` 外) を起こしていた。fix: `max(0, min(len(body), max_preview))` で下限 clamp、`max_preview=0` の preview 抑止 contract と `max_preview > len(body)` の上限 clamp は維持。`test_observability_redact_provider_body_preview_length_boundaries` を追加 (104→105 件)、8 boundary case (上限 clamp +100/+1/0 / -1 boundary / 1 / 0 抑止 / -1 / -100 negative clamp) で contract lock-in + raw body / sensitive token 非漏洩 invariant 相互強化 + 1 件 regression test | PR-AI |
| 36 | `user_content_meta()` / `redact_provider_body()` の `sha256` field format invariant lock-in lint 追加 (`test_observability_sha256_hash_format_invariant`)。`sha256` は v1 schema の機械的指紋で downstream consumer / log diff / regression test が「16 char lower hex prefix」前提で扱うため、`_hash16()` 内部実装が SHA-256 full hex (64 char) / upper case / 別 encoding に silent drift した場合の format-dependent caller 破壊を early fail。6 層 lock-in: (1) `user_content_meta` sha256 が str / 16 char / `[0-9a-f]{16}`、(2) `redact_provider_body` 同 format、(3) deterministic (同 input → 同 hash)、(4) 既知入力 snapshot (`hello world` → `b94d27b9934d3e08`)、(5) 異 input → 異 hash (algorithm 化崩れ regression detect)、(6) 空文字 / 非 ASCII / control char / emoji 入力でも format 維持 + 1 件 regression test | PR-AJ |
| 37 | `build_status(counts=...)` / `build_status(artifacts=...)` の defensive contract fix + lock-in。旧実装 `counts or {}` / `artifacts or []` は型 validation なしで list / str / int / bool / dict 単体 / list 内 non-dict を silent payload 通過、v1 schema common field の構造 drift を起こしていた。fix: build_status 入口で `counts is not None and not isinstance(counts, dict)` / `artifacts is not None and not isinstance(artifacts, list)` / `not all(isinstance(item, dict) for item in artifacts)` を explicit TypeError で fail-loud (artifacts は entry index 含む詳細 msg)。`test_observability_build_status_counts_artifacts_strict` を追加 (106→107 件)、counts 14 case (accept None / dict / empty dict + reject list/empty list/str/empty str/int/0/True/False/tuple/set/object) + artifacts 13 case (accept None / list-of-dict / empty + dict 単体 reject + non-list 4 (str/empty str/int/tuple) reject + 5 mixed list pattern reject) で contract lock-in (合計 27 case)。既存 callers (build_slide_data / build_telop_data / generate_slide_plan / voicevox 等) は dict / list-of-dict 渡しなので backward compatible + 1 件 regression test | PR-AK |
| 38 | `build_cost_payload(rate_source=...)` の str + `env:` prefix + non-empty env name contract fix + lock-in。旧実装は型 / format validation なしで `""` / `None` / int / bool / list / `"SUPERMOVIE_RATE_X"` (prefix なし) を silent payload 通過、§Cost JSON Shape の env var convention canonical contract を崩していた。fix: build_cost_payload 入口で `isinstance(rate_source, str)` 違反 → TypeError、`startswith("env:")` 違反 + prefix 直後 env name 空 (`len <= len("env:")`) → ValueError で fail-loud。`test_observability_build_cost_payload_rate_source_contract` を追加 (107→108 件)、accept 3 case (default placeholder / 通常 ENV_NAME / minimal `env:X`) + reject 7 type case (None/int/float/bool/list/dict) + reject 5 prefix 欠如 case + reject 2 empty case で contract lock-in。既存 caller (default 値 + `env:SUPERMOVIE_RATE_<PROVIDER>_<DIR>_USD_PER_MTOK` 系) は backward compatible + 1 件 regression test | PR-AL |
| 39 | `build_status(script=...)` の str + non-empty + no-control-char contract fix + lock-in。`script` は v1 status JSON の core identifier (downstream consumer / log filter / dashboard が script 名で payload を bucket する根拠 field)。旧実装は型 / 空文字 / 制御文字 validation なしで `""` / `"   "` (whitespace-only) / `"a\n b"` / `"a\x00b"` / int / bool / list / None を silent payload 通過、emit_json 1-line format invariant (PR-Y) も control char 経由で破壊しうる drift を起こしていた。fix: build_status 入口で (1) `isinstance(script, str)` 違反 → TypeError、(2) `script.strip() == ""` (whitespace-only 含む) → ValueError、(3) `\x00-\x1F` / `\x7F` 制御文字含む → ValueError で fail-loud。`test_observability_build_status_script_identifier_contract` を追加 (108→109 件)、accept 4 case (snake_case identifier / 拡張子付き / 1 char / 日本語 unicode) + reject 7 type case (None/int/float/bool/list/dict) + reject 6 empty/whitespace case + reject 7 control-char case + 7 既存 caller name regression guard で contract lock-in。既存 callers は all snake_case identifier なので backward compatible + 1 件 regression test | PR-AM |
| 40 | `STATUS_MAP` の category string format invariant lint 追加 (`test_observability_status_map_category_format_invariant`)。category は v1 status JSON の bucket field で、downstream consumer / log analyzer / dashboard が同 category 値で payload を集約する根拠。旧 PR-T `test_observability_status_map_lint` は (status, category) tuple shape + 重複 key + 空文字 reject を被覆していたが、format invariant (UPPERCASE / 制御文字 / leading-trailing dash / non-ASCII 特殊) の drift は lock されていなかった。permissive regex `^[a-z](?:[a-z0-9_-]*[a-z0-9])?$` で 46 unique category 全て validate (snake_case + kebab-case 両方 accept、segment case style 統一は別 lint 候補)、加えて 14 drift pattern (UPPERCASE / mixed / leading dash/underscore / trailing dash/underscore / leading digit / whitespace / tab / newline / dot / slash / 特殊文字 / 空文字) を regex 自体が reject することの negative control もまとめて lock-in + 1 件 regression test | PR-AN |
| 41 | `redact_secret()` の `mask_char` strict contract fix + boundary lock-in。旧実装は mask_char に空文字 (`""`) を渡すと `mask_char * N == ""` で last_n char が unmasked で raw 露出する semantic leak (例: `redact_secret("abcdefgh", last_n=4, mask_char="")` → `"efgh"`)。fix: value 検査後に mask_char isinstance str 違反 → TypeError、`not mask_char` (空文字) → ValueError で fail-loud (None / "" / non-str value passthrough は mask_char 検査前で backward compat 維持)。`test_observability_redact_secret_boundary_and_mask_char_strict` を追加 (110→111 件)、14 case (4 boundary len + 2 extreme last_n + 3 1-char value + 4 mask_char strict / leak 防止 / non-str / multi-char accept + 4 既存 passthrough regression guard) で contract lock-in + 1 件 regression test | PR-AO |
| 42 | `build_status(v0_status=...)` の str + non-empty + no-control-char defensive contract fix + lock-in。`v0_status` は `map_status()` lookup key で、未知 status は `("error", v0_status)` defensive fallback で category=v0_status となる仕様 (PR-T 設計)。旧実装は型 / 空文字 / 制御文字 validation なしで `""` / `"   "` / `"a\n b"` / `"a\x00b"` / int / bool / None を silent fallback で category 経路汚染、`list` は dict lookup の unhashable で uncaught TypeError drift。fix: build_status 入口で (1) `isinstance(v0_status, str)` 違反 → TypeError、(2) `v0_status.strip() == ""` (whitespace-only 含む) → ValueError、(3) `\x00-\x1F` / `\x7F` 制御文字 → ValueError で fail-loud。unknown str fallback (PR-T 設計) は維持、PR-AM script identifier 同型。`test_observability_build_status_v0_status_defensive_lint` を追加 (111→112 件)、accept 3 case (success / unknown str / 1 char) + reject 8 type case (None/int/float/bool/list/dict 等) + reject 6 empty/whitespace + reject 7 control-char + 7 既存 v0 emission regression guard で contract lock-in + 1 件 regression test | PR-AP |
| 43 | `emit_json(enabled, payload)` の payload dict 必須 contract fix + lock-in。`--json-log` の末尾行は v1 status JSON object 前提に downstream parser が組まれるため、payload は dict 必須。旧実装は entry で直接 `payload.get("exit_code", 0)` を呼ぶため、non-dict 入力 (None / list / str / int / tuple / set / object) で uncaught AttributeError "X object has no attribute 'get'" が出るだけで、caller の責務違反が分かりにくい drift。fix: emit_json 入口に `isinstance(payload, dict)` 違反 → explicit TypeError で fail-loud (PR-AC exit_code int 検査より前置で payload 自体の型を最優先 validate)。`test_observability_emit_json_payload_must_be_dict` を追加 (112→113 件)、accept 3 case (通常 dict / empty dict / error dict) + reject 14 non-dict case (None / list / str / int / float / bool / tuple / set / object) + reject 経路で stdout 空 invariant (PR-AC 同型) で contract lock-in。既存 caller (build_status() 経由 dict のみ) は backward compatible + 1 件 regression test | PR-AQ |
| 44 | `redact_error_message()` の URL+path 混在 input order independence lint 追加 (`test_observability_redact_error_message_url_path_order_independence`)。error message には URL と HOME 配下 abs path が混在することが多く、caller の文章組み立て順違いで output が drift してはならない (path → `<HOME>` placeholder 化 + URL → preserve invariant)。PR-K (URL 破壊回避 + multiple paths) / PR-R (URL edge case port/query/fragment lock-in) と相補で、両者混在 + 入力順序の組み合わせを未 lock 領域として塞ぐ。4 case (URL→path / path→URL / URL+segment+path / path→URL+segment) で path raw 漏れなし + URL host preserve + 各 case で `<HOME>` placeholder が実際に出る (count >= 1) + A/B C/D 間の `<HOME>` placeholder count + URL host count 一致による order-independence を assert + 1 件 regression test | PR-AR |
| 45 | `redact_error_message()` の `~/...` / `~user/...` tilde path token redact contract 拡張 + URL preserve invariant 維持。旧実装は `_ABS_PATH_RE` のみで `/...` 部分しか拾わず、`~/secret/file.json` を `~<ABS>/file.json` という `~` 残留 leak として出力していた (PR-V `safe_artifact_path` の `~` expansion fix と error_message redact 経路で contract drift)。fix: `_TILDE_PATH_RE` を新設し `_sub_tilde` で `os.path.expanduser` → `_lexical_redact` 経由で `<HOME>/...` placeholder 化、未知 user (`~unknownuser/...`) は `<ABS>/<basename>` に落とす。`_TILDE_PATH_RE` lookbehind は `/` / `=` / `?` / `&` を reject set に含め、URL path segment (`https://example.com/~/x.json` の `~`) と query parameter (`?next=~/x.json`) を誤発火させず PR-K URL preserve invariant を維持 (Codex 04:51 P1 fix)。`_ABS_PATH_RE` lookbehind に `>` + `~` を追加で `<HOME>/...` placeholder 二重 redact 防止 + URL 内 `/~/x.json` の `/x.json` 誤発火防止。`test_observability_redact_error_message_tilde_path_token` を追加 (114→115 件)、12 case (`~/path` / `~/dir-only` / `~/.dotfile` / `~unknownuser/data` / mixed `~`+abs / abs HOME regression / URL+tilde / `~` 単独 + URL path-segment-`~` / query-param-`~` / URL-`~user` preserve / plain abs path regression) で contract lock-in + 1 件 regression test | PR-AS |
| 46 | `build_status(category_override=...)` の str + non-empty + no-control-char defensive contract fix + lock-in。`category_override` は STATUS_MAP lookup の category を bypass して `if category_override is not None: v1_category = category_override` で payload core field に直接代入される経路で、`STATUS_MAP` 側の category format invariant lint (PR-AN) は通常 lookup ペアのみ対象、override 経路は検査されない gap が残っていた。旧実装は型 / 空文字 / 制御文字 validation なしで `""` / `"   "` / `"a\n b"` / `"a\x00b"` / 5 / True / [...] / {...} を silent payload 通過、emit_json 1-line format invariant (PR-Y) も control char 経由で破壊しうる drift。fix: build_status 入口の category_override 代入直前に (1) `isinstance(category_override, str)` 違反 → TypeError、(2) `category_override.strip() == ""` (whitespace-only 含む) → ValueError、(3) `\x00-\x1F` / `\x7F` 制御文字 → ValueError で fail-loud。None は引き続き許容 (STATUS_MAP の category を活かす経路、既存 visual_smoke / build_slide_data / build_telop_data 互換)、PR-AM script identifier / PR-AP v0_status_strict と同型。`test_observability_build_status_category_override_defensive_lint` を追加 (115→116 件)、accept 5 case (kpi-comparison / dimension-regression / preflight-source-meta / slide-build / telop-build の既存 caller 値) + 1 char 最小 + None 経由の STATUS_MAP / fallback / default 省略 3 case + reject 10 type case (int/float/bool/list/dict/tuple/object 等) + reject 5 empty/whitespace + reject 7 control-char で contract lock-in + 1 件 regression test | PR-AT |
| 47 | `warn_legacy_cost_extras(payload)` の payload dict 必須 contract fix + lock-in。`warn_legacy_cost_extras()` は build_status() 出力 dict 前提で `payload.get("cost")` / `for k in LEGACY_COST_EXTRAS_KEYS if k in payload` を直接呼ぶ実装で、entry に型 guard がなく non-dict 入力 (None / list / str / int / tuple / set / object) で uncaught AttributeError "X object has no attribute 'get'" / TypeError "argument of type 'X' is not iterable" を投げて caller 責務違反が見えにくい drift。`emit_json` (PR-AQ) と同型の dict-only contract に統一し、env gate より前に置くことで env=0/unset の no-op 経路でも caller bug を即時検知 (stderr-warning 空 invariant 維持)。fix: warn_legacy_cost_extras 入口に `isinstance(payload, dict)` 違反 → explicit TypeError で fail-loud (env check より前)。`test_observability_warn_legacy_cost_extras_payload_must_be_dict` を追加 (116→117 件)、accept 3 case (no nested cost / empty / nested cost+legacy keys、env off 全 no-op) + reject 14 non-dict case (env off で前置 fail-loud) + stderr 空 invariant + env=1 path の 5 reject + dict regression guard で contract lock-in。既存 caller (build_status() 経由 dict のみ) は backward compatible + 1 件 regression test | PR-AU |
| 48 | `docs/OBSERVABILITY.md §Migration steps` table の step 番号 contract lint 追加 (`test_observability_docs_migration_steps_numbering`)。Migration steps は手書きで step 1..N を順に append する運用で (PR #3 で 1..4 から始まり、PR-AU で 47 まで成長)、後続 PR で重複 (同じ step 番号 2 回) / skip (47→49) / 1 始まり違反 / row 順序入れ替え (step 5 を step 4 の上に動かす等) が発生すると、release note ↔ PR ↔ step 番号の紐付けが壊れて handoff / fork audit の引用元が不明瞭になる drift。本 lint は `### Migration steps` heading から次の `^## ` heading 直前までを section として抽出し、`| <int> | ... |` 行の step 番号を正規表現で集めて (1) 抽出件数 ≥ 1、(2) 重複なし、(3) 出現順そのものが `list(range(1, N+1))` (Codex 05:14 P2 fix で sorted 比較から append-only 契約強化へ tighten)、(4) 各行 PR cell 非空 を assert。Codex 05:11 推奨 BC、PR-M `--unsafe-keep-abs-path` flag audit / PR-T STATUS_MAP static lint と同 level の docs/static lint + 1 件 regression test | PR-AV |
| 49 | `build_slide_data` / `build_telop_data` / `preflight_video` の v1 schema emit caller conformance audit 追加 (3 件)。helper 側 (build_status / emit_json) は PR-AC〜PR-AU で type / format invariant を fail-loud 化済だが、3 caller script が実際に組み立てて出す `--json-log` tail が docs §Common Fields contract に準拠しているかは static 検査だけでは拾えない (caller の kwargs 受け渡し漏れ / 順序ずれ / counts key 改名 / artifacts.kind drift)。`_assert_v1_payload_common` shared helper で core 9 field (schema_version=1 / script / status / ok / exit_code / category / counts dict / artifacts list-of-dict (path+kind str) / redaction.applied_rules list-of-str sorted unique / cost dict-or-None / duration_ms int>=0 if present) を一括 lint、加えて script 別の domain key (build_slide_data: input_segments / output_slides / used_plan + artifact kind="ts"、build_telop_data: telop_count / weaknesses + kind="ts"、preflight_video: counts={} + write-config 時 kind="json") を assert。build_slide_data / build_telop_data は in-process main() + monkey-patched PROJ で実 e2e (既存 _setup_temp_project + budoux stub 流儀)、preflight_video は ffmpeg 合成 mp4 + subprocess (ffmpeg/ffprobe 不在は skip)、Codex 05:25 推奨 BG。PR-T STATUS_MAP static lint / PR-W field order / PR-AK counts/artifacts strict と相補な caller-side regression test 層 + 3 件 regression test | PR-AW |
| 50 | `compare_telop_split` / `visual_smoke` の v1 schema emit caller conformance audit 追加 (2 件、Codex 05:33 推奨 BH 前半)。PR-AW pair で残 4 v1 caller (compare_telop_split / visual_smoke / generate_slide_plan / voicevox_narration) のうち前半 2 件を audit。compare_telop_split は success path で in-process main() + 最小 transcript_fixed.json + 1 件 telopData.ts 2 つの fixture、`script="compare_telop_split"` / `v0_status="all_pass"` → STATUS_MAP `("ok", "kpi-comparison")` + counts={baseline_kpi dict, new_kpi dict} (各 6 KPI metric) + artifacts 2 件 (kind="ts" x 2) + redaction abs_path を assert。実装が `sys.exit(0)` を直接呼ぶため SystemExit catch して exit code 採取。visual_smoke は npx remotion still / Node toolchain が test 環境前提に依存するため early-error path (out-dir-mkdir-error) で v1 conformance を audit、`_emit_early` の minimum payload (counts={} / artifacts=[] / redaction_rules=[] / cost=None) でも v1 common field 全種 (PR-AW `_assert_v1_payload_common` 共有) が contract 通り揃うことを lock-in。既存 PR-G `test_visual_smoke_out_dir_mkdir_error_emits_tail` (status=error / category 単体 assert) と相補、本 test は core 9 field 一括 lint + 2 件 regression test | PR-AX |
| 51 | `generate_slide_plan` / `voicevox_narration` の v1 schema emit caller conformance audit + duration_ms caller fix (2 件、Codex 05:43 推奨 BH 後半 / 案A、PR-AW/AX/AY pair で 7 v1 caller 全件 audit 完了)。本 PR で interesting drift 発見: 旧 emit_json wrapper (generate_slide_plan.py:226-247 / voicevox_narration.py:559-581) は build_status に duration_ms を渡さず、PR-AW `_assert_v1_payload_common` P1 fix で必須化された duration_ms key が payload に欠落していた。caller fix として両 script の main 冒頭で `start_time = time.monotonic()` を capture、emit_json wrapper 内で `duration_ms = int((time.monotonic() - start_time) * 1000)` を build_status に渡すよう修正 (build_slide_data:454 / build_telop_data:510 / preflight_video:334 と同流儀)。conformance test は generate_slide_plan: api_key_skipped path (ANTHROPIC_API_KEY 未設定 + 非 dry-run) で `("skipped", "api_key_missing")` payload + counts={} / artifacts=[] / cost=None の minimum payload。voicevox_narration: engine_skipped path (VOICEVOX engine 不在 + 非 require) で `("skipped", "engine_unavailable")` payload + 同 minimum shape。`_assert_v1_payload_common` (PR-AW shared helper) で v1 common field 全種を機械 lock-in、PR-AW (3 caller success+write-config) / PR-AX (compare_telop_split success / visual_smoke error) と相補で 7 v1 caller 全 audit 完了 + 2 件 regression test + caller fix 2 file | PR-AY |
| 52 | `STATUS_MAP` ↔ caller の forward direction lint 追加 (`test_observability_status_map_caller_usage_lint`、Codex 05:54 推奨 BJ)。`map_status()` は未知 v0_status を `("error", v0_status)` に defensive fallback するため、caller が typo / 改名 / 新規追加された status を渡しても runtime では即時 fail せず category=v0_status (PR-AN format invariant 違反するケース) で silent payload 通過する drift。既存 PR-T `test_observability_helper_status_map` は手書き `must_have` set 比較で caller 側 commit に同期更新が必要。本 lint は AST parse で 7 v1 caller (build_slide_data / build_telop_data / preflight_video / compare_telop_split / visual_smoke / generate_slide_plan / voicevox_narration) から literal v0_status を機械抽出: emit_json / _emit_early / _emit / emit_obs / _emit_error の 1st positional + build_status(v0_status=...) kwarg + `v0` / `v0_status` 変数への constant assign (visual_smoke 条件分岐 v0 fallback 含む、env_error は STATUS_MAP fallback handle 済で除外)。forward direction `set(caller_literals) - set(STATUS_MAP.keys())` を空集合で assert、未登録 literal あれば script:line 含む msg で fail、加えて 7 caller 全 source coverage の sanity check + 1 件 regression test。reverse direction (STATUS_MAP entry が caller literal で参照されない) は env_error 等の変数経由間接参照や defensive entry の余地があるため info-only。PR-T STATUS_MAP static lint / PR-AN category format invariant / PR-AW caller conformance と相補な caller↔map 双方向 audit の forward 直接 lock-in | PR-AZ |
| 53 | `build_status()` の v1_status / exit_code 整合性 contract 追加 (Codex 06:01 推奨 BL)。docs §Status Naming は `error = exit_code != 0` を明示しているが、旧実装は `ok` を v1_status から自動算出するだけで exit_code 整合性 (ok/skipped/dry_run → 0、error → 非 0) は caller 任せだった。caller 側 typo / refactor で `emit_json("success", 1)` 等の不整合を渡しても silent payload 通過、downstream consumer が `ok=True && exit_code=1` 矛盾 record を生む drift。fix: build_status 入口で (1) `v1_status='error' && exit_code == 0` → ValueError、(2) `v1_status in {'ok','skipped','dry_run'} && exit_code != 0` → ValueError で fail-loud。PR-AC exit_code int contract / PR-AP v0_status defensive lint と同 level の strict contract 層。`test_observability_build_status_exit_code_consistency` を追加 (126→127 件)、STATUS_MAP 全 entry を v1_status に応じた正 exit_code (error=3 / ok-skipped-dry_run=0) で accept loop + 4 reject case (error+0) + 4 ok-path×4 bad ec = 16 reject case で contract lock-in。caller side fix: 既存 `test_observability_build_status_v0_status_defensive_lint` の 1c 単一 char accept (exit_code=0 → 2) + 7 件 regression guard を v1_status に応じた exit_code map に更新 (success/api_key_skipped/dry_run/smoke_ok=0、rate_limited/cost_guard_aborted/ffprobe_failed=非0)。caller 既存 55 combos (AST scan で確認) は全件 contract 準拠で regression なし + 1 件 regression test | PR-BA |
| 54 | `build_status()` の `artifacts[i]` dict shape contract 追加 (Codex 06:11 推奨 BO)。docs §Common Fields は `artifacts: [{"path", "kind: json|wav|ts|png|..."}]` を contract として例示しているが、旧実装は dict 内 key 存在 / 値型 / kind 値 enum を検査せず、`{"path": "x.ts"}` (kind 欠落) や `{"kind": ""}` / `{"kind": "TYPESCRIPT"}` (UPPERCASE) / `{"kind": 5}` (int) を silent payload 通過、downstream consumer (release dashboard / asset audit / artifact bucket aggregator) の file format 集計 key drift を起こす。fix: 新 module 定数 `ARTIFACT_KIND_ENUM = frozenset({"ts", "json", "wav", "png", "mp3", "mp4"})` を追加 (caller-used 3 件 + 将来用 3 件、単一 source of truth)、build_status 入口の artifacts 検査 loop に (1) `path` key 必須 (ValueError on missing) + 値非空 str (TypeError)、(2) `kind` key 必須 (ValueError on missing) + 値 str (TypeError)、(3) `kind ∈ ARTIFACT_KIND_ENUM` (ValueError on unknown) を追加。`test_observability_build_status_artifact_kind_enum_contract` を追加 (127→128 件)、accept (enum 全 6 entry + 複数 artifact 各 valid) + reject (kind 欠落 / path 欠落 / kind 非 str 8 case / kind 未知 12 case (UPPERCASE / mixed / leading-trailing space / slash 含み等) / path 値型違反 7 case) で contract lock-in。既存 caller (build_slide_data:460 / build_telop_data:516 / compare_telop_split:230,238 ts、preflight_video:343 / visual_smoke:494 json、visual_smoke:503 png) は全 cover、`test_observability_build_status_counts_artifacts_strict` の path-only dict 想定 case を valid kind 付き dict / non-dict head 並べ替えで PR-BB contract に整合化。PR-AK artifacts list-of-dict / PR-AN STATUS_MAP category format invariant と同 level + 1 件 regression test | PR-BB |
| 55 | `build_status()` の counts 内 cell value 型 contract 追加 (Codex 06:18 推奨 BN)。PR-AK で counts dict shape (`is None or isinstance(dict)`) は固定済だが、dict 内 value 型は未検査だった。downstream aggregator (release dashboard / log analytics) は counts 内 value で sum / avg / diff を取るため、float (precision drift) / bool (True == 1 と int の conflate) / None (NoneType arith error) / tuple / set / object (JSON encode 不能) の混入は contract 違反。fix: build_status 入口の counts shape 検査直後に (1) key 必須 str (TypeError on non-str)、(2) value ∈ {int, str, dict, list} のみ accept (TypeError on bool / float / None / tuple / set / object)、(3) bool は int subclass で `True == 1` の conflate を避けるため明示 reject を追加。caller side fix: build_slide_data:472 の `used_plan: bool` を `int(used_plan)` (1/0) に正規化、他 caller (build_telop_data:525 telop_count/weaknesses int、visual_smoke:538 total/mismatched/formats_count/frames_count int、compare_telop_split:241 baseline_kpi/new_kpi/gates dict) は本 contract に整合済 (no caller fix needed)。`test_observability_build_status_counts_value_contract` を追加 (128→129 件)、accept (int/str/dict/list 全 9 cell type + 6 caller shape regression guard) + reject (bool 2 case / float/None/tuple/set/object 6 case / non-str key 5 case) で contract lock-in。PR-AK counts dict shape / PR-BB artifact kind enum と同 level の cell-value strict contract 層 + 1 件 regression test | PR-BC |
| 56 | docs §Common Fields ↔ build_status payload key の双方向整合性 lint 追加 (`test_observability_common_fields_docs_payload_key_lint`、Codex 06:25 推奨 BQ)。docs §Common Fields は v1 schema の正準 source として top-level field を JSON code block で列挙 (schema_version / script / status / ok / exit_code / category / duration_ms / counts / artifacts / cost / redaction / run_id / parent_run_id / step_id の 14 件)、PR-W は field order を固定済だが docs list ↔ actual payload key set の双方向一致は別軸。caller / consumer / 後続 refactor が片側だけ追加・削除すると docs と payload の field set が drift し downstream parser に missing key / unknown key が発生。本 lint は docs §Common Fields の JSON code block を regex で抽出 (2-space indent 直下の `"key":` のみ、nested cost.* / redaction.* / artifacts[].* は除外で 14 件取得)、build_status を全 optional field 付き (counts/artifacts/cost/duration_ms/run_id/parent_run_id/step_id 全 populate) で構築した payload と set 比較。`docs_keys - payload_keys` (missing in payload) と `payload_keys - docs_keys` (extra in payload) の双方を assert、drift があれば script:line + key 名 list 込みで fail。PR-T STATUS_MAP static lint / PR-AV docs migration steps numbering と同 level の docs/code 双方向 audit + 1 件 regression test | PR-BD |
| 57 | docs §Cost JSON Shape ↔ build_cost_payload output key の双方向整合性 lint 追加 (`test_observability_cost_json_shape_docs_payload_key_lint`、Codex 06:31 推奨 BV、PR-BD §Common Fields lint と同型を Cost JSON Shape に展開)。docs §Cost JSON Shape は nested cost object の正準 source として 8 field を JSON code block で列挙 (currency / estimate / rate_source / rate_input_usd_per_mtok / rate_output_usd_per_mtok / tokens_input / tokens_output / rate_missing)。caller / consumer / 後続 refactor が片側だけ追加・削除すると、docs と build_cost_payload output の field set が drift して downstream cost aggregator (rate_source による provider 別 sum / token count diff) に missing key / unknown key を生む。本 lint は docs §Cost JSON Shape の最初の JSON code block (future canonical schema) を regex で抽出、`build_cost_payload(estimate=0.001, rate_input=3.0, rate_output=15.0, tokens_input=1234, tokens_output=567)` で全 8 field populate output と set 比較。`docs_keys - payload_keys` (missing in payload) と `payload_keys - docs_keys` (extra in payload) 両方を assert。PR-BD §Common Fields lint と相補で docs/code 双方向 audit を v1 schema 全域に展開 + 1 件 regression test | PR-BE |
| 58 | docs §Status Naming 表 ↔ STATUS_MAP value 第 1 要素 (v1 status) の双方向整合性 lint 追加 (`test_observability_status_naming_docs_status_map_value_lint`、Codex 06:36 推奨 BU、PR-BD/BE 同型を canonical v1 status set に展開)。docs §Status Naming は v1 status の正準 4 値 (`ok` / `skipped` / `error` / `dry_run`) を表で定義、STATUS_MAP は各 entry の value 第 1 要素として v1 status を保持し、build_status payload `status` field を生成する。docs と code が drift すると、(a) docs に追記した新 status (例: partial) が code 未対応で fallback 'error' 経路に流れる、(b) code に追加した v1 status が docs 未掲載で consumer が contract 認識せず handle 漏れ、(c) typo (okay vs ok / erorr vs error) が片側だけ起きる。本 lint は docs heading 直後 markdown table を regex 抽出 (連続する `|` 行を 1 ブロックとして capture)、1 列目 backtick value 4 件と `{v1 for v1, _ in STATUS_MAP.values()}` を双方向 set diff で完全一致 assert (missing_in_code は docs 追記後 code 未対応 / typo を fail-loud、extra_in_code は code 追加後 docs 未掲載を fail-loud)。PR-T STATUS_MAP static lint (内部構造) / PR-AZ caller forward direction lint (caller→map) と相補な map↔docs 双方向 audit + 1 件 regression test | PR-BF |
| 59 | `build_cost_payload()` の currency / tokens_* cell-value 型 contract 追加 (Codex 06:42 推奨 BS)。PR-BE で docs §Cost JSON Shape ↔ build_cost_payload output key の双方向 set lint は積んだが、各 cell の値型は未検査の field がある (currency: docs example "USD" だが str 型 / 非空 enforce なし、tokens_input/tokens_output: docs example int だが int 型 / 非負 enforce なし、bool / float / str が silent payload 通過)。downstream cost aggregator (currency 別 sum / token total / per-token unit cost) は型不整合 / silent miscount を起こす drift。fix: build_cost_payload 入口の rate_source 検査直後に (1) currency: 非 None str (TypeError) + 非空 (ValueError)、(2) tokens_input / tokens_output: int (not bool) or None (TypeError on bool/float/str/list)、< 0 reject (ValueError) を追加。estimate / rate_*_usd_per_mtok は PR-AA NaN/Inf defense の silent coerce 設計を意図的に維持 (defense-in-depth)、本 contract は caller-passthrough の currency / tokens に限定。`test_observability_build_cost_payload_currency_tokens_value_contract` を追加 (132→133 件)、accept (default USD / JPY / tokens 0 boundary / tokens 100/200 通常) + reject (currency 8 type case (None/int/float/True/False/list/dict/tuple) + 空文字、tokens 9 type case × 2 label = 18 + 負 int 2 case) で contract lock-in。caller (generate_slide_plan で `tokens_input=estimated_input_tokens` `tokens_output=estimated_output_tokens_upper_bound` 双方 int passthrough) は本 contract に整合済 (no caller fix needed)。PR-AL rate_source env: prefix / PR-BC counts cell value contract と同 level の cell value strict contract 層 + 1 件 regression test | PR-BG |
| 60 | docs §Script Coverage Matrix ↔ code 7 v1-migrated caller set の双方向整合性 lint 追加 (`test_observability_script_coverage_matrix_docs_code_lint`、Codex 06:51 推奨 BX、PR-BD/BE/BF docs/code 双方向同型を v1 schema coverage 対象 script set に展開)。docs §Script Coverage Matrix は v1 migration 対象 7 script (build_slide_data / build_telop_data / preflight_video / compare_telop_split / visual_smoke / generate_slide_plan / voicevox_narration) を明記、`_observability.py` (helper) と `timeline.py` (library 性質で対象外) を明示的に区別する。code 側は PR-AZ STATUS_MAP caller usage lint の `caller_scripts` list と PR-AW/AX/AY caller conformance test 群が同 7 set を canonical source として使う。docs と code が drift すると、(a) 新 script 追加時に片側だけ追記して migration 半端、(b) script 改名時に片側だけ修正して 7 set 不一致、(c) timeline.py 等の対象外 script が誤って migration 対象に分類される。本 lint は docs heading 直後 ～ 次 heading までを section として抽出し backtick `*.py` name 取得、`_observability.py` (helper) + `timeline.py` (対象外) を exclude set で除外、残り 7 set と code canonical 7 set を双方向 set diff で完全一致 assert。加えて docs `**7 script**` bold count claim ↔ enumerate 数の整合性 sanity check (count typo 検出)。PR-BD §Common Fields key / PR-BE §Cost JSON Shape key / PR-BF §Status Naming value と同 level の docs/code 双方向 audit、本 lint は v1 schema coverage 対象 script set という別 axis を fix + 1 件 regression test | PR-BH |
| 61 | docs §Trace Context Convention env precedence 表 ↔ code constants (TRACE_*_ENV / MAX_TRACE_CONTEXT_VALUE_LEN) の双方向整合性 lint 追加 (`test_observability_trace_context_docs_code_lint`、Codex 07:02 推奨 BY、PR-BD/BE/BF/BH docs/code 双方向同型を Trace Context env contract に展開)。docs §env precedence 表は 3 env var (`SUPERMOVIE_RUN_ID` / `SUPERMOVIE_PARENT_RUN_ID` / `SUPERMOVIE_STEP_ID`) + cap (`MAX_TRACE_CONTEXT_VALUE_LEN = 128`) を定義、code は同 3 const + 同 cap 値を保持し `resolve_run_context()` が env precedence 仕様 (set+非空→pass-through、未設定→run_id だけ uuid4 auto-generate / parent・step は null) を実装。docs と code が drift すると、(a) env 名 typo (SUPERMOVE vs SUPERMOVIE) が片側だけで起き orchestrator pass-through 不能、(b) cap 値が docs 128 / code 256 等の不一致で payload に长 trace value 漏れ、(c) run_id auto-generate 仕様 docs 文言と code 実装の不一致。本 lint は docs §env precedence table の backtick env name 3 件 + cap 数値抽出、code constants と双方向 set diff + 値一致 assert、加えて docs run_id 行に `auto-generate` + `uuid` spec 残存 + parent/step 行に `auto-generate しない` / `null` fallback 仕様残存の sanity spot-check。PR-BD/BE/BF/BH と同 level の docs/code 双方向 audit、本 lint は trace context env contract という別 axis を fix + 1 件 regression test | PR-BI |
| 62 | `REDACTION_CLASSES` enum 導入 + docs §Sensitive Classes ↔ code 双方向 lint + `_normalize_redaction_rules()` membership reject (Codex 07:14 推奨 BV-redact、PR-BD/BE/BF/BH/BI 同型を sensitive class set に展開)。docs §Sensitive Classes は v1 redaction contract の正準 source として 4 class (secret / user_content / abs_path / provider_response_body) を表で定義しているが、code 側は class enum 単一 source がなく、`_normalize_redaction_rules()` は str/list 型と非空 entry のみ検査で「typo / 改名 / 旧名」を silent payload 通過させる drift。fix: 新 module 定数 `REDACTION_CLASSES = frozenset({"secret","user_content","abs_path","provider_response_body"})` を追加 (PR-BB ARTIFACT_KIND_ENUM と同型 single source of truth)、`_normalize_redaction_rules()` で entry が REDACTION_CLASSES set 外なら ValueError fail-loud。`test_observability_sensitive_classes_docs_code_lint` (135→136、docs heading 直後 markdown table の 1 列目 class identifier 抽出 + REDACTION_CLASSES 双方向 set diff) と `test_observability_normalize_redaction_rules_membership_reject` (136→137、4 class accept loop + multi-valid sorted unique + None/empty/tuple 互換 + 8 unknown rule reject + mixed valid+unknown reject + caller 想定 {abs_path, user_content} regression) を追加。caller-side fix: 既存 PR-AD `test_observability_build_status_redaction_rules_strict` の `["b","a","a","b"]` 重複 unique 検証を `["user_content","abs_path","abs_path","user_content"]` (実 enum value) に置換、duplicate / order semantics は維持。caller 既存 (build_slide_data:462 / build_telop_data:518 user_content + abs_path、preflight_video:347 / visual_smoke:508 / generate_slide_plan:244 abs_path) は本 contract に整合済 (no caller fix needed)。PR-BD/BE/BF/BH/BI 同 level の docs/code 双方向 audit + helper-side enforcement + 2 件 regression test | PR-BJ |
| 63 | docs §Redaction Rules ↔ helper / flag mapping presence lint 追加 (`test_observability_redaction_rules_helper_mapping_lint`、Codex 07:17 推奨 BV-rules、PR-BJ Sensitive Classes class set lint の rule-mapping extension)。docs §Redaction Rules は 4 class の各 rule に対応する helper / flag を明記 (secret → `redact_secret`、user_content → `--unsafe-show-user-content` opt-in + length/hash、abs_path → `safe_artifact_path` + `--unsafe-keep-abs-path`、provider_response_body → `--unsafe-dump-response` + structured summary)。本 lint は 4 helper (redact_secret / safe_artifact_path / user_content_meta / redact_provider_body) の callable 存在を全件 assert + docs §Redaction Rules で explicitly mention される 2 helper (redact_secret / safe_artifact_path) の docs↔code drift assert + 3 CLI flag (`--unsafe-show-user-content` / `--unsafe-keep-abs-path` / `--unsafe-dump-response`) の docs section mention 残存 + 7 v1 caller のいずれかでの存在を assert。helper rename / flag rename / docs spec drift / 実装削除 を fail-loud 検出。PR-BJ Sensitive Classes class set lint と相補な rule-side audit + 1 件 regression test | PR-BK |
| 64 | docs §Stdout And Stderr 表 ↔ caller stream emission pattern presence lint 追加 (`test_observability_stdout_stderr_stream_contract_lint`、Codex 07:28 推奨 BW、PR-BD/BE/BF/BH/BI/BJ/BK 同型を Stdout And Stderr 表に展開)。docs §Stdout And Stderr は v1 emission の 3 stream 役割を表で定義 (`stdout (human)` 進捗 message、`stdout (json tail)` 末尾 1 行 schema v1 JSON、`stderr` error/warning/stack trace)。本 lint は docs heading 直後 markdown table から 3 stream label の docs presence assert (label rename / 表 構造変更 fail-loud) + V1_CALLER_SCRIPTS 7 caller それぞれを `ast.parse()` で AST walk し 3 stream emission primitive を全持ちすることを Call node 検出で audit ((1) `ast.Call(func=Name('print'))` で keyword 'file' 無し = human stdout、(2) `ast.Call(func=Name('_obs_emit_json'))` Call node 1 件以上 = json tail (caller scripts は `import emit_json as _obs_emit_json` で alias 済、behavioral 系 drift は PR-AW/AX/AY caller conformance test で別途 cover)、(3) `print(..., file=<word>.stderr)` keyword.value Attribute / `<word>.stderr.write(...)` Call attr で stderr stream、build_telop_data の `_sys.stderr` alias も AST `_is_stderr_value()` で確実 match)。caller の stream 欠落 drift を fail-loud。Codex 07:34/07:36/07:42/07:46/07:54 の P2 review 5 iteration を経て (regex `[^)]*file=` greedy 終端 → AST 切替、`def emit_json(` raw text false-positive → Call node、wrapper depth tracking で main/cli mis-classification + module-level main() trivial pass → simplify、最終的に `_obs_emit_json` Call presence のみの static lint に収束、wrapper-vs-call 区別 drift は behavioral conformance test 側で cover する設計に決定)。PR-BD/BE/BF/BH/BI/BJ/BK 同 level の docs/code 双方向 audit、本 lint は stream 振り分け axis を fix + 1 件 regression test | PR-BL |
| 65 | docs §User Content Policy ↔ `user_content_meta()` output keys の双方向整合性 lint 追加 (`test_observability_user_content_policy_docs_meta_key_lint`、Codex 07:54 推奨 CA、PR-BD/BE/BF/BH/BI/BJ/BK/BL 同型を user_content meta key set に展開)。docs §User Content Policy は user_content default emission の構造を `length / sha256 hash` の 2 field と明記、code 側 `user_content_meta()` は `{length: int, sha256: str}` の dict を返す。docs と code が drift すると、(a) docs に新 field を書いたが code 未対応で consumer KeyError、(b) code に新 field を追加したが docs 未掲載で undocumented 拡張、(c) field rename (sha256→hash / length→text_length 等) が片側だけ起きる。本 lint は docs section から literal token (`length`, `sha256`) を抽出 + 任意 input string で `user_content_meta()` を呼んだ output dict の keys と双方向 set diff で完全一致 assert、加えて値型 (length=int / sha256=str) の sanity check (PR-AH `test_observability_sha256_hash_format_invariant` と相補)。PR-BD/BE/BF/BH/BI/BJ/BK/BL 同 level の docs/code 双方向 audit、本 lint は user_content meta dict shape という別 axis を fix + 1 件 regression test | PR-BM |
| 66 | docs §Path Policy ↔ `safe_artifact_path()` / `_lexical_redact()` の path placeholder set 双方向整合性 lint 追加 (`test_observability_path_policy_placeholder_set_docs_code_lint`、Codex 07:58 推奨 BZ、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM 同型を path placeholder set に展開)。docs §Path Policy は abs path redaction の placeholder set として `<HOME>` / `<TMP>` / `<ABS>` の 3 種を明記、code 側 `safe_artifact_path()` / `_lexical_redact()` body は同 3 placeholder string literal で sub する設計。docs と code が drift すると、(a) docs に新 placeholder を追記したが code 未対応で観測不能、(b) code に新 placeholder を追加 (例: `<MNT>` for /mnt/ paths) したが docs 未掲載で consumer classification 漏れ、(c) placeholder rename (`<HOME>` → `~/`) が片側だけ起きる。本 lint は docs §Path Policy section から backtick で囲まれた angle-bracket token (`<HOME>` / `<TMP>` / `<ABS>` 等) を抽出 + helper 関数 (`safe_artifact_path` / `_lexical_redact`) の AST FunctionDef body を `ast.get_source_segment` で抽出 (regex の indented body greedy match は blank line を `\s*\n` で吸って catastrophic backtracking 発生のため AST 経由)、その body 内 `"<X>"` / `'<X>'` / f-string prefix `f"<X>/` literal 全種から path placeholder を集めて双方向 set diff で完全一致 assert。helper 関数自体の rename / 移動も AST 抽出で `target_fns` set 一致を検証。docs 別 section の `<PROVIDER>` / `<DIR>` (rate_source canonical 例示) 等 path 以外の placeholder は helper body 内に存在しないため自動 filter。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM 同 level の docs/code 双方向 audit、本 lint は path redaction placeholder set という別 axis を fix + 1 件 regression test | PR-BN |
| 67 | docs §Rate Env Var Convention ↔ caller (`generate_slide_plan.py`) の `SUPERMOVIE_RATE_*` env name 双方向整合性 lint 追加 (`test_observability_rate_env_var_convention_docs_code_lint`、Codex 08:09 推奨 CB、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN 同型を rate env naming convention に展開)。docs §Rate Env Var Convention は cost rate env name の canonical 命名規約として v1 pattern `SUPERMOVIE_RATE_<PROVIDER>_<DIRECTION>_USD_PER_MTOK` (例 `SUPERMOVIE_RATE_ANTHROPIC_INPUT_USD_PER_MTOK`) + Anthropic 専用 v0 alias 2 件 (`SUPERMOVIE_RATE_INPUT_PER_MTOK` / `SUPERMOVIE_RATE_OUTPUT_PER_MTOK`、backward compat) を明記、code 側 `generate_slide_plan.py` `_resolve_decimal()` は同 4 env を参照。docs と code が drift すると、(a) caller が docs 規約外 env name を使い orchestrator が rate set できず estimate=null fallback、(b) docs v0 alias が宣言される一方 caller 削除で backward compat 喪失、(c) 新 provider 追加で命名規約と矛盾、(d) Anthropic v1 input/output の片方欠落で asymmetric drift。本 lint は (1) docs heading 直後 section から v1 canonical pattern literal + v0 alias 2 件の docs presence assert、(2) `generate_slide_plan.py` AST から `^SUPERMOVIE_RATE_[A-Z_]+$` 形式 string Constant を walk 抽出、(3) 各 caller env name が v1 canonical regex `^SUPERMOVIE_RATE_[A-Z]+_[A-Z]+_USD_PER_MTOK$` または v0 alias set のいずれかに該当する forward direction (caller→convention) assert、(4) docs v0 alias 2 件が caller で実参照されていることの backward 方向 assert、(5) caller の Anthropic v1 input/output pair の両側 presence (asymmetric drift 防止) を audit。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN 同 level の docs/code 双方向 audit、本 lint は rate env naming convention という別 axis を fix + 1 件 regression test | PR-BO |
| 68 | docs §v0 → v1 status mapping table ↔ `STATUS_MAP` の forward strict + v1 mapping consistency lint 追加 (`test_observability_status_mapping_policy_docs_code_lint`、Codex 08:18 推奨 CC、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO 同型を Migration Policy normative spec axis に展開)。docs §v0 → v1 status mapping は新規 script 追加時の normative table として 6 explicit v0 status row (`success` slide-plan / `success` narration / `api_key_skipped` / `engine_skipped` / `list_speakers` / `dry_run`) と 5 named error variants (`cost_guard_arg_invalid` / `inputs_missing` / `rate_limited` / `api_http_error` / `llm_json_invalid`) を parenthetical で記載、各 row は v1 canonical (ok / skipped / error / dry_run) を割り当てる。code 側 `STATUS_MAP` (`_observability.py`) は v0 → (v1, category) tuple を保持する正準 source で、`map_status()` 経由で build_status payload の `status` field を生成する。docs と code が drift すると、(a) docs 新規 row 追記後 STATUS_MAP 未登録 → defensive fallback `("error", v0_status)` 経路に流れ silent payload misclassification + PR-AN category format invariant 違反、(b) docs v0 名 typo (例 `api_key_skipped` ↔ `api_key_skip`) で片側だけ rename、(c) v1 mapping が docs と STATUS_MAP value で食い違う (例 docs に `api_key_skipped → ok` で code は `skipped` のまま) と downstream aggregation の status bucket 崩れ。本 lint は (1) docs heading 直後 section から table 抽出 (`### v0 → v1 status mapping` ～ 次 heading)、(2) 1st column backtick literal から v0 名 + 2nd column backtick literal から v1 canonical を集める。`error variants` 行は parenthetical 内 backtick literal 5 件を別途抽出 (1st backtick `error` は説明用語で skip)、(3) `_observability.STATUS_MAP` を import、(4) forward 5 explicit non-error v0 status (`success` 同 v0 を 1 row に統合): STATUS_MAP 存在 + table v1_canonical 列と `STATUS_MAP[v0][0]` 一致 (mapping consistency)、(5) forward 5 explicit error variants: STATUS_MAP 存在 + `STATUS_MAP[v0][0] == 'error'`、(6) reverse direction (STATUS_MAP entry が docs 未掲載) は table 末尾の `等` 拡張表記で開いているため strict 化できず info-only (本 lint は forward + mapping consistency のみ lock-in、PR-AZ caller forward / PR-T STATUS_MAP static lint と相補)。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO 同 level の docs/code 双方向 audit、本 lint は Migration Policy normative spec ↔ STATUS_MAP value mapping という別 axis を fix + 1 件 regression test | PR-CC |
| 69 | docs §Missing Rate Behavior ↔ `_resolve_decimal()` / `STATUS_MAP["cost_guard_arg_invalid"]` / generate_slide_plan の `except ValueError` 経路 双方向 lint 追加 (`test_observability_missing_rate_behavior_docs_code_lint`、Codex 08:27 推奨 CD、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC 同型を cost guard rate validation contract axis に展開)。**併せて docs ↔ code drift 修正**: 旧 docs §Missing Rate Behavior bullet 2 は `category = "rate-invalid"` を spec していたが、code 側 `STATUS_MAP` には `rate-invalid` entry が存在せず実際の emission は `cost_guard_arg_invalid` を使う drift が PR-CD investigation で発覚 (Bash grep 実測: `rate-invalid` は code に 0 hit、`cost_guard_arg_invalid` は STATUS_MAP + generate_slide_plan の except ValueError 経路で 4 hit)。docs を code に合わせて `category = "cost_guard_arg_invalid"` / `exit_code = 4` に修正、加えて再 drift を fail-loud 化する lint を追加。docs §Missing Rate Behavior bullet 1 (rate 未設定): `estimated_cost_usd_upper_bound = null` + `rate_missing = true` + `status = ok`、bullet 2 (rate invalid: NaN / Inf / -Inf / 負値 / decimal parse 不能等): `_resolve_decimal()` ValueError → `except ValueError` 経路で `status = error` / `category = "cost_guard_arg_invalid"` / `exit_code = 4`。本 lint は 4-part validation: (1) docs §Missing Rate Behavior section に bullet 1 contract literal + bullet 2 contract literal の docs presence (`estimated_cost_usd_upper_bound`, `rate_missing`, `status = ok` / `status = error`, `"cost_guard_arg_invalid"`, `exit_code = 4`, `_resolve_decimal`, `except ValueError` の 8 token)、(2) `STATUS_MAP["cost_guard_arg_invalid"] == ("error", "cost_guard_arg_invalid")` mapping 確認、(3) `_resolve_decimal()` AST 抽出 + exec 経由で 5 invalid input case (NaN / Infinity / -Infinity / -1.5 / "not-a-number") が ValueError raise + 各 error message に期待 substr 含むこと + finite/non-negative 2 case (0 / 3.5) が pass-through する regression guard、(4) `generate_slide_plan.py` AST に `emit_json("cost_guard_arg_invalid", 4, ...)` Call literal 存在 + 2nd arg exit_code が 4 であること (except ValueError 経路 wiring)。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC 同 level の docs/code 双方向 audit、本 lint は cost guard rate validation contract という別 axis を fix + 1 件 regression test | PR-CD |
| 70 | docs §Cost Abort Threshold ↔ `STATUS_MAP["cost_guard_aborted"]` / generate_slide_plan の pre-API abort 経路 wiring 双方向 lint 追加 (`test_observability_cost_abort_threshold_docs_code_lint`、Codex 08:34 推奨、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC/CD 同型を pre-API cost abort contract axis に展開)。docs §Cost Abort Threshold は PR-F で確立した contract を 5 bullet で定義: (1) `SUPERMOVIE_COST_USD_ABORT_AT` env / `--cost-abort-at` CLI で USD 上限値、超過時 `status=error / category=cost_guard_aborted / exit_code=10`、(2) precedence `CLI > env > None`、(3) rate 未設定 (`estimated_cost_usd_upper_bound=None`) 時は閾値設定があっても skip (cost 不明で勝手に abort せず通常進行)、(4) abort emission payload に `estimated_cost_usd_upper_bound` / `cost_abort_at` / `estimated_input_tokens` / `estimated_output_tokens_upper_bound` 4 keys を含む、(5) PR-F 関連 test 3 件 (`test_generate_slide_plan_cost_abort_blocks_api_when_estimate_exceeds` / `_skipped_when_rate_unset` / `_cli_overrides_env`)。docs と code が drift すると、(a) `exit_code=10` 以外の値で emit すると runbook / dashboard alarm trigger 条件が壊れ、(b) `cost_guard_aborted` category 名 rename が片側だけ起きると payload filter drift、(c) abort payload 4 keys 片側削除で cost analytics 欠落、(d) rate 未設定 skip 条件 (AND ガード) 片側削除で `estimate=None` でも abort 経路に流れ silent failure、(e) docs 関連 test name rename / 削除で docs reference 切れ。本 lint は 5-part validation: (1) docs section bullet 1-4 contract literal 9 token presence (`SUPERMOVIE_COST_USD_ABORT_AT`, `--cost-abort-at`, `category=cost_guard_aborted`, `exit_code=10`, `CLI > env > None`, `estimated_cost_usd_upper_bound=None`, `estimated_input_tokens`, `estimated_output_tokens_upper_bound`, `cost_abort_at`) + bullet 5 関連 test 3 件 docs presence、(2) `STATUS_MAP["cost_guard_aborted"] == ("error", "cost_guard_aborted")` mapping consistency、(3) `generate_slide_plan.py` AST に `emit_json("cost_guard_aborted", 10, ...)` Call literal 存在 + 2nd arg exit_code == 10 + abort kwargs 4 keys 全 presence (estimated_cost_usd_upper_bound / cost_abort_at / estimated_input_tokens / estimated_output_tokens_upper_bound)、(4) abort 判定 if 文に `cost_abort_at is not None` AND `estimated_cost_usd_upper_bound is not None` の AND ガード AST 存在 (BoolOp(And) + Compare(IsNot, None) × 2 で機械検証、片側削除 → silent abort drift fail-loud)、(5) docs bullet 5 関連 test 3 件が `test_timeline_integration.py` に FunctionDef として登録済 (rename / 削除で docs reference 切れ fail-loud)。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC/CD 同 level の docs/code 双方向 audit、本 lint は pre-API cost abort threshold contract という別 axis を fix + 1 件 regression test | PR-CE |
| 71 | docs §Provider Notes 4 row ↔ provider routing isolation 双方向 lint 追加 (`test_observability_provider_notes_routing_docs_code_lint`、Codex 08:40 推奨、PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC/CD/CE 同型を Provider Notes table axis に展開)。docs §Provider Notes は 4 provider の cost unit / rate kind 分類表を持ち、Anthropic API (slide plan) は `$/MTok input + $/MTok output` の text generation で `env 必須`、Gemini API (image gen) は `$/image` の image generation で別 env 系 (本 doc 後の別 PR で固定)、VOICEVOX (narration) は `local engine` で `cost なし` / `telemetry 対象外`、Kling / 他 は `将来` provider 個別で別 doc 化、と分類している。docs と code が drift すると、(a) docs 4 provider の片方の row が削除 / rename されると caller が docs を信じて誤った env / cost 経路を実装、(b) Anthropic row の `env 必須` 注釈消失で新 caller が rate env を passthrough 失念 → estimate=null silent failure、(c) VOICEVOX row の `cost なし / telemetry 対象外` claim が drift して voicevox_narration.py に誤って Anthropic/Gemini rate env 参照を追加すると provider 経路の混在 / cost 二重計上、(d) Gemini row の `別 env 系` 注釈消失で Anthropic env を流用しようとする drift。本 lint は 4-part validation: (1) docs §Provider Notes section に 4 row 必須 keyword presence (Anthropic API + $/MTok + env 必須、Gemini API + $/image、VOICEVOX + local engine + cost なし + telemetry 対象外、Kling + 将来 の合計 11 keyword)、(2) Anthropic provider routing: `generate_slide_plan.py` AST に `SUPERMOVIE_RATE_ANTHROPIC_INPUT_USD_PER_MTOK` + `SUPERMOVIE_RATE_ANTHROPIC_OUTPUT_USD_PER_MTOK` の 2 env literal 参照あり (PR-BO 同型 caller→docs 整合の Anthropic provider 視点)、(3) VOICEVOX provider isolation: `voicevox_narration.py` AST walk で `^SUPERMOVIE_RATE_[A-Z_]+$` 形式 string Constant が 0 件 (cost なし契約 lock-in)、(4) VOICEVOX cross-provider isolation: `voicevox_narration.py` source text に `Anthropic` / `Gemini` 語が出現しない (provider 経路混在 fail-loud、telemetry 対象外契約)。PR-BD/BE/BF/BH/BI/BJ/BK/BL/BM/BN/BO/CC/CD/CE 同 level の docs/code 双方向 audit、本 lint は provider 分類表 ↔ provider 経路 isolation という別 axis を fix + 1 件 regression test | PR-CF |
| 72 | V1_CALLER_SCRIPTS 7 caller の `_obs_emit_json` / `emit_json` / wrapper bare Call site が同一 stmt list 内で stdout 末尾 1 行 invariant を破る trailing stdout writer に followed されないことを AST scan で audit する caller-side static lint 追加 (`test_observability_emit_json_tail_invariant_caller_ast_lint`、Codex 08:46 推奨 BT、PR-Y emit_json helper format invariant + PR-BL stream emission presence lint と相補な caller-side single-line tail invariant axis)。docs §Stdout And Stderr line 104 は `--json-log` consumer が **stdout の末尾 1 行のみを JSON parse** する想定を明記、helper 側 `emit_json()` は 1 line + final newline + `splitlines()[-1]` 前提で固定済 (PR-Y / PR-AC)。一方 caller 側は PR-AZ STATUS_MAP forward direction lint / PR-BL stream emission presence lint まではあるが、emit_json 後に追加 stdout writer (`print("done")` / `sys.stdout.write(...)` 等) が同一 body 内で sibling stmt として出現すると末尾 1 行 invariant を壊す drift が静的検出されていなかった。本 lint は (1) V1_CALLER_SCRIPTS 7 caller (build_slide_data / build_telop_data / preflight_video / compare_telop_split / visual_smoke / generate_slide_plan / voicevox_narration) それぞれの AST を walk、(2) FunctionDef.body / If.body / If.orelse / Try.body / Try.orelse / Try.finalbody / For.body / While.body 等 body-bearing node の stmt list 単位で `emit_json` / `_obs_emit_json` / `_emit_early` / `_emit` / `emit_obs` / `_emit_error` の 6 EMIT_FUNC_NAMES Call を Expr 文として呼ぶ site (bare Call) を検出、(3) 同 stmt list 内 sibling として AFTER に `print(...)` Call (`file=stderr` 系 keyword なし → stdout 扱い) または `sys.stdout.write(...)` Call attr が出現したら drift fail-loud、(4) Return 文 / sys.exit(Call) wrap されている emit_json Call は flow がそこで終わるので audit 対象外 (検査は bare Expr のみ)。加えて docs §Stdout And Stderr line 104 の「stdout の末尾 1 行のみを JSON parse」契約文言 docs presence も 1 行 assert で同一 lint で fail-loud。drift 検知例: `_obs_emit_json(args.json_log, payload)` の直後 sibling に `print("done!")` が来るケース。PR-Y emit_json helper format invariant / PR-BL stream emission presence と相補、本 lint は emit_json bare Call → trailing stdout writer drift という別 axis を fix + 1 件 regression test | PR-BT |
| 73 | V1_CALLER_SCRIPTS 7 caller の `_obs_emit_json` 直接 Call (helper alias bypass 経路) の 1st positional arg = `args.json_log` Attribute (or `json_log` Name) であることを AST audit する caller-side disabled silent invariant lint 追加 (`test_observability_emit_json_disabled_silent_caller_ast_lint`、Codex 08:55 推奨 BR、PR-BT trailing stdout writer audit と相補な `--json-log` disabled silent contract caller-side 形式固定)。docs §Emission Contract bullet 1 (line 50) は「`--json-log` flag が指定された時のみ、stdout の **末尾 1 行** に純 JSON (改行で終端) を emit する」契約を明示、helper 側 `emit_json(enabled, payload)` は `if enabled: print(...)` で disabled 時 silent (PR-Y / PR-AC で固定済)。一方 caller 側は `_obs_emit_json(args.json_log, payload)` 形を 7 caller × 11 sites で揃えているが (Bash 実測)、(a) 1st arg を hardcoded `True` に書き換えると `--json-log` 無関係に常時 emit、disabled silent invariant 崩壊 + human stdout 経路 + downstream parser 双方を汚染、(b) hardcoded `False` で常時 silent → CLI 指定時も status 受信できず observability hole、(c) args 以外の Name 経由で flag propagation chain 破壊、と drift risk あり。本 lint は 3-part validation: (1) docs §Emission Contract section に 「`--json-log`」「指定された時のみ」 disabled silent contract 文言 docs presence (docs spec drift fail-loud)、(2) V1_CALLER_SCRIPTS 7 caller AST から `_obs_emit_json(...)` Call site 全列挙し 1st positional arg が `Attribute` (attr=`json_log`、例 `args.json_log`) または `Name` (`id` に `json_log` 含む、loop local var 経由許容) であることを assert、`Constant` (hardcoded True/False/None/数値) は reject、各 caller 最低 1 件の Call site 存在 lock-in、合計 7 件以上の Call site lock-in、(3) 各 caller に `from _observability import emit_json as _obs_emit_json` alias import が AST ImportFrom node として存在 (alias rename / 削除 drift fail-loud)。PR-BT trailing stdout writer audit / PR-BL stream emission presence / PR-Y helper format invariant と相補、本 lint は emit_json disabled silent contract の caller-side 形式 audit という別 axis を fix + 1 件 regression test | PR-BR |
| 74 | docs §env precedence (Trace Context Convention) table semantic claims ↔ `resolve_run_context()` runtime behavior 双方向 lint 追加 (`test_observability_trace_context_precedence_semantics_lint`、Codex 09:01 推奨 PR-CG、PR-BI Trace Context env name + cap 双方向 lint と相補な precedence semantic axis)。docs §env precedence table は 3 row precedence claims (`SUPERMOVIE_RUN_ID`: 設定+非空 → そのまま採用、未設定 → `uuid.uuid4().hex` 32 char auto-generate / `SUPERMOVIE_PARENT_RUN_ID`: 設定+非空 → そのまま、未設定 → null auto-generate しない / `SUPERMOVIE_STEP_ID`: 同) + cap (`MAX_TRACE_CONTEXT_VALUE_LEN = 128`、超過時 `TraceContextError` raise truncation せず error) + rationale (parent/step を auto-generate しない理由 = 関係性情報なので呼出元 orchestrator が明示注入していない時に偽値を作ると trace tree が破綻するため) + 関連 test 3 件 (PR-E)。PR-BI は env 名 + cap 値の双方向 lint まで止まっており、precedence semantic (run_id auto-gen / parent/step never auto-gen / cap 超過 raise) が docs claims 通りに helper で動いているかは静的に未検証。drift risk: (a) helper が parent/step を generate_if_missing=True で auto-gen するように改修 → docs rationale (trace tree 破綻防止) 違反、(b) run_id auto-gen が uuid4 hex 32 char でなく別 format (uuid7 / 短縮 hash 等) に変わると downstream parser drift、(c) cap 超過時に truncate 動作に変わると docs `TraceContextError raise` contract 違反、(d) docs 関連 test 名 が rename / 削除で docs reference 切れ。本 lint は 5-part validation: (1) docs §env precedence section に 3 env name + uuid.uuid4().hex / 32 char / auto-generate しない / MAX_TRACE_CONTEXT_VALUE_LEN = 128 / TraceContextError / trace tree が破綻 の 9 token literal docs presence + 関連 test 3 件 (`test_observability_resolve_run_context_uses_env` / `_generates_when_missing` / `_no_generate`) docs presence、(2) helper signature: `_observability.resolve_run_context` 関数存在 + `inspect.signature` で kwargs に `generate_if_missing` 含む (rename / 削除 fail-loud)、(3) run_id auto-gen: 3 env 全 unset + `generate_if_missing=True` で呼び、戻り値 run_id が `re.match(r"^[0-9a-f]{32}$")` 一致 + parent_run_id / step_id は None (parent/step never auto-gen 契約 lock-in、docs rationale 違反 fail-loud)、(4) env pass-through: 3 env 全設定 (短い値 rid-abc/parent-xyz/step-001) で呼び、戻り値 3 field が env そのまま採用、(5) cap 違反 raise: 各 env に cap+1 = 129 char を個別設定 → `TraceContextError` raise (truncation せず error 契約) を 3 field 個別検証、+ env 復元を try/finally で保証。PR-BI Trace Context env name + cap 双方向 lint と相補、本 lint は runtime semantics の docs/code drift 検出という別 axis を fix + 1 件 regression test | PR-CG |
| 75 | `template/scripts/normalize_fixture.sh` recipe contract ↔ `docs/PHASE3_RELEASE_NOTE.md §b1 fixture normalize evidence trail` section 双方向 lint 追加 (`test_observability_normalize_fixture_recipe_evidence_trail_lint`、Codex 09:08 推奨 PR-Q1、Q full ではなく Q-1: fixture e2e smoke evidence + minimal guard、observability follow-up cycle 18 件 docs/code audit に続く runtime fixture-side guard)。docs §b1 fixture normalize evidence trail (PHASE3_RELEASE_NOTE.md line 171, 193, 202-215) は b1 fixture (HEVC HDR DoVi 4K) を Remotion 互換の H.264 SDR / yuv420p / bt709 / Display Matrix なし に正規化する recipe contract を明記、script 自体は (a) bash shebang、(b) `set -euo pipefail`、(c) idempotent skip 経路 (H.264 SDR + Display Matrix なし + risks=[] で no-op exit 0)、(d) ffprobe で Display Matrix side_data 検出、(e) 後段 ffprobe gate で post-normalize Display Matrix 残存 fail-loud、を持つ。docs と script が drift すると、(a) script path rename で docs 実行例 invocation 壊れ、(b) idempotent skip 削除で re-run 時 backup file 大量生成、(c) ffprobe gate 削除で Display Matrix 残存 silent 成功 → Remotion sideways drawing regression 再発、(d) `set -euo pipefail` 削除で error path silent failure。本 lint は 5-part validation: (1) script 存在 + 1 行目 `#!/usr/bin/env bash` shebang + `set -euo pipefail` safety guard、(2) script body 必須 contract keyword (`Display Matrix` / `ffprobe` / `idempotent` or `Idempotent` (case-insensitive) / `exit 0` / `backup` / `risks=[]`) 6 件 presence、(3) docs §b1 fixture normalize evidence trail section に script path + `b1 transcode` / `idempotent recipe` / `ffprobe gate` / `Display Matrix` の 5 keyword docs presence、(4) docs section 内に `bash template/scripts/normalize_fixture.sh ...` invocation 実行例 1 件以上 docs presence、(5) script の post ffprobe gate (`Display Matrix metadata still present` error message or `POST_DM` 変数経由) literal 存在 (gate 削除 fail-loud)。Q full (実 main.mp4 + node_modules + render e2e) ではなく Q-1 minimum guard scope。observability follow-up cycle 18 件 docs/code audit に続く runtime fixture-side の最小 guard、新規 medium axis 追加せず existing fixture recipe の docs/script 双方向 audit + 1 件 regression test | PR-Q1 |
| 76 | `scripts/check_release_ready.sh` の composite 7-gate 定義 ↔ `docs/PHASE3_RELEASE_NOTE.md` の 7 gate composite 記述 双方向 lint 追加 (`test_observability_composite_7gate_release_note_drift_lint`、Codex 09:32 推奨 PR-R1、PR-Q1 fixture recipe ↔ release note 双方向 audit と同型を release readiness gate axis に展開)。`scripts/check_release_ready.sh` は Phase 3 release readiness の composite gate runner で、7 gate 構成 (gate 1 env / gate 2 worktree clean / gate 3 regen verify / gate 4 integration smoke test / gate 5 TS compile surface optional / gate 6 React component test optional / gate 7 anchor drift check) を 2 style 混在で実装: gates 1-2 は `echo "  [OK]   env:..."` / `echo "  [OK]   worktree:..."` 形式の implicit 結果 line、gates 3-7 は `echo "--- xxx ---"` section header 形式。`docs/PHASE3_RELEASE_NOTE.md` line 76 / 145 / 160 は「7 gate composite」claim + `bash scripts/check_release_ready.sh` invocation + `ALL PASS` claim を持つ。docs と script が drift すると、(a) script に gate 追加 / 削除した時に release note の "7 gate composite" 文言が古いまま (gate 8 / 6 化など) → CI 失敗時の人為診断ミス、(b) gate 名称 rename (例: anchor drift → freeze sentinel) で release note ↔ runbook 不整合、(c) `bash scripts/check_release_ready.sh` invocation 削除で実行例 reference 切れ、(d) `=== ALL GATES PASS ===` terminator が release note の `ALL PASS` claim と food chain で食い違うと PR description 自動化壊れ。本 lint は 5-part validation: (1) `scripts/check_release_ready.sh` ファイル存在 + 1 行目 shebang、(2) `^echo "--- (.*) ---"` regex で section header 5 件 (gates 3-7 explicit) + `[OK] env:` / `[OK] worktree:` regex で gates 1-2 implicit 経路 + `=== ALL GATES PASS ===` terminator literal で composite 7 gate 数 lock-in、(3) explicit 5 header に必須 keyword (`regen verify` / `smoke` / `anchor drift`) 含むこと (TS / React は optional 化済で外し)、(4) docs 内に `7 gate composite` literal 1 件以上 docs presence (gate count claim の "6 gate" / "8 gate" silent rewrite fail-loud)、(5) docs 内に `bash scripts/check_release_ready.sh` invocation literal 1 件以上 + `ALL PASS` claim literal 存在 (script terminator との food chain 整合 lock-in)。PR-Q1 fixture recipe ↔ release note 双方向 audit と同型、本 lint は composite 7-gate 定義の docs/script drift という別 axis を fix + 1 件 regression test | PR-R1 |
| 77 | `generate_slide_plan.py` / `voicevox_narration.py` の local `emit_json(status: str, exit_code: int, ...)` wrapper signature type annotation ↔ 全 call site での positional arg literal type contract 双方向 lint 追加 (`test_observability_emit_json_local_wrapper_positional_type_contract_lint`、Codex 09:xx 推奨 PR-S1、PR-BR disabled silent caller audit / PR-BT trailing stdout audit と相補な local wrapper positional args type contract axis)。`generate_slide_plan.py` / `voicevox_narration.py` は `main()` 内に local `emit_json(status: str, exit_code: int, ...)` wrapper を定義し、v0 status string + exit_code int を受け取り `build_status()` 経由で v1 payload を組み立てて `_obs_emit_json(args.json_log, payload)` に委譲する設計。`emit_json` helper (PR-AC) は `payload['exit_code']` の bool subclass reject + int 限定 contract を fail-loud 化済だが、local wrapper call site で `exit_code` に `True`/`False` (bool は int subclass) を渡すと helper 入口 contract を素通りして `json.dumps` に bool が流れ、`1`/`0` ではなく `true`/`false` として downstream parser の int 前提 exit_code 解析を壊す。`status` に変数を渡すと v0 status 名の静的確認ができず STATUS_MAP fallback 経路のサイレント drift を発見できない。本 lint は 4-part validation: (1) V1_CALLER_SCRIPTS 中 local `emit_json` FunctionDef を持つ scripts が `{generate_slide_plan.py, voicevox_narration.py}` と set-equal (wrapper 追加 / 削除 drift fail-loud)、(2) 各 wrapper signature: `args[0].arg == "status"` + `annotation == str`、`args[1].arg == "exit_code"` + `annotation == int` (rename / annotation 削除 fail-loud)、(3) 各 script の wrapper 外 call site 全件: `args[0]` が `ast.Constant(value: str)` (variable / int / bool 拒否)、`args[1]` が `ast.Constant(value: int, not bool)` (True/False 拒否、bool subclass であっても reject)、(4) docs §Emission Contract section に local wrapper type contract 必須 keyword (`emit_json(status: str, exit_code: int` / `not bool` / `generate_slide_plan` / `voicevox_narration`) presence (docs spec drift fail-loud)。PR-AC exit_code int contract / PR-BR disabled silent caller audit と相補、本 lint は local wrapper positional args type contract という別 axis を fix + 1 件 regression test | PR-S1 |
| 78 | V1_CALLER_SCRIPTS 7 scripts の emit helper call site で使われる全 v0_status 文字列が `STATUS_MAP` に登録済であることを AST audit で検証する per-script v0_status enumeration completeness lint 追加 (`test_observability_v0_status_enumeration_completeness_lint`、Codex 推奨 PR-S3、PR-S1 local wrapper positional type contract と相補な v0_status 登録漏れ detection axis)。7 scripts それぞれの emit helper 関数 (`_emit_error` / `_emit` / `_emit_early` / `emit_obs` / `emit_json`) call site で arg[0] str Constant を収集 + `v0_status=` keyword str Constant を収集し、全件 `STATUS_MAP` key set に含まれることを assert。未登録 v0_status は `map_status()` defensive fallback `("error", v0_status)` を通るが category が劣化し dashboard alarm / runbook の category フィルタが miss する silent drift を fail-loud 化。本 lint は 3-part validation: (1) `SCRIPT_EMIT_HELPERS` key set が `V1_CALLER_SCRIPTS` 7-set と set-equal (helper mapping 追加/削除 drift fail-loud)、(2) 各 script AST walk: helper def 内 Call を除外したうえで emit helper arg[0] str Constant + 任意 Call の `v0_status=` keyword str Constant を全収集し全件 `STATUS_MAP` key set に含まれることを assert (未登録 v0_status fail-loud)、(3) docs §Status Naming section に `v0_status` / `STATUS_MAP` / `enumeration completeness` 3 keyword presence (docs spec drift fail-loud)。PR-S1 local wrapper positional type contract と相補、本 lint は v0_status 登録漏れ という別 axis を fix + 1 件 regression test | PR-S3 |
| 79 | V1_CALLER_SCRIPTS 7 scripts の `from _observability import (...)` block が原名 alphabetical order であることを AST audit で検証する caller import order canonical lint 追加 + `generate_slide_plan.py` の import order 修正 (`test_observability_caller_import_order_canonical_lint`、Codex 推奨 PR-S2、PR-S1/S3 local wrapper / v0_status enumeration と相補な import 整序 axis)。各 script の ImportFrom node から alias.name (原名、`as` alias 前) を抽出し `sorted(names)` と一致することを assert。`generate_slide_plan.py` は `resolve_run_context` / `redact_provider_body` の順序が誤っていたため修正 (PR-S2 で同時 fix)。本 lint は 2-part validation: (1) 各 V1_CALLER_SCRIPT の `from _observability import` block aliases が alphabetical order と一致 (import 追加時の順番混在 fail-loud)、(2) docs §Script Coverage Matrix section に `import order` / `_observability` / `alphabetical` 3 keyword presence (docs spec drift fail-loud)。PR-S1/S3 caller-side AST lint と相補、本 lint は import 整序 という別 axis を fix + 1 件 regression test | PR-S2 |
| 80 | `STATUS_MAP` の全 key が V1_CALLER_SCRIPTS 7 scripts の何れかで str literal として出現することを検証する reverse direction coverage lint 追加 (`test_observability_status_map_reverse_direction_coverage_lint`、Codex 推奨 PR-BM'、PR-S3 forward direction (全 emit v0_status が STATUS_MAP 登録済) と対をなす逆方向 dead entry detection)。STATUS_MAP に登録されているが scripts に str literal として現れないキーは orphaned entry (投機的追加で実際に emit されない mapping 膨張) であり fail-loud 化。本 lint は 2-part validation: (1) STATUS_MAP の全 key が `set(V1_CALLER_SCRIPTS の全 str Constant を合算)` に含まれること (`str literal 未出現 = orphaned fail-loud`)、(2) docs §v0 → v1 status mapping section に `reverse direction` / `STATUS_MAP` / `orphaned` 3 keyword presence (docs spec drift fail-loud)。PR-S3 forward direction と双方向で STATUS_MAP ↔ scripts の bijection を保証、本 lint は reverse direction という別 axis を fix + 1 件 regression test | PR-BM' |
| 81 | `_observability.py` の全 module-level function に return type annotation と全 parameter type annotation を追加する type hint 統一 + AST audit lint 追加 (`test_observability_helper_type_annotation_completeness_lint`、Codex 推奨 PR-U、helper の型 contract を明文化して将来 annotation 欠落を fail-loud 化)。対象 17 関数: `map_status` / `_hash16` / `_lexical_redact` / `redact_secret` / `redact_error_message` / `safe_artifact_path` / `user_content_meta` / `redact_provider_body` / `_coerce_finite_or_none` / `compute_rate_missing` / `build_cost_payload` / `warn_legacy_cost_extras` / `_normalize_redaction_rules` / `build_status` / `emit_json` / `_validate_trace_value` / `resolve_run_context`。本 lint は 2-part validation: (1) 全 module-level FunctionDef の return annotation と全 parameter annotation が存在すること (annotation 追加時の欠落 fail-loud)、(2) docs §Emission Contract section に `type annotation` / `_observability` / `module-level` 3 keyword presence (docs spec drift fail-loud) | PR-U |
| 82 | `safe_artifact_path()` の root 解決優先順序 (`project_root > repo_root > lexical_redact placeholder`) を docs §Path Policy で正式仕様化し、Open Question (artifact path の repo-root vs project-root 解釈) を close する docs + docs keyword lint 追加 (`test_observability_artifact_path_root_priority_policy_doc_lint`、Codex 推奨 PR-V)。実装は PR #3 で `for root in (project_root, repo_root)` ループとして確定済だが、仕様 docs としての明文化が未了だった。本 lint は 2-part validation: (1) docs §Path Policy section に `project_root` / `repo_root` / `priority` 3 keyword presence (policy spec drift fail-loud)、(2) §Open Questions に artifact path 項目が resolved 済として記録されていること (`resolved` keyword presence) | PR-V |
| 83 | `template/package.json` の npm script 構成 ↔ `docs/PHASE3_RELEASE_NOTE.md` の runbook 記述 双方向 lint 追加 (`test_npm_script_package_json_runbook_contract_lint`、Codex 推奨 PR-W)。`test` は `lint + test:timeline + test:react` の CI-safe composite として定義されており、fixture-dependent な `visual-smoke` / `render` は `test` に混入してはならない。4-part validation: (1) `test` script に `lint` / `test:timeline` / `test:react` 全 3 component 含む、(2) `test` に `visual-smoke` / `render` が混入していない、(3) `visual-smoke` と `render` が standalone script として存在、(4) `docs/PHASE3_RELEASE_NOTE.md` に `test:timeline` / `test:react` / `visual-smoke` separation language と combined description が存在。CI 高頻度 `npm test` に fixture gate が混入する事故と runbook stale を即検出 | PR-W |
| 84 | `.claude-plugin/plugin.json` ↔ `.claude-plugin/marketplace.json` ↔ `skills/` ディレクトリ の整合性 lint 追加 (`test_plugin_manifest_marketplace_contract_lint`、Codex 推奨 PR-X)。4-part validation: (1) plugin.json 必須 field (name / version / skills) 存在、(2) skills path が実在 dir に解決 + 全 skill subdir に SKILL.md 存在、(3) marketplace.json plugins[0].source = "." 確認、(4) plugin.json name == marketplace.json plugins[0].name 一致。新規 skill 追加時に SKILL.md 忘れ / name 不整合を即検出 | PR-X |
| 85 | `skills/*/SKILL.md` frontmatter `name:` ↔ `README.md` コマンド表 ↔ skill dir 名 の 3者 drift lint 追加 (`test_skill_readme_command_surface_lint`、Codex 推奨 PR-Y)。3-part validation: (1) 非 template SKILL.md の `name:` が dir 名と一致、(2) 各 skill name が README.md に `` `/{name}` `` として掲載、(3) README.md の `` `/{supermovie-*}` `` コマンドに対応する skills dir が存在。`<` / `>` を含む template placeholder (supermovie-skill-creator) は skip。新規 skill 追加・rename 時に README コマンド面との 3者 drift を即検出 | PR-Y |
| 86 | `template/package.json` ↔ `template/package-lock.json` root dependency key drift lint 追加 (`test_package_lock_root_dependency_drift_lint`、Codex 推奨 PR-Z)。3-part validation: (1) name / version 一致、(2) dependencies keys 一致、(3) devDependencies keys 一致。`package-lock.json` が git 未追跡 (ローカル専用) の場合は soft-skip。npm install を忘れた drift を即検出 | PR-Z |
| 87 | `README.md` の install `git clone` URL が `.claude-plugin/plugin.json` の `repository` と一致することを lint (`test_readme_install_url_matches_plugin_repository_lint`、Codex 推奨 PR-AA)。URL 末尾 `.git` suffix の有無を正規化して比較。README の clone 手順が plugin 本家 repo と diverge した場合を即検出 | PR-AA |
| 88 | `skills/*/SKILL.md` 埋め込みワークフロー図が `CLAUDE.md` 正規パイプライン順と一致することを lint (`test_skill_embedded_workflow_matches_claude_pipeline_lint`、Codex 推奨 PR-AB)。2-part validation: (1) `subtitles` + `image-gen` を両方含む 4 step 以上のコードブロックには `slides` + `narration` も必須、(2) ステップの相対順序が canonical 順と一致。slides / narration 追加以前の旧 SKILL.md 7 ファイルのワークフロー図を同時修正 | PR-AB |
| 89 | `README.md` スキル一覧コマンド表の掲載順が `CLAUDE.md` 正規パイプライン順と一致することを lint (`test_readme_workflow_order_matches_claude_pipeline_lint`、Codex 推奨 PR-AC)。Canonical pipeline steps の相対順序が README 表で逆転していないことを検証。非 pipeline スキル (telop-creator / skill-creator 等) は任意位置を許可 | PR-AC |
| 90 | `CLAUDE.md` 正規パイプラインの各 `/supermovie-*` step が (1) `skills/<step>/SKILL.md` として実在し、(2) `README.md` コマンド表に掲載されていることを lint (`test_claude_pipeline_commands_have_skill_and_readme_coverage_lint`、Codex 推奨 PR-AD)。新規 step を CLAUDE.md に追加した際の SKILL.md 未作成 / README 掲載漏れを即検出 | PR-AD |
| 91 | `README.md` クイックスタートの `Claude: ... → ...` ワークフロー文が正規パイプライン全 step を含み正規順序で並んでいることを lint (`test_readme_quickstart_workflow_matches_claude_pipeline_lint`、Codex 推奨 PR-AE)。canonical step → 日本語キーワードの hardcoded マッピングで presence + 単調順序を検証。新規 step 追加時のクイックスタート更新漏れを即検出 | PR-AE |
| 92 | 全 `skills/*/SKILL.md` YAML frontmatter に `name` / `description` / `argument-hint` / `allowed-tools` が存在し非空であることを lint (`test_skill_frontmatter_required_fields_lint`、Codex 推奨 PR-AF)。frontmatter ブロック自体の欠落も検出。新規 skill 追加時の必須 metadata 漏れを即検出 | PR-AF |
| 93 | 全 `skills/*/SKILL.md` frontmatter の `allowed-tools` 値が承認済み Claude Code ツール名セット (`Read, Write, Edit, Bash, Glob, Grep, Agent, AskUserQuestion`) のみで構成されていることを lint (`test_skill_frontmatter_allowed_tools_allowlist_lint`、Codex 推奨 PR-AG)。空トークン・重複・未承認ツール名を検出。document body のプレースホルダーは frontmatter 抽出後のため誤検出しない | PR-AG |
| 94 | 全 `skills/*/SKILL.md` frontmatter に `effort` フィールドが存在し値が `high` / `medium` / `low` のいずれかであることを lint (`test_skill_frontmatter_effort_field_lint`、Codex 推奨 PR-AH)。`supermovie-init` / `supermovie-se` の `effort` 未設定を fix して追加 (init=high、se=medium)。新規 skill の effort 設定漏れを即検出 | PR-AH |
| 95 | `.claude-plugin/plugin.json` の必須フィールド (`name` / `description` / `version` / `repository`) が存在・非空・型正当であることを lint (`test_plugin_json_required_fields_lint`、Codex 推奨 PR-AI)。`version` は semver `X.Y.Z` 形式、`repository` は `https://` URL を要求。plugin manifest 劣化による discovery 破壊を即検出 | PR-AI |
| 96 | `CLAUDE.md` の `## データスキーマ` section に固定 5 data file (`telopData.ts` / `titleData.ts` / `insertImageData.ts` / `cutData.ts` / `seData.ts`) の各 `### heading` + TypeScript code block + 期待 type 宣言 (`interface|type TelopSegment` 等) が存在することを lint (`test_claude_data_schema_sections_lint`、Codex 推奨 PR-AJ)。既存 schema 削除・rename による drift を即検出。word-boundary declaration regex で単なる substring mention を通さない | PR-AJ |
| 97 | `CLAUDE.md` の `## ファイルパス規約` table に固定 5 data file の canonical パス (`src/cutData.ts` / `src/InsertImage/insertImageData.ts` / `src/テロップテンプレート/telopData.ts` / `src/Title/titleData.ts` / `src/SoundEffects/seData.ts`) が `<PROJECT>/` prefix 付き backtick エントリとして存在することを lint (`test_claude_filepath_table_data_files_lint`、Codex 推奨 PR-AK)。canonical path の rename による drift を即検出 | PR-AK |
| 98 | `CLAUDE.md` の `## 用語統一ルール` table に固定 5 canonical/forbidden ペア (`TelopSegment`/SubtitleSegment 等) が同一行内に canonical=backtick・forbidden=テキストとして存在することを lint (`test_claude_terms_table_canonical_pairs_lint`、Codex 推奨 PR-AL)。用語の rename / forbidden 表記追加忘れによる drift を即検出 | PR-AL |
| 99 | 全 `skills/*/SKILL.md` frontmatter の `argument-hint` 値が `<required>` / `[optional]` トークンのみで構成されていることを lint (`test_skill_frontmatter_argument_hint_shape_lint`、Codex 推奨 PR-AM)。frontmatter block 限定で body プレースホルダーは誤検出しない。トークン除去後の残余文字検査で bare word や非標準記法を即検出 | PR-AM |
| 100 | 全 `skills/*/SKILL.md` frontmatter の `name` フィールドがディレクトリ名と一致し、かつ `plugin.json` の `name` を namespace prefix として持つことを lint (`test_skill_frontmatter_name_plugin_namespace_lint`、Codex 推奨 PR-AN)。frontmatter block 限定で body テンプレートプレースホルダーは誤検出しない。skill name の rename / namespace 外れによる command collision を即検出 | PR-AN |
| 101 | `CLAUDE.md` / `.claude-plugin/plugin.json` / `docs/OBSERVABILITY.md` / 全 `skills/*/SKILL.md` に未解決の git merge conflict marker (`<<<<<<<` / `=======` / `>>>>>>>`) が含まれていないことを lint (`test_plugin_docs_no_merge_conflict_markers_lint`、Codex 推奨 PR-AO)。行頭一致検査で誤検出を最小化。merge 事故による manifest/doc 破損を即検出 | PR-AO |
| 102 | 全 `skills/*/SKILL.md` frontmatter の `description` が `「...」と言われたときに使用` 形式の起動トリガー文を含むことを lint (`test_skill_frontmatter_description_trigger_phrase_lint`、Codex 推奨 PR-AP)。YAML block scalar (`|`/`>`) スタイルの description を対象とし、body プレースホルダーは frontmatter 抽出後のため誤検出しない。skill discovery 品質劣化を即検出 | PR-AP |
| 103 | `plugin.json` の `skills` フィールドが `./skills/` であることを検証し、そのパスがリポジトリルート相対で実在するディレクトリに解決されること、および `skills/` 配下の全サブディレクトリに `SKILL.md` が存在することを lint (`test_plugin_json_skills_path_resolves_to_skill_dirs_lint`、Codex 推奨 PR-AQ)。manifest parent 相対解決の罠を防ぎ、SKILL.md 欠落による skill 検出漏れを即検出 | PR-AQ |
| 104 | 全 `skills/*/SKILL.md` frontmatter のキー集合が `name` / `description` / `argument-hint` / `allowed-tools` / `effort` の 5 個と完全一致し、余剰キー・欠落キー・重複キーが存在しないことを lint (`test_skill_frontmatter_keyset_and_no_duplicate_keys_lint`、Codex 推奨 PR-AR)。frontmatter block 限定で body プレースホルダーは対象外。typo キー (`argument_hint` 等) や legacy key 残置によるスキーマドリフトを即検出 | PR-AR |
| 105 | `plugin.json` のオプショナルメタデータフィールド (`author.name` 非空文字列 / `homepage` https:// URL / `license` 非空文字列 / `keywords` 非空ユニーク文字列リスト) の型と形式を lint (`test_plugin_json_optional_metadata_shape_lint`、Codex 推奨 PR-AS)。marketplace 公開品質の劣化（空 author / http ホームページ / 重複 keyword 等）を即検出 | PR-AS |
| 106 | 全 `skills/*/SKILL.md` frontmatter の `allowed-tools` トークンが `Read, Write, Edit, Bash, Glob, Grep, Agent, AskUserQuestion` の canonical 順序に従っていることを lint (`test_skill_frontmatter_allowed_tools_canonical_order_lint`、Codex 推奨 PR-AT)。サブセットは許容、既知トークンの相対順序のみを検査。順序 drift による skill 品質劣化・レビュー困難を即検出 | PR-AT |
| 107 | 全 `skills/*/SKILL.md` 本文の先頭 H1 見出しが `# SuperMovie ` で始まり、ディレクトリ名スラッグの各単語 (e.g. `supermovie-image-gen` → `image`, `gen`) を単語境界一致で含むことを lint (`test_skill_md_h1_title_consistency_lint`、Codex 推奨 PR-AU)。frontmatter 除去後の本文を対象。skill rename 時の H1 title drift を即検出 | PR-AU |
| 108 | `plugin.json` トップレベルキー集合が `author / description / homepage / keywords / license / name / repository / skills / version` の 9 個と完全一致し、余剰・欠落なしであることを lint (`test_plugin_json_top_level_keyset_lint`、Codex 推奨 PR-AV)。step 95 の required fields / step 105 のオプショナル metadata に加え、stray key (commands 等) の混入による manifest 肥大化を即検出 | PR-AV |
| 109 | `plugin.json` の `keywords[]` 各要素が `^[a-z0-9]+(?:-[a-z0-9]+)*$` (lowercase kebab-case、空白・大文字・日本語・underscore・先頭末尾 hyphen 禁止) に適合することを lint (`test_plugin_json_keywords_canonical_tokens_lint`、Codex 推奨 PR-AW)。step 105 の非空/ユニーク検査に加え、keyword token 形式を固定。marketplace indexing 品質の劣化を即検出 | PR-AW |
| 110 | `plugin.json` の `license` フィールドと `LICENSE` ファイルの整合性を lint (`test_plugin_json_license_file_consistency_lint`、Codex 推奨 PR-AX)。`license == "MIT"` / LICENSE 先頭行が `MIT License` / Copyright 行に `author.name` を含む の 3 点を検証。manifest 更新漏れや author 表記のズレを即検出 | PR-AX |
| 111 | `marketplace.json` の `owner.name` / `metadata.version` が `plugin.json` の `author.name` / `version` と一致することを lint (`test_marketplace_json_mirrors_plugin_json_lint`、Codex 推奨 PR-AY)。plugin.json バージョンアップ時に marketplace.json の更新漏れによる不整合を即検出 | PR-AY |
| 112 | `marketplace.json` の `plugins[]` が正確に 1 エントリを持ち、`name == "supermovie"` / `source == "."` / `description` が非空文字列であることを lint (`test_marketplace_json_plugins_array_shape_lint`、Codex 推奨 PR-AZ)。plugin 定義の欠落・重複・必須フィールド欠損を即検出 | PR-AZ |
| 113 | 各 `SKILL.md` の本文（frontmatter と fenced code block を除去後）に H1 見出し (`^# .+`) がちょうど 1 行あることを lint (`test_skill_md_body_single_h1_lint`、Codex 推奨 PR-BA）。code block 内の `# ...` をコード例として誤カウントせずに正確に検出し、H1 欠落・重複・code block 外への書き漏れを即検出 | PR-BA |
| 114 | `README.md` / `CLAUDE.md` / `docs/OBSERVABILITY.md` / `skills/*/SKILL.md` の全 markdown ファイルにおいて、backtick (` ``` `) と tilde (`~~~`) のフェンスブロックが対応・閉合されていることを lint (`test_markdown_fence_balanced_lint`、Codex 推奨 PR-BB)。frontmatter を除いてから fence 行をカウントし、EOF 時点の depth が 0 でなければ unclosed block として fail | PR-BB |
| 115 | `skills/` 配下の全ディレクトリ名が lowercase の `supermovie-` prefix を持つ kebab-case スラグ (`^supermovie-[a-z0-9]+(?:-[a-z0-9]+)*$`) に一致し、casefold 重複がないことを lint (`test_skill_directory_name_canonical_slug_lint`、Codex 推奨 PR-BC)。大文字・アンダースコア・二重ハイフン・prefix 欠落・casefold 衝突を即検出 | PR-BC |
| 116 | `README.md` / `CLAUDE.md` / `docs/OBSERVABILITY.md` / `skills/*/SKILL.md` 内の相対 markdown リンク (URL / anchor を除く) がすべてファイルシステム上に実在することを lint (`test_markdown_local_links_resolve_lint`、Codex 推奨 PR-BD)。inline code span (バッククォート) を除去してから検索することで、ドキュメント内の regex パターンによる誤検出を回避 | PR-BD |
| 117 | `README.md` / `CLAUDE.md` / `docs/OBSERVABILITY.md` / `skills/*/SKILL.md` 内で見出しレベルをスキップしていないことを lint (`test_markdown_heading_level_skip_lint`、Codex 推奨 PR-BE)。frontmatter と fenced code block を除去後、H{n} → H{n+2} 以上のジャンプを検出。ドキュメントの階層構造の一貫性を機械保証 | PR-BE |
| 118 | `plugin.json` の `homepage` と `repository` が同一の canonical GitHub URL (`https://github.com/{owner}/{name}`) であり、URL 末尾が plugin `name` スラグに一致し trailing slash がないことを lint (`test_plugin_json_homepage_repository_canonical_url_lint`、Codex 推奨 PR-BF)。install/discovery メタデータの指定先ドリフトを即検出 | PR-BF |
| 119 | `skills/*/SKILL.md` frontmatter フィールドが canonical 順序 (`name → description → argument-hint → allowed-tools → effort`) で並んでいることを lint (`test_skill_frontmatter_field_order_lint`、Codex 推奨 PR-BG)。key-set/no-duplicate lint (PR-AR) を補完し、読み取り順で依存するパーサーやレビュアーが期待する順序を保証する | PR-BG |
| 120 | `git ls-files` で列挙された git 追跡ファイルのパス成分に dependency/build ディレクトリ名 (`node_modules` / `.venv` / `venv` / `__pycache__` / `dist` / `build` 等) が含まれていないことを lint (`test_no_dependency_dirs_committed_lint`、Codex 推奨 PR-BH)。ファイルシステム上に存在するが gitignore で除外されているディレクトリは検査対象外とし誤検出を回避。インストール成果物の誤コミットを即検知 | PR-BH |
| 121 | `marketplace.json` top-level key-set が完全一致 (`name` / `owner` / `metadata` / `plugins` の 4 件、過不足禁止) であることを lint (`test_marketplace_json_top_level_keyset_lint`、Codex 推奨 PR-BI)。PR-AY (owner/version mirror) / PR-AZ (plugins[] shape) と相補な manifest 構造の全体 drift 検知 | PR-BI |
| 122 | `plugin.json` top-level キーが canonical 順序 (`name → description → version → author → homepage → repository → license → keywords → skills`) で並んでいることを lint (`test_plugin_json_top_level_key_order_lint`、Codex 推奨 PR-BJ)。PR-AV (key-set) を補完し、insertion order を固定することで diff の安定性とパーサーの期待する順序を保証する | PR-BJ |
| 123 | `marketplace.json` top-level `name` が `{plugin_name}-marketplace` 形式であることを lint (`test_marketplace_json_name_derives_from_plugin_name_lint`、Codex 推奨 PR-BK)。PR-BI (keyset) / PR-AY (owner/version mirror) と相補な top-level name の命名規則を固定し、manifest 同士の identity 整合を保証する | PR-BK |
| 124 | git 追跡下の shell script (`.sh`) で shebang (`#!`) を持つファイルが git index で mode 100755 になっていることを lint (`test_shebang_scripts_are_executable_lint`、Codex 推奨 PR-BL)。Python/JS ファイルはインタープリタ経由起動のため除外。直接実行される `.sh` ファイルの executable bit 欠落を clone 直後に検知 | PR-BL |
| 125 | git 追跡下の `.sh` ファイルが `bash -n` で構文エラーなく解析できることを lint (`test_shell_scripts_parse_with_bash_n_lint`、Codex 推奨 PR-BM)。PR-BL (executable mode) を補完し、shell 構文の破損を実行前に検知。`git ls-files *.sh` で追跡ファイルのみを対象 | PR-BM |
| 126 | git 追跡ファイルの最上位パス成分が canonical root allowlist (`.claude-plugin` / `.gitignore` / `CLAUDE.md` / `CONTEXT_ANCHOR.md` / `LICENSE` / `README.md` / `agents.disabled` / `docs` / `scripts` / `skills` / `sm-claude.sh` / `template`) のみであることを lint (`test_repo_root_tracked_paths_allowlist_lint`、Codex 推奨 PR-BN)。`git ls-files -z` で non-ASCII ファイル名を安全に処理し、scratch ファイルや生成物の誤コミットを即検知 | PR-BN |
| 127 | `template/package.json` の `scripts.render` に含まれる Remotion render target ID が `template/src/Root.tsx` の `<Composition id=...>` と一致し、かつ `template/src/index.ts` が `registerRoot()` を呼び出していることを lint (`test_remotion_entrypoint_coherence_lint`、Codex 推奨 PR-BO)。entrypoint/ID のずれによる実行時 `Composition not found` エラーを事前検知 | PR-BO |
| 128 | `template/src/videoConfig.ts` の `VideoFormat` union メンバーと `RESOLUTION_MAP` / `TELOP_CONFIG_MAP` のキー集合が一致することを lint (`test_video_config_format_map_parity_lint`、Codex 推奨 PR-BP)。`^}` ベースの lazy regex で nested brace を安全に処理。format 追加/削除時に解像度またはテロップ設定だけが欠ける drift を防ぐ | PR-BP |
| 129 | `template/package.json` の `scripts['test:timeline']` が `python3 <path>` 形式で、かつ `<path>` が実在するファイルを指していることを lint (`test_package_json_test_timeline_script_path_resolves_lint`、Codex 推奨 PR-BQ)。スクリプト名変更/移動時に CI が silent-pass しないよう drift を即検知 | PR-BQ |
| 130 | `.gitignore` が必須エントリ (`node_modules/` / `.venv/` / `__pycache__/` / `*.pyc` / `out/` / `.env` / `.DS_Store`) を全て含むことを lint (`test_gitignore_required_entries_lint`、Codex 推奨 PR-BR)。コメント・空行を除外した active エントリのみを対象に set 差分で検査し、削除/欠落による依存物・秘匿情報の誤コミットを防ぐ | PR-BR |
| 131 | `template/tsconfig.json` の `compilerOptions` が `strict: true` / `jsx: "react-jsx"` / `noEmit: true` を保持することを lint (`test_tsconfig_compiler_options_contract_lint`、Codex 推奨 PR-BS)。これらの設定の削除/変更は型安全性・JSX 変換・Remotion ビルドに直接影響するため contract として固定する | PR-BS |
| 132 | `template/src/**/*.ts(x)` の相対 import/export が全て実在するファイルまたは index.ts/tsx 経由のディレクトリに解決されることを lint (`test_template_src_relative_imports_resolve_lint`、Codex 推奨 PR-BT)。`//` コメント行を除外してから `^(?:import|export)` を走査し、ファイル名変更/移動による broken import を TypeScript コンパイル前に検知 | PR-BT |
| 133 | `template/vitest.config.ts` の `setupFiles` 配列の相対パスエントリが全て `template_root` 配下の実在ファイルに解決されることを lint (`test_vitest_setup_files_resolve_lint`、Codex 推奨 PR-BU)。setup ファイルの rename/移動で `npm run test:react` がサイレントに壊れるケースを `test:timeline` で早期検知 | PR-BU |
| 134 | `template/eslint.config.mjs` の `@typescript-eslint/no-explicit-any` ルールが `"error"` に設定されていることを lint (`test_eslint_config_no_explicit_any_contract_lint`、Codex 推奨 PR-BV)。`"warn"` / `"off"` への誤変更や削除で any-free contract がサイレントに失われることを防ぐ | PR-BV |
| 135 | `template/remotion.config.ts` が `Config.setVideoImageFormat("jpeg")` / `Config.setOverwriteOutput(true)` / `Config.overrideWebpackConfig(enableTailwind)` の 3 呼び出しを保持することを lint (`test_remotion_config_contract_lint`、PR-BW)。Tailwind 統合削除や出力フォーマット変更によるレンダリング破損を早期検知 | PR-BW |
| 136 | `template/package.json` の `remotion` / `@remotion/*` パッケージ全てが同一バージョンを持つことを lint (`test_package_json_remotion_version_parity_lint`、Codex 推奨 PR-BX)。semver prefix (`^`/`~`/`=`) を除去して正規化比較し、部分アップグレードによるバージョン不一致を早期検知 | PR-BX |
| 137 | `template/src/videoConfig.ts` が `FORMAT` / `FPS` / `RESOLUTION` / `TELOP_CONFIG` / `VIDEO_FILE` の 5 エクスポートを全て保持することを lint (`test_videoconfig_required_exports_present_lint`、PR-BY)。全コンポーネントが依存する SSoT 定数の削除を早期検知 | PR-BY |
| 138 | `template/package.json` の `private: true` / `license: "UNLICENSED"` / `name` / `version` の identity contract を lint (`test_package_json_identity_contract_lint`、Codex 推奨 PR-BZ)。誤って publishable になった場合や name/version の空値による package manager の誤動作を防ぐ | PR-BZ |
| 139 | `template/src/` が `InsertImage` / `SoundEffects` / `Title` / `テロップテンプレート` の 4 サブディレクトリを全て含むことを lint (`test_template_src_required_directories_lint`、Codex 推奨 PR-CA)。パッケージング時に import 先やアセット配置ディレクトリが欠落するケースを早期検知 | PR-CA |
| 140 | `docs/` ディレクトリが `OBSERVABILITY.md` / `PHASE3_PROGRESS.md` / `PHASE3_RELEASE_NOTE.md` の 3 ファイルを全て含むことを lint (`test_docs_required_files_present_lint`、PR-CB)。エージェントや CI が参照する権威ドキュメントの誤削除を早期検知 | PR-CB |
| 141 | `CONTEXT_ANCHOR.md` がリリースガバナンスに必要な `## Purpose` / `## Verified Snapshot` / `## Roku Authorized Decisions` / `## Required Gates` / `## External Actions` / `## Codex Review Protocol` / `## 更新責任` / `## Related Files` 等の全 ## セクションを保持することを lint (`test_context_anchor_required_sections_lint`、Codex 推奨 PR-CC)。handoff drift で必須ゲートや認可決定がサイレントに消えることを防ぐ | PR-CC |
| 142 | `template/src/MainVideo.tsx` が `./videoConfig` から import していることを lint (`test_main_video_imports_video_config_lint`、Codex 推奨 PR-CD)。レンダー設定変更がコンポジションエントリポイントに反映されない drift を早期検知 | PR-CD |
| 143 | `scripts/` に `check_release_ready.sh` と `regen_phase3_progress.sh` が存在し、両方が実行可能 (`+x`) であることを lint (`test_scripts_required_files_executable_lint`、Codex 推奨 PR-CE)。リリース/プリフライト自動化スクリプトの欠落や実行権限剥奪を早期検知 | PR-CE |
| 144 | `sm-claude.sh` がリポジトリルートに存在し実行可能 (`+x`) であることを lint (`test_sm_claude_entrypoint_executable_lint`、Codex 推奨 PR-CF)。プラグインインストール時にメイン CLI エントリポイントが呼び出せない壊れた状態を早期検知 | PR-CF |
| 145 | `template/src/Root.tsx` が `import "./index.css"` を保持し、`template/package.json` の `sideEffects` に `"*.css"` が含まれることを lint (`test_template_css_entrypoint_side_effects_lint`、Codex 推奨 PR-CG)。Tailwind/グローバル CSS エントリポイントがサイレントに削除またはツリーシェイクされることを早期検知 | PR-CG |
| 146 | `template/src/Root.tsx` が `FPS`/`SOURCE_DURATION_FRAMES`/`RESOLUTION` を `./videoConfig` から import し、`<Composition>` の `fps`/`durationInFrames`/`width`/`height` に SSoT 経由でバインドされていることを lint (`test_root_composition_uses_video_config_lint`、Codex 推奨 PR-CH)。ハードコードされたレンダー寸法・タイミングが videoConfig SSoT を迂回する drift を早期検知 | PR-CH |
| 147 | `template/src/MainVideo.tsx` が `VIDEO_FILE` を `./videoConfig` から import し、`staticFile(VIDEO_FILE)` でベース動画ソースを参照していることを lint (`test_main_video_staticfile_uses_video_file_lint`、Codex 推奨 PR-CI)。import だけ存在してもハードコード文字列で迂回される drift を早期検知 | PR-CI |
| 148 | `template/src/MainVideo.tsx` が 7 つの必須コンポジションレイヤー (`SlideSequence`/`ImageSequence`/`TelopPlayer`/`TitleSequence`/`NarrationAudioWithMode`/`BGM`/`SESequence`) を全て import かつ JSX でレンダーしていることを lint (`test_main_video_required_layers_contract_lint`、Codex 推奨 PR-CJ)。編集・音声レイヤーが静かにコンポジションから除去される drift を早期検知 | PR-CJ |
| 149 | `template/src/MainVideo.tsx` が `useNarrationMode()` を呼び出し、`narrationMode.kind` から `baseVolume` を導出し、ベース動画に `volume={() => baseVolume}` をバインドし、`NarrationAudioWithMode` に `mode={narrationMode}` を渡していることを lint (`test_main_video_narration_mode_ssot_contract_lint`、Codex 推奨 PR-CK)。ナレーションモード SSoT がハードコード音量やフック配線漏れで迂回されることを早期検知 | PR-CK |
| 150 | `template/src/テロップテンプレート/Telop.tsx` が `TELOP_CONFIG.fontSize/bottomOffset/maxWidth/containerPadding` を参照し、`template/src/Title/Title.tsx` が `TELOP_CONFIG.titleTop/titleLeft/titleFontSize` を参照していることを lint (`test_text_overlay_telop_config_ssot_contract_lint`、Codex 推奨 PR-CL)。テキストオーバーレイのレイアウト値がハードコードされ TELOP_CONFIG SSoT を迂回する drift を早期検知 | PR-CL |
| 151 | `template/src/メインテロップ`・`強調テロップ`・`ネガティブテロップ` の全 `.tsx` ファイルが `telopTemplateRegistry.tsx` に import されていることを lint (`test_telop_template_registry_file_coverage_lint`、Codex 推奨 PR-CM)。新規追加・名前変更されたテロップテンプレートがレジストリから到達不能になる drift を早期検知 | PR-CM |
| 152 | `telopTemplateRegistry.tsx` の各エントリの `displayName` がファイル名 stem と一致し、`category` がソースディレクトリと対応していること (`main`↔メインテロップ/`emphasis`↔強調テロップ/`negative`↔ネガティブテロップ) を lint (`test_telop_template_registry_metadata_contract_lint`、Codex 推奨 PR-CN)。テロップテンプレートの表示名・カテゴリ分類の不正確な drift を早期検知 | PR-CN |
| 153 | `SlideSequence.tsx`・`ImageSequence.tsx`・`Title.tsx`・`SESequence.tsx` がそれぞれ対応するローカルデータ配列 (`slideData`/`insertImageData`/`titleData`/`seData`) を import し `.map()` でレンダーしていることを lint (`test_sequence_components_are_data_driven_from_local_arrays`、Codex 推奨 PR-CO)。タイムライン項目のデータドリブン構造が崩れ、インライン JSX にデータがハードコードされる drift を早期検知 | PR-CO |
| 154 | `TelopPlayer.tsx` が `telopData` と `telopTemplateRegistry` を import し、`telopData.find()` でフレーム範囲から現在 segment を選択し、`subtitles: [current]` で SubtitleData を生成してレジストリ経由で描画し、templateId なし時は `Telop` にフォールバックすることを lint (`test_telop_player_bridges_telop_data_to_registry_templates`、Codex 推奨 PR-CP)。TelopPlayer の registry ブリッジ構造が断絶する drift を早期検知 | PR-CP |
| 155 | `useNarrationMode.ts` が `NARRATION_LEGACY_FILE`・`NARRATION_READY_FILE` を `./mode` から import し、`narrationData` を `./narrationData` から import し、両定数を `watchStaticFile` に渡し、`narrationData` のループで chunk ファイルを動的に watch していることを lint (`test_narration_watch_uses_mode_constants_and_data_files`、Codex 推奨 PR-CQ)。ナレーション監視が定数をバイパスして asset パスをハードコードする drift を早期検知 | PR-CQ |
| 156 | `NarrationAudioWithMode` が `mode: NarrationMode` prop を受け取り、関数本体内で `useNarrationMode()` を呼び出さない pure mode-prop renderer であることを lint (`test_narration_audio_with_mode_is_pure_mode_prop_renderer`、Codex 推奨 PR-CR)。MainVideo 側で hook を 1 回だけ呼ぶ watcher 二重登録防止設計が壊れる drift を早期検知 | PR-CR |
| 157 | `template/src/index.ts` が `RemotionRoot` を `./Root` から import し、`registerRoot(RemotionRoot)` を呼んでいることを lint (`test_entrypoint_registers_remotion_root`、Codex 推奨 PR-CS)。Remotion エントリポイントの registerRoot 配線が壊れる drift を早期検知 | PR-CS |
| 158 | `BGM.tsx` が `BGM_FILE` 定数を定義し、その定数を `getStaticFiles()` の存在チェックと `staticFile(BGM_FILE)` の音声ソースの両方で使用していることを lint (`test_bgm_file_constant_is_single_source_for_presence_check_and_audio_src`、Codex 推奨 PR-CT)。BGM ファイルパスがチェックと src でハードコードされ定数が片方だけ使われる drift を早期検知 | PR-CT |
| 159 | `Slide.tsx` が `useCurrentFrame()` の結果 `frame` を `segment.startFrame` 減算なしに `interpolate(frame, ...)` へ直接渡していることを lint (`test_slide_uses_sequence_local_frame_without_startframe_offset`、Codex 推奨 PR-CU)。`SlideSequence` が `<Sequence from={startFrame}>` でラップするため Remotion 仕様で `useCurrentFrame()` は既に 0 基準 local frame を返す。startFrame 二重減算 (double-offset) バグが混入する drift を早期検知 | PR-CU |
| 160 | `template/src/Slides/types.ts` が `SlideSegment` の必須フィールド (`id`/`startFrame`/`endFrame`/`title`)、`videoLayer` の `'visible' | 'dimmed' | 'hidden'` union、`SlideAlignment = 'center' | 'left'` 型を保持していることを lint (`test_slide_segment_schema_contract_lint`、Codex 推奨 PR-CV)。フィールド追加・型変更でスキーマ互換性が壊れる drift を早期検知 | PR-CV |
| 161 | `template/src/Narration/mode.ts` がモジュールレベルの `let _modeCache` を定義し、`getNarrationMode()` が `_modeCache !== undefined` の早期リターンガードを持ち、`invalidateNarrationMode()` が `_modeCache = undefined` でリセットすることを lint (`test_narration_mode_invalidate_resets_cached_mode`、Codex 推奨 PR-CW)。キャッシュ戦略の変更で Studio hot-reload 無効化が壊れる drift を早期検知 | PR-CW |
| 162 | `template/src/Narration/narrationData.ts` が `NarrationSegment` を `./types` から import し、`export const narrationData: NarrationSegment[] = []` の型付き空配列でエクスポートしていることを lint (`test_template_narration_data_exports_typed_empty_array_lint`、Codex 推奨 PR-CX)。`voicevox_narration.py` の all-or-nothing 書き換え対象ファイルの型契約が壊れる drift を早期検知 | PR-CX |
| 163 | `template/src/InsertImage/types.ts` が `ImageSegment` の必須フィールド (`id`/`startFrame`/`endFrame`/`file`/`type`)、`type` の `'photo' | 'infographic' | 'overlay'` union、オプション `scale?: number` を保持していることを lint (`test_insert_image_segment_schema_contract_lint`、Codex 推奨 PR-CY)。挿入画像スキーマが壊れる drift を早期検知 | PR-CY |
| 164 | `template/src/Narration/types.ts` が `NarrationSegment` の必須フィールド `id: number`/`startFrame: number`/`durationInFrames: number`/`file: string` を非オプション形式で保持していることを lint (`test_narration_segment_required_fields_contract_lint`、Codex 推奨 PR-CZ)。`voicevox_narration.py` の Sequence タイミング計算に必須なフィールドがオプション化される drift を早期検知 | PR-CZ |
| 165 | `template/src/Title/titleData.ts` が `FPS` を `../videoConfig` から import し、`toFrame` helper で `Math.round()` を使用し、かつ `toFrame` 定義内で `FPS` を参照していることを lint (`test_title_data_toframe_uses_video_config_fps_lint`、Codex 推奨 PR-DA)。frame 計算が videoConfig の FPS SSoT をバイパスする drift を早期検知 | PR-DA |
| 166 | `template/src/テロップテンプレート/telopTypes.ts` が `TelopSegment` の必須フィールド (`id`/`startFrame`/`endFrame`/`text`)、`style` の `'normal'|'emphasis'|'warning'|'success'` union、`template` の `1|2|3|4|5|6` union、`templateId?: TelopTemplateId` を保持していることを lint (`test_telop_segment_schema_contract_lint`、Codex 推奨 PR-DB)。テロップ型定義の互換性が壊れる drift を早期検知 | PR-DB |
| 167 | `template/src/テロップテンプレート/telopConfigTypes.ts` が `TelopStyleConfig` の必須フィールド (`font`/`textShadow`/`textStroke`/`background`/`position`) と `TelopSlideDirection = 'up' | 'down' | 'left' | 'right'` union を保持していることを lint (`test_telop_config_types_export_style_shape_and_slide_direction_union`、Codex 推奨 PR-DC)。アニメーション方向リテラルとスタイル構造が壊れる drift を早期検知 | PR-DC |
| 168 | `template/src/SoundEffects/SEPlayer.ts` が `SoundEffect` の必須フィールド (`id:number`/`startFrame:number`/`file:string`) とオプション `volume?:number` を持ち、`seData.ts` が `SoundEffect` を `./SEPlayer` から import して `export const seData: SoundEffect[]` を保持していることを lint (`test_sound_effect_schema_and_se_data_export_lint`、Codex 推奨 PR-DD)。SE スキーマと配列エクスポートの型契約が壊れる drift を早期検知 | PR-DD |
| 169 | `template/src/テロップテンプレート/telopStyles.ts` の 9 つの `animation_*` export (`animation_none`/`animation_slideFromLeft`/`animation_fadeBlurFromBottom`/`animation_slideIn`/`animation_fadeOnly`/`animation_slideLeftFadeBlur`/`animation_fadeFromRight`/`animation_fadeFromLeft`/`animation_charByChar`) が全て `slideDirection: '...' as const` と `spring: { damping, stiffness, mass }` を保持していることを lint (`test_telop_styles_animation_exports_motion_contract_lint`、Codex 推奨 PR-DE)。アニメーション定数のリネームや spring フィールド欠落による Telop モーション drift を早期検知 | PR-DE |
| 170 | `template/src/テロップテンプレート/telopStyles.ts` の 6 つのスタイルテンプレート export (`template1_gradient`/`template2_purpleStroke`/`template3_gradientText`/`template4_negative`/`template4_negative_v2`/`template6_whiteGradientText`) が全て必須フィールド (`font`/`textShadow`/`textStroke`/`background`/`position`) を持ち、`subtitleConfig = template3_gradientText` alias と `getTextShadowCSS`/`getTextStrokeCSS` helper export が存在することを lint (`test_telop_styles_template_exports_and_config_helpers_lint`、Codex 推奨 PR-DF)。テンプレートスタイル構造とエイリアス・ヘルパーの drift を早期検知 | PR-DF |
| 171 | `template/src/Title/Title.tsx` が `TitleSegment` interface の必須フィールド (`id`/`startFrame`/`endFrame`: number、`text`: string) を保持し、`titleData.ts` が `TitleSegment` を `./Title` から import して `export const titleData: TitleSegment[]` を保持していることを lint (`test_title_segment_schema_and_title_data_typed_export_contract_lint`、Codex 推奨 PR-DG)。renderer/data 間の schema 不整合による supermovie-subtitles 自動生成失敗を早期検知 | PR-DG |
| 172 | `template/src/Slides/slideData.ts` が `SlideSegment` を `./types` から import して `export const slideData: SlideSegment[]` を保持していることを lint (`test_slide_data_exports_typed_empty_array_lint`、Codex 推奨 PR-DH)。supermovie-slides 自動生成時の all-or-nothing 上書き安全性のための型付き export 契約を保護 | PR-DH |
| 173 | `template/src/InsertImage/insertImageData.ts` が `ImageSegment` を `./types` から import し、`FPS` を `../videoConfig` から import し、`toFrame` が `Math.round()` と `FPS` を使用し、`export const insertImageData: ImageSegment[]` を保持していることを lint (`test_insert_image_data_typed_export_and_toframe_fps_contract_lint`、Codex 推奨 PR-DI)。supermovie-image-gen 自動生成時の型付き export と FPS SSoT 契約を保護 | PR-DI |
| 174 | `template/src/Slides/SlideSequence.tsx` が `slideData.map()` で data-driven に `<Sequence key={segment.id} from={segment.startFrame} durationInFrames={segment.endFrame - segment.startFrame}>` を張り、`<Slide segment={segment}/>` を描画することを lint (`test_slide_sequence_wraps_slide_segments_in_sequence_frame_ranges_lint`、Codex 推奨 PR-DJ)。SlideSegment の frame range contract が Sequence 構築で正しく使われることを保護 | PR-DJ |
| 175 | `template/src/InsertImage/ImageSequence.tsx` が `insertImageData.map()` で data-driven に `<Sequence key={segment.id} from={segment.startFrame} durationInFrames={segment.endFrame - segment.startFrame}>` を張り、`<InsertImage segment={segment}/>` を描画することを lint (`test_image_sequence_wraps_image_segments_in_sequence_frame_ranges_lint`、Codex 推奨 PR-DK)。ImageSegment の frame range contract が Sequence 構築で正しく使われることを保護 | PR-DK |
| 176 | `template/src/テロップテンプレート/telopData.ts` が `TelopSegment` を型 import し、`FPS as CONFIG_FPS` / `SOURCE_DURATION_FRAMES` を `../videoConfig` から import し、`export const FPS = CONFIG_FPS` / `export const TOTAL_FRAMES = SOURCE_DURATION_FRAMES` で再 export し、`export const telopData: TelopSegment[]` を保持していることを lint (`test_telop_data_typed_export_and_video_config_ssot_contract_lint`、Codex 推奨 PR-DL)。videoConfig SSoT からの FPS/duration 再 export チェーンと型付き telopData export 契約を保護 | PR-DL |
| 177 | `template/src/SoundEffects/SESequence.tsx` が `Audio`/`Sequence`/`staticFile` を `remotion` から import し、`seData.map()` で data-driven に `<Sequence key={se.id} from={se.startFrame} durationInFrames={90}>` を張り、`<Audio src={staticFile(...)} volume={() => v}/>` で再生し、`se.volume ?? 1` で volume を default 1 に保つことを lint (`test_se_sequence_wraps_sound_effects_in_sequence_audio_contract_lint`、Codex 推奨 PR-DM)。SE の Remotion タイミング/audio wiring contract が破壊されないことを保護 | PR-DM |
| 178 | `template/src/Narration/NarrationAudio.tsx` が `Audio`/`Sequence`/`staticFile` を `remotion` から import し、`mode.kind === 'chunks'` 分岐で `mode.segments.map()` を使い `<Sequence key={seg.id} from={seg.startFrame} durationInFrames={seg.durationInFrames}>` を張り、`<Audio src={staticFile(seg.file)} volume={() => volume}/>` で chunk 再生することを lint (`test_narration_audio_chunks_sequence_wiring_contract_lint`、Codex 推奨 PR-DN)。narration chunks モードの Remotion Sequence/Audio wiring contract を保護 | PR-DN |
| 179 | `template/src/Narration/mode.ts` の `NarrationMode` 型 union が `'chunks'`/`'legacy'`/`'none'` を持ち、`chunks` は `segments: readonly NarrationSegment[]`、`legacy` は `file: string`、`getNarrationMode()` が `narrationData.length > 0 + every chunk file 存在 → chunks`、`NARRATION_LEGACY_FILE 存在 → legacy`、`else → none` の優先順位 dispatch を保つことを lint (`test_narration_mode_priority_dispatch_contract_lint`、Codex 推奨 PR-DO)。narration mode SSoT の型形状と3方向 priority dispatch 契約を保護 | PR-DO |
| 180 | `template/src/Narration/NarrationAudio.tsx` の `mode.kind === 'legacy'` 分岐が `<Audio src={staticFile(mode.file)} volume={() => volume}/>` で単一ファイル再生することを lint (`test_narration_audio_legacy_branch_wiring_contract_lint`、Codex 推奨 PR-DP)。narration legacy モードの静的ファイル Audio wiring contract を保護 | PR-DP |
| 181 | `template/src/SoundEffects/BGM.tsx` が `Audio`/`getStaticFiles`/`staticFile` を `remotion` から import し、`BGM_FILE = 'BGM/bgm.mp3'` 定数を持ち、`getStaticFiles().some()` で存在ガードして不在時 `return null` を返し、存在時 `<Audio src={staticFile(BGM_FILE)} volume={() => volume} loop/>` で再生することを lint (`test_bgm_asset_gate_and_audio_loop_contract_lint`、Codex 推奨 PR-DQ)。BGM の asset gate と loop 再生 contract を保護 | PR-DQ |
| 182 | `template/src/Title/Title.tsx` が `TitleSequence` の `<Sequence from={segment.startFrame} durationInFrames={segment.endFrame - segment.startFrame}>` 内で `Title` に `segment` を渡し、`Title` 側では `useCurrentFrame()` の local frame を `interpolate(frame, ...)` と `spring({ frame, ... })` に直接渡して `frame - segment.startFrame` を使わないことを lint (`test_title_uses_sequence_local_frame_without_startframe_offset_lint`、Codex 推奨 PR-DR)。Title の Sequence-local frame contract を保護 | PR-DR |
| 183 | `template/src/Slides/Slide.tsx` が `align ?? 'center'`、`backgroundColor ?? 'rgba(20, 26, 44, 0.92)'`、`textColor ?? '#ffffff'` の default を持ち、`align` から `justifyContent` / `textAlign` / bullet `paddingLeft` / `listStyle` を決め、emphasis bullet を `#ffd166`、通常 bullet を `fg` で描画することを lint (`test_slide_style_defaults_and_alignment_contract_lint`、Codex 推奨 PR-DS)。Slide renderer の style default と alignment contract を保護 | PR-DS |
| 184 | `template/src/MainVideo.tsx` が base `<Video volume={() => baseVolume}>` の後に `<SlideSequence />` → `<ImageSequence />` → `<TelopPlayer />` → `<TitleSequence />` → `<NarrationAudioWithMode volume={1.0} mode={narrationMode} />` → `<BGM volume={0.08} />` → `<SESequence />` の順で composition layer を並べることを lint (`test_main_video_layer_order_contract_lint`、Codex 推奨 PR-DT)。MainVideo の layer order と audio volume wiring を保護 | PR-DT |
| 185 | `template/src/MainVideo.tsx` が root `<AbsoluteFill style={{ backgroundColor: 'black' }}>` を持ち、base `<Video>` を `src={staticFile(VIDEO_FILE)}` / `volume={() => baseVolume}` / `style={{ width: '100%', height: '100%', objectFit: 'contain' }}` で描画することを lint (`test_main_video_base_video_fit_contract_lint`、Codex 推奨 PR-DU)。MainVideo の base video fit と黒背景 contract を保護 | PR-DU |
| 186 | `template/src/Root.tsx` が `Composition` を `remotion` から import し、`MainVideo` を `./MainVideo` から import し、`export const RemotionRoot: React.FC` 内の `<Composition id="MainVideo" component={MainVideo} />` 接続を保持することを lint (`test_root_composition_uses_main_video_component_lint`、Codex 推奨 PR-DV)。Remotion entrypoint と MainVideo component 接続 contract を保護 | PR-DV |
| 187 | `template/src/テロップテンプレート/TelopPlayer.tsx` が `frame >= startFrame && frame < endFrame` の半開区間で active segment を選び、該当なしは空 `<AbsoluteFill />` を返し、registry 経路では `segmentToSubtitleItem(current, fps)` で秒換算済み `SubtitleItem` を `subtitles: [...]` に包むことを lint (`test_telop_player_active_segment_range_and_subtitle_item_contract_lint`、Codex 推奨 PR-DW)。TelopPlayer の frame range と registry SubtitleData 変換 contract を保護 | PR-DW |
| 188 | `template/src/テロップテンプレート/telopTemplateRegistry.tsx` が `TelopCategory` / `TelopTemplateId` 型 union を保持し、`findTemplateIdByDisplayName(displayName)` は `displayName` 一致の registry key を返し、`listTemplatesByCategory(category)` は category 一致の registry keys を返すことを lint (`test_telop_template_registry_lookup_helpers_contract_lint`、Codex 推奨 PR-DX)。telopStyle displayName 逆引きと category list helper contract を保護 | PR-DX |
| 189 | `template/src/テロップテンプレート/Telop.tsx` の legacy path が `segment.template` 1〜6 を既存 template config に写像し、`style` fallback では success→`template3_gradientText`、warning→`getNegativeTemplate(segment.id)`、emphasis→`getEmphasisTemplate(segment.id)`、default→`template2_purpleStroke` を保つことを lint (`test_telop_legacy_template_selection_contract_lint`、Codex 推奨 PR-DY)。registry 未指定時の legacy telop template selection contract を保護 | PR-DY |
| 190 | `template/src/テロップテンプレート/Telop.tsx` の legacy path が `fadeOnly` を ID-based variation (`animation_fadeOnly` / `animation_fadeFromLeft` / `animation_slideFromLeft` / `animation_slideLeftFadeBlur`) に写像し、明示 animation (`slideIn` / `slideFromLeft` / `fadeBlurFromBottom` / `slideLeftFadeBlur` / `fadeFromRight` / `fadeFromLeft` / `charByChar` / `none`) と default を既存 animation config に写像することを lint (`test_telop_legacy_animation_selection_contract_lint`、Codex 推奨 PR-DZ)。registry 未指定時の legacy telop animation selection contract を保護 | PR-DZ |
| 191 | `template/src/テロップテンプレート/Telop.tsx` が `localFrame = frame - segment.startFrame` / `duration = endFrame - startFrame` を使い、fade duration を `duration / 3` で cap し、opacity は localFrame の clamp interpolate、slide は `spring({ frame: localFrame, fps, config: animation.spring })` と direction 別 translate で描画することを lint (`test_telop_render_timing_and_slide_transform_contract_lint`、Codex 推奨 PR-EA)。Telop legacy renderer の timing と slide transform contract を保護 | PR-EA |
| 192 | `template/src/テロップテンプレート/Telop.tsx` の `CharByCharText` が `charDelay` default / per-char `localFrame` offset / `spring` + `translateY` / per-char fade opacity / gradient color interpolation / NBSP space preservation を保つことを lint (`test_telop_char_by_char_text_contract_lint`、Codex 推奨 PR-EB)。charByChar legacy animation rendering contract を保護 | PR-EB |
| 193 | `template/src/テロップテンプレート/Telop.tsx` の SVG text branch が `strokeGradientId` / `fillGradientId` / `filterId`、`svgHeight` / `baseY`、stroke/fill gradient `<defs>`、`feDropShadow` filter、stroke/fill 二重 `<text>`、multi-line `<tspan>` render を保つことを lint (`test_telop_svg_text_defs_filter_gradient_contract_lint`、Codex 推奨 PR-EC)。background なし legacy telop SVG rendering contract を保護 | PR-EC |
| 194 | `template/src/テロップテンプレート/Telop.tsx` の `background.enabled` CSS text branch が `React.CSSProperties` の `textStyle`、font/textShadow/line-break style、fillGradient 時の CSS gradient text clipping、非 gradient 時の `font.color` fallback、背景 container style、`segment.text` paragraph render を保つことを lint (`test_telop_background_text_css_gradient_contract_lint`、Codex 推奨 PR-ED)。background 付き legacy telop rendering contract を保護 | PR-ED |
| 195 | `template/src/テロップテンプレート/Telop.tsx` の `hexToRgb` / `interpolateColor` helper が optional `#` 付き 6桁 HEX parse、invalid input の black fallback、RGB 各 channel の base-16 parse、`Math.round(c1 + (c2 - c1) * t)`、CSS `rgb(...)` return を保つことを lint (`test_telop_color_interpolation_helpers_contract_lint`、Codex 推奨 PR-EE)。charByChar gradient color interpolation の helper contract を保護 | PR-EE |
| 196 | `template/src/Narration/mode.ts` が `NARRATION_LEGACY_FILE = 'narration.wav'` / `NARRATION_READY_FILE = 'narration.ready.json'`、`getStaticFiles().map((f) => f.name)` の `Set` 化、`names.has(seg.file)` / `names.has(NARRATION_LEGACY_FILE)` による chunks/legacy 判定、`_modeCache` への chunks/legacy/none 代入と return を保つことを lint (`test_narration_mode_static_file_lookup_contract_lint`、Codex 推奨 PR-EF)。Narration mode の static-file lookup contract を保護 | PR-EF |
| 197 | `template/src/Narration/NarrationAudio.tsx` の `NarrationAudioWithMode` / `NarrationAudio` が `volume = 1.0` default、chunks/legacy 分岐、none fallback の `return null` を保ち、wrapper 側が `useNarrationMode()` で mode を取得することを lint (`test_narration_audio_none_and_default_volume_contract_lint`、Codex 推奨 PR-EG)。Narration audio の none/default-volume contract を保護 | PR-EG |
| 198 | `template/src/Narration/useNarrationMode.ts` が `useState(() => getNarrationMode())` 初期化、`useRef<Map<string,string>>` と `pendingRef`、`queueMicrotask` による cross-file update coalescing、sentinel 削除 event 無視、`lastModified:sizeInBytes` dedup、watcher cancel 収集と cleanup を保つことを lint (`test_use_narration_mode_watch_dedup_contract_lint`、Codex 推奨 PR-EH)。Studio hot-reload watcher の dedup/cleanup contract を保護 | PR-EH |
| 199 | `template/src/SoundEffects/BGM.tsx` が `BGMProps` の optional `volume`、`React.FC<BGMProps>` component の `volume = 0.3` default、`<Audio volume={() => volume} loop />` wiring を保つことを lint (`test_bgm_props_default_volume_contract_lint`、Codex 推奨 PR-EI)。BGM の props/default-volume contract を保護 | PR-EI |
| 200 | `template/src/videoConfig.ts` が `RESOLUTION_MAP` の youtube/short/square 寸法、`TELOP_CONFIG_MAP` の各 format 別 field/value、`RESOLUTION = RESOLUTION_MAP[FORMAT]` / `TELOP_CONFIG = TELOP_CONFIG_MAP[FORMAT]` の派生 export を保つことを lint (`test_video_config_format_values_contract_lint`、Codex 推奨 PR-EJ)。format 別 render/telop 設定値 contract を保護 | PR-EJ |
| 201 | `template/src/Title/Title.tsx` が opacity fade envelope、spring config、translateX range、gradient badge background/padding、title typography (`Noto Sans JP`/shadow/skew/nowrap)、`segment.text` render を保つことを lint (`test_title_visual_style_contract_lint`、Codex 推奨 PR-EK)。Title visual style contract を保護 | PR-EK |
| 202 | `template/src/InsertImage/InsertImage.tsx` が overlay/default branch の `staticFile(\`images/${segment.file}\`)`、overlay backdrop/flex centering/max 80% contain、default full-size image、infographic contain/photo cover、`segment.scale ?? 1` scale fallback を保つことを lint (`test_insert_image_render_variant_style_contract_lint`、Codex 推奨 PR-EL)。InsertImage type 別 render style contract を保護 | PR-EL |
| 203 | `template/src/Slides/Slide.tsx` が opacity fade envelope、AbsoluteFill wrapper、title `h1` typography、subtitle conditional paragraph、bullets conditional map、bullet emphasis fontWeight/text render を保つことを lint (`test_slide_content_render_contract_lint`、Codex 推奨 PR-EM)。Slide content render contract を保護 | PR-EM |
| 204 | `template/src/Slides` / `Title` / `InsertImage` / `Narration` の barrel `index` が MainVideo や外部 skill から使う named exports (`SlideSequence`/`TitleSequence`/`InsertImage`/`ImageSequence`/`NarrationAudioWithMode`/mode helpers/types 等) を保つことを lint (`test_template_component_barrel_exports_contract_lint`、Codex 推奨 PR-EN)。template component barrel export surface を保護 | PR-EN |
| 205 | `template/src/SoundEffects/SESequence.tsx` の `<Audio>` が `staticFile(\`se/${se.file}\`)` で SE asset directory prefix を保つことを lint (`test_se_sequence_asset_path_prefix_contract_lint`、Codex 推奨 PR-EO)。SE audio asset path contract を保護 | PR-EO |
| 206 | `template/src/Narration/useNarrationMode.ts` が `NARRATION_LEGACY_FILE` / `NARRATION_READY_FILE` / `type NarrationMode` / `getNarrationMode` / `invalidateNarrationMode` を `./mode` import block から取得することを lint (`test_use_narration_mode_imports_mode_exports_contract_lint`、Codex 推奨 PR-EP)。Narration hook の mode SSoT import contract を保護 | PR-EP |
| 207 | `template/src/index.css` が Tailwind v4 entrypoint の `@import "tailwindcss";` を保持することを lint (`test_template_index_css_tailwind_v4_entrypoint_lint`、Codex 推奨 PR-EQ)。Tailwind CSS entry contract を保護 | PR-EQ |
| 208 | `template/package.json` の `dependencies` が `tailwindcss` と `@remotion/tailwind-v4` を含み、両方とも v4-compatible spec であることを lint (`test_package_json_tailwind_v4_dependencies_lint`、Codex 推奨 PR-ER)。Tailwind v4 dependency contract を保護 | PR-ER |
| 209 | `template/src/メインテロップ` / `強調テロップ` / `ネガティブテロップ` の 30 template component が Props interface の `subtitleData: SubtitleData`、`React.FC<Props>` export、`subtitleData.subtitles.find(...)` active lookup、`currentSubtitle` 不在時 `return null`、`currentSubtitle.lines.map(...)` render を保つことを lint (`test_telop_template_components_subtitle_data_contract_lint`、Codex consult 推奨 PR-ES)。Telop registry component subtitleData contract を保護 | PR-ES |
| 210 | `template/src/テロップテンプレート/telopTypes.ts` が `TelopTemplateId` を `./telopTemplateRegistry` から type import し、`templateId?: TelopTemplateId` と `animation?: 'none' | 'slideIn' | 'fadeOnly' | 'slideFromLeft' | 'fadeBlurFromBottom' | 'slideLeftFadeBlur' | 'fadeFromRight' | 'fadeFromLeft' | 'charByChar'` を保つことを lint (`test_telop_segment_template_id_and_animation_contract_lint`、Codex 推奨 PR-ET)。TelopSegment registry templateId / animation union contract を保護 | PR-ET |
| 211 | `template/src/テロップテンプレート/index.tsx` が `Telop` / `TelopPlayer` / `telopData` / `TOTAL_FRAMES` / `FPS` / `TelopSegment` / `subtitleConfig` の barrel export を保つことを lint (`test_template_component_barrel_exports_contract_lint`、Codex 推奨 PR-EU)。Telop component barrel exports contract を保護 | PR-EU |
| 212 | `template/src/videoConfig.ts` が Root composition duration の SSoT である `export const SOURCE_DURATION_FRAMES = <positive integer>;` を保つことを lint (`test_videoconfig_source_duration_frames_numeric_export_lint`、Codex 推奨 PR-EV)。videoConfig source duration numeric export contract を保護 | PR-EV |
| 213 | `template/src/videoConfig.ts` の `/supermovie-init` rewrite block が `FORMAT: VideoFormat`、positive integer の `FPS` / `SOURCE_DURATION_FRAMES`、public-relative `VIDEO_FILE` export を保持することを lint (`test_videoconfig_supermovie_init_block_scalar_exports_lint`、Codex 推奨 PR-EW)。videoConfig init scalar rewrite block contract を保護 | PR-EW |
| 214 | `template/src/Slides/types.ts` が `SlideBullet` の `text: string` / `emphasis?: boolean` と `SlideSegment` の `subtitle?: string` / `bullets?: SlideBullet[]` / `align?: SlideAlignment` / `backgroundColor?: string` / `textColor?: string` を保つことを lint (`test_slide_types_bullet_and_optional_fields_contract_lint`、Codex 推奨 PR-EX)。Slide types bullet and optional display fields contract を保護 | PR-EX |
| 215 | `template/src/Title/titleData.ts` が `TitleSegment` type import、`FPS` import、`export const toFrame = (seconds: number) => Math.round(seconds * FPS);`、コメント除去後の `export const titleData: TitleSegment[] = [];` placeholder を保つことを lint (`test_title_data_placeholder_empty_array_and_toframe_export_contract_lint`、Codex 推奨 PR-EY)。Title data placeholder and toFrame export contract を保護 | PR-EY |
| 216 | `template/src/Slides/slideData.ts` がコメント除去後に `export const slideData: SlideSegment[] = [];` の empty placeholder を保つことを lint (`test_slide_data_placeholder_empty_array_contract_lint`、Codex 推奨 PR-EZ)。Slide data empty placeholder contract を保護 | PR-EZ |
| 217 | `template/src/InsertImage/insertImageData.ts` が `export const toFrame = (seconds: number) => Math.round(seconds * FPS);` とコメント除去後の `export const insertImageData: ImageSegment[] = [];` placeholder を保つことを lint (`test_insert_image_data_placeholder_empty_array_and_toframe_export_contract_lint`、Codex 推奨 PR-FA)。InsertImage data placeholder and toFrame export contract を保護 | PR-FA |
| 218 | `template/src/SoundEffects/seData.ts` がコメント除去後に `export const seData: SoundEffect[] = [];` の empty placeholder を保つことを lint (`test_se_data_placeholder_empty_array_contract_lint`、Codex 推奨 PR-FB)。SE data empty placeholder contract を保護 | PR-FB |
| 219 | `template/src/テロップテンプレート/telopData.ts` がコメント除去後に `export const telopData: TelopSegment[] = [];` の empty placeholder を保つことを lint (`test_telop_data_placeholder_empty_array_contract_lint`、Codex 推奨 PR-FC)。Telop data empty placeholder contract を保護 | PR-FC |
| 220 | `template/src/index.ts` が `registerRoot` を `remotion` から named import することを lint (`test_entrypoint_imports_register_root_from_remotion_lint`、Codex 推奨 PR-FD)。Remotion entrypoint import contract を保護 | PR-FD |
| 221 | `template/src/MainVideo.tsx` が `AbsoluteFill` / `Video` / `staticFile` を `remotion` から named import することを lint (`test_main_video_imports_remotion_primitives_lint`、Codex 推奨 PR-FE)。MainVideo Remotion primitive import contract を保護 | PR-FE |
| 222 | `template/src/MainVideo.tsx` が主要 layer/helper (`TelopPlayer` / `SESequence` / `BGM` / `ImageSequence` / `TitleSequence` / `SlideSequence` / `NarrationAudioWithMode` / `useNarrationMode` / `VIDEO_FILE`) を canonical path から named import することを lint (`test_main_video_canonical_layer_import_paths_lint`、Codex 推奨 PR-FF)。MainVideo layer import path contract を保護 | PR-FF |
| 223 | `template/src/MainVideo.tsx` が `const baseVolume = narrationMode.kind === 'none' ? 1.0 : 0;` を保つことを lint (`test_main_video_base_volume_none_branch_contract_lint`、Codex 推奨 PR-FG)。MainVideo base video volume branch contract を保護 | PR-FG |
| 224 | `template/src/Slides/SlideSequence.tsx` が `Sequence` を `remotion` から、`Slide` を `./Slide` から、`slideData` を `./slideData` から named import することを lint (`test_slide_sequence_canonical_imports_contract_lint`、Codex 推奨 PR-FH)。SlideSequence import contract を保護 | PR-FH |
| 225 | `template/src/InsertImage/ImageSequence.tsx` が `Sequence` を `remotion` から、`InsertImage` を `./InsertImage` から、`insertImageData` を `./insertImageData` から named import することを lint (`test_image_sequence_canonical_imports_contract_lint`、Codex 推奨 PR-FI)。ImageSequence import contract を保護 | PR-FI |
| 226 | `template/src/Title/Title.tsx` が `useCurrentFrame` / `interpolate` / `spring` / `useVideoConfig` / `Sequence` を `remotion` から、`titleData` を `./titleData` から、`TELOP_CONFIG` を `../videoConfig` から named import することを lint (`test_title_canonical_imports_contract_lint`、Codex 推奨 PR-FJ)。Title import contract を保護 | PR-FJ |
| 227 | `template/src/Narration/NarrationAudio.tsx` が `Audio` / `Sequence` / `staticFile` を `remotion` から、`useNarrationMode` を `./useNarrationMode` から、`NarrationMode` 型を `./mode` から import することを lint (`test_narration_audio_canonical_imports_contract_lint`、Codex 推奨 PR-FK)。NarrationAudio import contract を保護 | PR-FK |
| 228 | `template/src/Slides/Slide.tsx` が `AbsoluteFill` / `useCurrentFrame` / `interpolate` を `remotion` から、`SlideSegment` 型を `./types` から import することを lint (`test_slide_canonical_imports_contract_lint`、Codex 推奨 PR-FL)。Slide import contract を保護 | PR-FL |
| 229 | `template/src/InsertImage/InsertImage.tsx` が `useCurrentFrame` / `interpolate` / `Img` / `staticFile` を `remotion` から、`ImageSegment` 型を `./types` から import することを lint (`test_insert_image_canonical_imports_contract_lint`、Codex 推奨 PR-FM)。InsertImage import contract を保護 | PR-FM |
| 230 | `template/src/テロップテンプレート/Telop.tsx` が Remotion helper、Telop 型、`TELOP_CONFIG`、legacy template / animation config を canonical module から import することを lint (`test_telop_canonical_imports_contract_lint`、Codex 推奨 PR-FN)。Telop import contract を保護 | PR-FN |
| 231 | `template/src/テロップテンプレート/TelopPlayer.tsx` が React `useMemo`、Remotion helper、legacy `Telop`、`telopData`、registry 値/型、`TelopSegment` 型を canonical module から import することを lint (`test_telop_player_canonical_imports_contract_lint`、Codex 推奨 PR-FO)。TelopPlayer import contract を保護 | PR-FO |
| 232 | `template/src/テロップテンプレート/telopTemplateRegistry.tsx` が type-only React import、`SubtitleItem` / `SubtitleData` / `TelopComponent` / `TelopTemplateEntry` の exported surface、`telopTemplateRegistry` の `as const satisfies Record<string, TelopTemplateEntry>` を保つことを lint (`test_telop_template_registry_type_surface_contract_lint`、Codex 推奨 PR-FP)。Telop registry type surface contract を保護 | PR-FP |
| 233 | `template/src/テロップテンプレート/telopStyles.ts` の `getTextShadowCSS()` が `subtitleConfig.textShadow` から `offsetX` / `offsetY` / `blur` / `color` を取り CSS shadow string を返し、`getTextStrokeCSS()` が `subtitleConfig.textStroke` から `width` / `gradient` を返すことを lint (`test_telop_styles_css_helper_body_contract_lint`、Codex 推奨 PR-FQ)。Telop style CSS helper body contract を保護 | PR-FQ |
| 234 | `template/src/Narration/types.ts` の `NarrationSegment` が debug/alignment 用 optional fields `text?: string` / `sourceStartMs?: number` / `sourceEndMs?: number` を保持することを lint (`test_narration_segment_optional_debug_fields_contract_lint`、Codex 推奨 PR-FR)。NarrationSegment optional debug fields contract を保護 | PR-FR |
| 235 | `template/src/メインテロップ` / `強調テロップ` / `ネガティブテロップ` の 30 template component が React default import と Remotion `AbsoluteFill` / `useCurrentFrame` / `interpolate` / `Easing` named import を保つことを lint (`test_telop_template_components_canonical_imports_contract_lint`、Codex 推奨 PR-FS)。Telop template component import contract を保護 | PR-FS |
| 236 | `template/src/メインテロップ` / `強調テロップ` / `ネガティブテロップ` の 30 template component が `SubtitleItem` (`text` / `lines` / `start` / `end` / `startFrame` / `endFrame`) と `SubtitleData` (`fps` / `subtitles`) の exported interface surface を保つことを lint (`test_telop_template_components_subtitle_type_surface_contract_lint`、Codex 推奨 PR-FT)。Telop template component subtitle type surface contract を保護 | PR-FT |
| 237 | `template/src/メインテロップ` / `強調テロップ` / `ネガティブテロップ` の 30 template component が `currentSubtitle` の frame bounds から `duration` を算出し、`Math.floor(duration / 3)` と 3 frame cap の fade duration、long/short subtitle の opacity interpolate、`Easing.out(Easing.cubic)` scale interpolate、`transform: scale(${scale})` を保つことを lint (`test_telop_template_components_fade_scale_animation_contract_lint`、Codex 推奨 PR-FU)。Telop template component fade/scale animation contract を保護 | PR-FU |
| 238 | `template/src/テロップテンプレート/telopTemplateRegistry.tsx` の registry entry が 30 件、category 内訳 `main=12` / `emphasis=13` / `negative=5`、`displayName` と `Component` ref が重複なし、各 entry が `Component: <registryKey> as TelopComponent` を保つことを lint (`test_telop_template_registry_entry_cardinality_contract_lint`、Codex 推奨 PR-FV)。Telop registry entry cardinality contract を保護 | PR-FV |
| 239 | `template/src/テロップテンプレート/telopTemplateRegistry.tsx` が 30 template component を named/alias import し、import dir 内訳 `メインテロップ=12` / `強調テロップ=13` / `ネガティブテロップ=5`、local import name の重複なし、registry `Component` refs と import local names の完全一致を保つことを lint (`test_telop_template_registry_component_imports_contract_lint`、Codex 推奨 PR-FW)。Telop registry component import binding contract を保護 | PR-FW |
| 240 | `template/src/SoundEffects/SESequence.tsx` が `const v = se.volume ?? 1` で SE volume default を局所変数化し、Remotion `<Audio>` に `volume={() => v}` callback 形式で渡すことを lint (`test_se_sequence_volume_callback_contract_lint`、Codex 推奨 PR-FX)。SESequence volume callback contract を保護 | PR-FX |
| 241 | `template/src/Narration/NarrationAudio.tsx` の `NarrationAudioWithMode` と `NarrationAudio` の両方が `mode.segments.map((seg) => ...)`、`<Sequence key={seg.id} from={seg.startFrame} durationInFrames={seg.durationInFrames}>`、chunk `<Audio src={staticFile(seg.file)} volume={() => volume}>`、legacy `<Audio src={staticFile(mode.file)} volume={() => volume}>` を保つことを lint (`test_narration_audio_dual_component_sequence_wiring_contract_lint`、Codex 推奨 PR-FY)。NarrationAudio dual component Sequence/Audio wiring contract を保護 | PR-FY |
| 242 | `template/src/Slides/SlideSequence.tsx` / `template/src/InsertImage/ImageSequence.tsx` / `template/src/Title/Title.tsx` / `template/src/SoundEffects/SESequence.tsx` の exported sequence component が `React.FC` として Fragment を返し、その直下で対応 data array (`slideData` / `insertImageData` / `titleData` / `seData`) を `map(...)` することを lint (`test_sequence_components_return_fragment_wrapped_data_maps_lint`、Codex 推奨 PR-FZ)。Sequence component の余計な layout wrapper 追加を防ぐ contract を保護 | PR-FZ |
| 243 | `template/src/**/*.ts(x)` の `staticFile(...)` 引数を走査し、literal / template literal / 同一ファイル定数 / imported string constant (`VIDEO_FILE` 等) の解決可能範囲で public-relative path のみを許可し、`/` 始まり・`public/` prefix・`..` traversal を禁止することを lint (`test_static_file_public_relative_contract_lint`、Codex consult 推奨 PR-GA)。Remotion static asset path の public-relative contract を repo-wide に保護 | PR-GA |
| 244 | `template/vitest.config.ts` が `defineConfig` を `vitest/config` から import し、`test.environment = 'jsdom'` / `globals: true` / `setupFiles: ['./vitest.setup.ts']` / `include: ['src/**/*.test.{ts,tsx}']` を保ち、`template/package.json` の `scripts.test:react` が `vitest run --config vitest.config.ts` を実行することを lint (`test_vitest_react_test_discovery_contract_lint`、Codex consult 推奨 PR-GB)。React/Vitest test discovery contract を保護 | PR-GB |
| 245 | `template/src/MainVideo.tsx` が timeline data arrays (`slideData` / `insertImageData` / `titleData` / `telopData` / `seData` / `narrationData`) や各 data module を直接 import せず、layer component / hook / config の composition に留めることを lint (`test_main_video_layer_boundary_no_data_imports_lint`、Codex consult 推奨 PR-GC)。MainVideo layer boundary contract を保護 | PR-GC |
| 246 | `template/vitest.setup.ts` が `import '@testing-library/jest-dom/vitest';` を保持し、`template/package.json` の `devDependencies` に `@testing-library/jest-dom` が存在することを lint (`test_vitest_setup_jest_dom_import_contract_lint`、Codex 推奨 PR-GD)。Vitest setup の jest-dom matcher extension contract を保護 | PR-GD |
| 247 | `template/src/MainVideo.tsx` が `useNarrationMode()` を 1 回だけ呼び、戻り値を `const narrationMode` に集約することを lint (`test_main_video_single_narration_hook_call_contract_lint`、Codex 推奨 PR-GE)。Narration watcher の二重登録回避 contract を保護 | PR-GE |
| 248 | `template` 配下の JS/TS test-like files (`*.test.*` / `*.spec.*`) が `vitest.config.ts` の `include: ['src/**/*.test.{ts,tsx}']` から外れないことを lint (`test_vitest_react_test_files_match_include_contract_lint`、Codex consult 推奨 PR-GF)。React/Vitest test file inventory discovery contract を保護 | PR-GF |
| 249 | `template/src/Root.tsx` の最初の import が `import "./index.css";` であり、global CSS side-effect entrypoint を component/config import より前に保持することを lint (`test_root_css_import_first_contract_lint`、Codex 推奨 PR-GG)。Root global CSS import order contract を保護 | PR-GG |
| 250 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file が `import { useNarrationMode } from "./useNarrationMode";` より前に `vi.mock("remotion", ...)` を宣言することを lint (`test_vitest_use_narration_mode_tests_mock_remotion_before_sut_import_lint`、Codex consult 推奨 PR-GH)。useNarrationMode React tests の Remotion mock isolation/order contract を保護 | PR-GH |
| 251 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file が `beforeEach` を `vitest` から import し、`beforeEach` 内で `invalidateNarrationMode()` を呼ぶことを lint (`test_vitest_use_narration_mode_tests_reset_cache_before_each_lint`、Codex 推奨 PR-GI)。useNarrationMode React tests の module-scope mode cache reset contract を保護 | PR-GI |
| 252 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file が `import { useNarrationMode } from "./useNarrationMode";` より前に `vi.mock("./narrationData", ...)` を宣言することを lint (`test_vitest_use_narration_mode_tests_mock_narration_data_before_sut_import_lint`、Codex consult 推奨 PR-GJ)。useNarrationMode React tests の narrationData fixture mock isolation/order contract を保護 | PR-GJ |
| 253 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file の `vi.mock("remotion", ...)` が `watchStaticFile` と `getStaticFiles` を return object で提供することを lint (`test_vitest_use_narration_mode_tests_remotion_mock_api_surface_lint`、Codex 推奨 PR-GK)。useNarrationMode React tests の Remotion mock API surface contract を保護 | PR-GK |
| 254 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file が `renderHook` を `@testing-library/react` から import し、deprecated `@testing-library/react-hooks` を使わず、`template/package.json` の devDependency に `@testing-library/react` を持つことを lint (`test_vitest_use_narration_mode_tests_render_hook_import_contract_lint`、Codex 推奨 PR-GL)。useNarrationMode React hook tests の renderHook import/package surface contract を保護 | PR-GL |
| 255 | `template/src/Narration/useNarrationMode*.test.tsx` 各 file が実使用する `describe` / `it` / `expect` / `vi` / `beforeEach` / `afterEach` を `vitest` から明示 import することを lint (`test_vitest_use_narration_mode_tests_vitest_import_surface_lint`、Codex 推奨 PR-GM)。useNarrationMode React tests の Vitest API import surface contract を保護 | PR-GM |
| 256 | `template/src/MainVideo.tsx` が `export const MainVideo: React.FC = () => {` の named export surface を保ち、default export に切り替わらないことを lint (`test_main_video_named_export_contract_lint`、Codex 推奨 PR-GN)。Root.tsx の named import と Remotion component 接続 contract を保護 | PR-GN |
| 257 | `template/src/index.ts` が export を持たない side-effect only entrypoint のまま、`registerRoot(...)` を 1 回だけ呼ぶことを lint (`test_entrypoint_side_effect_only_no_exports_contract_lint`、Codex 推奨 PR-GO)。Remotion entrypoint が module API surface 化する drift と二重 root 登録を保護 | PR-GO |
| 258 | `template/src/videoConfig.ts` の public export surface が `VideoFormat` / `FORMAT` / `FPS` / `SOURCE_DURATION_FRAMES` / `VIDEO_FILE` / `RESOLUTION` / `TELOP_CONFIG` に限定され、default export を持たないことを lint (`test_videoconfig_public_export_surface_contract_lint`、Codex 推奨 PR-GP)。RESOLUTION_MAP / TELOP_CONFIG_MAP など内部 map の API surface 化を保護 | PR-GP |
| 259 | `template/src/Narration/mode.ts` の public export surface が `NARRATION_LEGACY_FILE` / `NARRATION_READY_FILE` / `NarrationMode` / `getNarrationMode` / `invalidateNarrationMode` に限定され、default export を持たないことを lint (`test_narration_mode_public_export_surface_contract_lint`、Codex 推奨 PR-GQ)。Narration mode SSoT helper の API surface creep を保護 | PR-GQ |
| 260 | `template/src/Narration/useNarrationMode.ts` の public export surface が `useNarrationMode` だけに限定され、default export を持たないことを lint (`test_use_narration_mode_public_export_surface_contract_lint`、Codex 推奨 PR-GR)。Narration hook module の API surface creep を保護 | PR-GR |
| 261 | `template/src/Narration/NarrationAudio.tsx` の public export surface が `NarrationAudioWithMode` / `NarrationAudio` に限定され、default export を持たないことを lint (`test_narration_audio_public_export_surface_contract_lint`、Codex 推奨 PR-GS)。Narration audio module の API surface creep を保護 | PR-GS |
| 262 | `template/src/Slides/index.tsx` の public export surface が `SlideSequence` / `Slide` / `SlideSegment` / `SlideBullet` / `SlideAlignment` / `slideData` に限定され、default export を持たないことを lint (`test_slides_barrel_public_export_surface_contract_lint`、Codex 推奨 PR-GT)。Slides barrel の API surface creep を保護 | PR-GT |
| 263 | `template/src/Title/index.ts` の public export surface が `TitleSequence` / `TitleSegment` に限定され、default export を持たないことを lint (`test_title_barrel_public_export_surface_contract_lint`、Codex 推奨 PR-GU)。Title barrel の API surface creep を保護 | PR-GU |
| 264 | `template/src/InsertImage/index.ts` の public export surface が `InsertImage` / `ImageSequence` / `ImageSegment` に限定され、default export を持たないことを lint (`test_insert_image_barrel_public_export_surface_contract_lint`、Codex 推奨 PR-GV)。InsertImage barrel の API surface creep を保護 | PR-GV |
| 265 | `template/src/Narration/index.ts` の public export surface が `NarrationAudio` / `NarrationAudioWithMode` / `narrationData` / `getNarrationMode` / `invalidateNarrationMode` / `NARRATION_LEGACY_FILE` / `useNarrationMode` / `NarrationSegment` / `NarrationMode` に限定され、default export を持たないことを lint (`test_narration_barrel_public_export_surface_contract_lint`、Codex 推奨 PR-GW)。Narration barrel の API surface creep を保護 | PR-GW |
| 266 | `template/src/テロップテンプレート/index.tsx` の public export surface が `Telop` / `TelopPlayer` / `telopData` / `TOTAL_FRAMES` / `FPS` / `TelopSegment` / `subtitleConfig` に限定され、default export を持たないことを lint (`test_telop_barrel_public_export_surface_contract_lint`、Codex 推奨 PR-GX)。Telop barrel の API surface creep を保護 | PR-GX |
| 267 | `template/src/SoundEffects/BGM.tsx` の public export surface が `BGM` に限定され、default export を持たないことを lint (`test_bgm_public_export_surface_contract_lint`、Codex 推奨 PR-GY)。BGM module の API surface creep を保護 | PR-GY |
| 268 | `template/src/SoundEffects/SESequence.tsx` の public export surface が `SESequence` に限定され、default export を持たないことを lint (`test_se_sequence_public_export_surface_contract_lint`、Codex 推奨 PR-GZ)。SESequence module の API surface creep を保護 | PR-GZ |
| 269 | `template/src/SoundEffects/SEPlayer.ts` の public export surface が `SoundEffect` に限定され、default export / re-export を持たないことを lint (`test_seplayer_public_export_surface_contract_lint`、Codex 推奨 PR-HA)。SEPlayer module の API surface creep を保護 | PR-HA |
| 270 | `template/src/SoundEffects/seData.ts` の public export surface が `seData` に限定され、default export / re-export を持たないことを lint (`test_se_data_public_export_surface_contract_lint`、Codex 推奨 PR-HB)。SE data module の API surface creep を保護 | PR-HB |
| 271 | `template/src/Slides/SlideSequence.tsx` の public export surface が `SlideSequence` に限定され、default export / re-export を持たないことを lint (`test_slide_sequence_public_export_surface_contract_lint`、Codex 推奨 PR-HC)。SlideSequence module の API surface creep を保護 | PR-HC |
| 272 | `template/src/Slides/Slide.tsx` の public export surface が `Slide` に限定され、default export / re-export を持たないことを lint (`test_slide_public_export_surface_contract_lint`、Codex 推奨 PR-HD)。Slide module の API surface creep を保護 | PR-HD |
| 273 | `template/src/Title/Title.tsx` の public export surface が `TitleSegment` / `TitleSequence` に限定され、default export / re-export を持たないことを lint (`test_title_public_export_surface_contract_lint`、Codex 推奨 PR-HE)。Title module の API surface creep を保護 | PR-HE |
| 274 | `template/src/Slides/slideData.ts` の public export surface が `slideData` に限定され、default export / re-export を持たないことを lint (`test_slide_data_public_export_surface_contract_lint`、Codex 推奨 PR-HF)。Slide data module の API surface creep を保護 | PR-HF |
| 275 | `template/src/Slides/types.ts` の public export surface が `SlideAlignment` / `SlideBullet` / `SlideSegment` に限定され、default export / re-export を持たないことを lint (`test_slide_types_public_export_surface_contract_lint`、Codex 推奨 PR-HG)。Slides type module の API surface creep を保護 | PR-HG |
| 276 | `template/src/Title/titleData.ts` の public export surface が `toFrame` / `titleData` に限定され、default export / re-export を持たないことを lint (`test_title_data_public_export_surface_contract_lint`、Codex 推奨 PR-HH)。Title data module の API surface creep を保護 | PR-HH |
| 277 | `template/src/テロップテンプレート/telopData.ts` の public export surface が `FPS` / `TOTAL_FRAMES` / `telopData` に限定され、default export / re-export を持たないことを lint (`test_telop_data_public_export_surface_contract_lint`、Codex 推奨 PR-HI)。Telop data module の API surface creep を保護 | PR-HI |
| 278 | `template/src/テロップテンプレート/telopTypes.ts` の public export surface が `TelopSegment` に限定され、default export / re-export を持たないことを lint (`test_telop_types_public_export_surface_contract_lint`、Codex 推奨 PR-HJ)。Telop type module の API surface creep を保護 | PR-HJ |
| 279 | `template/src/テロップテンプレート/telopConfigTypes.ts` の public export surface が Telop config 系 10 型に限定され、default export / re-export を持たないことを lint (`test_telop_config_types_public_export_surface_contract_lint`、Codex 推奨 PR-HK)。Telop config type module の API surface creep を保護 | PR-HK |
| 280 | `template/src/テロップテンプレート/telopStyles.ts` の public export surface が animation 9件 / template 6件 / `subtitleConfig` / CSS helper 2件に限定され、default export / re-export を持たないことを lint (`test_telop_styles_public_export_surface_contract_lint`、Codex 推奨 PR-HL)。Telop styles module の API surface creep を保護 | PR-HL |
| 281 | `template/src/テロップテンプレート/telopTemplateRegistry.tsx` の public export surface が registry API 9 件に限定され、default export / re-export を持たないことを lint (`test_telop_template_registry_public_export_surface_contract_lint`、Codex 推奨 PR-HM)。Telop template registry module の API surface creep を保護 | PR-HM |
| 282 | `template/src/テロップテンプレート/Telop.tsx` の public export surface が `Telop` に限定され、default export / re-export を持たないことを lint (`test_telop_public_export_surface_contract_lint`、Codex 推奨 PR-HN)。Telop module の API surface creep を保護 | PR-HN |
| 283 | `template/src/テロップテンプレート/TelopPlayer.tsx` の public export surface が `TelopPlayer` に限定され、default export / re-export を持たないことを lint (`test_telop_player_public_export_surface_contract_lint`、Codex 推奨 PR-HO)。TelopPlayer module の API surface creep を保護 | PR-HO |
| 284 | `template/src/Narration/narrationData.ts` の public export surface が `narrationData` に限定され、default export / re-export を持たないことを lint (`test_narration_data_public_export_surface_contract_lint`、Codex 推奨 PR-HP)。Narration data module の API surface creep を保護 | PR-HP |
| 285 | `template/src/Narration/types.ts` の public export surface が `NarrationSegment` に限定され、default export / re-export を持たないことを lint (`test_narration_types_public_export_surface_contract_lint`、Codex 推奨 PR-HQ)。Narration types module の API surface creep を保護 | PR-HQ |
| 286 | `template/src/MainVideo.tsx` の public export surface が `MainVideo` に限定され、default export / re-export を持たないことを lint (`test_main_video_public_export_surface_contract_lint`、Codex 推奨 PR-HR)。MainVideo module の API surface creep を保護 | PR-HR |
| 287 | `template/src/Root.tsx` の public export surface が `RemotionRoot` に限定され、default export / re-export を持たないことを lint (`test_root_public_export_surface_contract_lint`、Codex 推奨 PR-HS)。Root module の API surface creep を保護 | PR-HS |
| 288 | `template/src/InsertImage/ImageSequence.tsx` の public export surface が `ImageSequence` に限定され、default export / re-export を持たないことを lint (`test_image_sequence_public_export_surface_contract_lint`、Codex 推奨 PR-HT)。ImageSequence module の API surface creep を保護 | PR-HT |
| 289 | `template/src/InsertImage/insertImageData.ts` の public export surface が `toFrame` / `insertImageData` に限定され、default export / re-export を持たないことを lint (`test_insert_image_data_public_export_surface_contract_lint`、Codex 推奨 PR-HU)。InsertImage data module の API surface creep を保護 | PR-HU |
| 290 | `template/src/InsertImage/types.ts` の public export surface が `ImageSegment` に限定され、default export / re-export を持たないことを lint (`test_insert_image_types_public_export_surface_contract_lint`、Codex 推奨 PR-HV)。InsertImage types module の API surface creep を保護 | PR-HV |
| 291 | `template/src/InsertImage/InsertImage.tsx` の public export surface が `InsertImage` に限定され、default export / re-export を持たないことを lint (`test_insert_image_public_export_surface_contract_lint`、Codex 推奨 PR-HW)。InsertImage module の API surface creep を保護 | PR-HW |
| 292 | `template/src/メインテロップ` / `強調テロップ` / `ネガティブテロップ` の 30 component file が `SubtitleItem` / `SubtitleData` / component const の 3 export のみに限定され、default export / re-export を持たないことを lint (`test_telop_template_components_public_export_surface_contract_lint`、Codex 推奨 PR-HX)。Telop template component file の API surface creep を保護 | PR-HX |
| 293 | `skills/supermovie-init/SKILL.md` の telop style 選択肢 / トーン別 default が `telopTemplateRegistry.tsx` の 30 displayName と category に同期し、`白青テロップver2` → `WhiteBlueTeleopV2` lookup guidance を保持することを lint (`test_supermovie_init_telop_style_choices_match_registry_lint`、Codex 推奨 PR-HY)。supermovie-init の hearing choices と registry の drift を保護 | PR-HY |
| 294 | `skills/supermovie-subtitles/SKILL.md` の style→templateId mapping table が `telopTemplateRegistry.tsx` の registry key / category / displayName と同期し、`normal/emphasis/warning/success` の templateId 既定値が registry 実体に存在することを lint (`test_supermovie_subtitles_template_id_mapping_matches_registry_lint`、Codex 推奨 PR-HZ)。supermovie-subtitles の deterministic style mapping と registry の drift を保護 | PR-HZ |
| 295 | `skills/supermovie-se/SKILL.md` のスタイル別SE選択マトリクスが `TelopSegment.style` union (`normal/emphasis/warning/success`) と同期し、各 style の SE候補 / volume / 配置 guidance が素材カタログと一致することを lint (`test_supermovie_se_style_matrix_matches_telop_style_union_lint`、Codex 推奨 PR-IA)。supermovie-se の SE selection docs と telop style schema の drift を保護 | PR-IA |
| 296 | `CLAUDE.md` の `project-config.json` schema sample にある `telopStyle` 既定値 (`白青テロップver2` / `オレンジグラデーション` / `黒紫グラデ`) が `telopTemplateRegistry.tsx` の displayName / category / templateId と同期することを lint (`test_claude_project_config_telop_style_defaults_match_registry_lint`、Codex 推奨 PR-IB)。全スキル共通 schema sample と registry の drift を保護 | PR-IB |
| 297 | `skills/supermovie-init/SKILL.md` の `project-config.json` sample が `CLAUDE.md` の全スキル共通 schema sample と同じ `telopStyle` / 基本ヒアリング値を保持し、`telopStyle.{main,emphasis,negative}` が registry category に存在することを lint (`test_supermovie_init_project_config_telop_style_defaults_match_claude_lint`、Codex 推奨 PR-IC)。supermovie-init の生成 sample と共通 schema の drift を保護 | PR-IC |
| 298 | `skills/supermovie-se/SKILL.md` の `seData.ts` output sample / validation table が `template/src/SoundEffects/SEPlayer.ts` の `SoundEffect` fields と `template/src/SoundEffects/seData.ts` の typed export contract に同期することを lint (`test_supermovie_se_output_schema_docs_match_sound_effect_contract_lint`、Codex 推奨 PR-ID)。supermovie-se の output docs と SE schema の drift を保護 | PR-ID |
| 299 | `CLAUDE.md` の `seData.ts（SoundEffect型）` schema code block が `template/src/SoundEffects/SEPlayer.ts` の `SoundEffect` field order / optional marker / type と同期することを lint (`test_claude_se_data_schema_matches_sound_effect_contract_lint`、Codex 推奨 PR-IE)。全スキル共通 SE schema と実装型の drift を保護 | PR-IE |
| 300 | `CLAUDE.md` の `titleData.ts（TitleSegment型）` schema code block が `template/src/Title/Title.tsx` の `TitleSegment` field order / type と同期することを lint (`test_claude_title_data_schema_matches_title_segment_contract_lint`、Codex 推奨 PR-IF)。全スキル共通 title schema と実装型の drift を保護 | PR-IF |
| 301 | `CLAUDE.md` の `insertImageData.ts（ImageSegment型）` schema code block が `template/src/InsertImage/types.ts` の `ImageSegment` field order / optional marker / union type と同期することを lint (`test_claude_insert_image_schema_matches_image_segment_contract_lint`、Codex 推奨 PR-IG)。全スキル共通 insert image schema と実装型の drift を保護 | PR-IG |
| 302 | `CLAUDE.md` の `cutData.ts（CutSegment型）` schema code block が `skills/supermovie-cut/SKILL.md` の `cutData.ts` 生成例にある `CutSegment` field order / type と同期することを lint (`test_claude_cut_data_schema_matches_supermovie_cut_skill_lint`、Codex 推奨 PR-IH)。全スキル共通 cut schema と生成スキルdocsの drift を保護 | PR-IH |
| 303 | `skills/supermovie-slides/SKILL.md` の `SlideSegment` shorthand schema が `template/src/Slides/types.ts` の `SlideSegment` field order / optional marker と同期することを lint (`test_supermovie_slides_schema_docs_match_slide_segment_contract_lint`、Codex 推奨 PR-II)。supermovie-slides 生成docsと実装型の drift を保護 | PR-II |
| 304 | `skills/supermovie-image-gen/SKILL.md` の `insertImageData.ts` 出力サンプルが `template/src/InsertImage/types.ts` の `ImageSegment` 必須field order / typed export / `type` union と同期することを lint (`test_supermovie_image_gen_output_sample_matches_image_segment_contract_lint`、Codex 推奨 PR-IJ)。supermovie-image-gen 生成docsと画像挿入schemaの drift を保護 | PR-IJ |
| 305 | `skills/supermovie-image-gen/SKILL.md` の Gemini API `-a` format/aspect table が `template/src/videoConfig.ts` の `RESOLUTION_MAP` (`youtube=16:9` / `short=9:16` / `square=1:1`) と同期することを lint (`test_supermovie_image_gen_aspect_table_matches_video_config_lint`、Codex 推奨 PR-IK)。画像生成format guidanceと動画format SSoTの drift を保護 | PR-IK |
| 306 | `skills/supermovie-image-gen/SKILL.md` の type別 prompt template 見出し / type別表示ロジック table が `template/src/InsertImage/types.ts` の `ImageSegment.type` union (`infographic` / `photo` / `overlay`) と同期することを lint (`test_supermovie_image_gen_type_docs_match_image_segment_union_lint`、Codex 推奨 PR-IL)。画像生成type guidanceとレンダーtype schemaの drift を保護 | PR-IL |
| 307 | `skills/supermovie-slides/SKILL.md` の Phase 4 validation docs が `template/scripts/build_slide_data.py` の validation guard (`title empty` / bullets上限 `MAX_BULLETS_PER_SLIDE=5` / overlap / cut_total) と同期することを lint (`test_supermovie_slides_validation_docs_match_build_slide_data_lint`、Codex 推奨 PR-IM)。supermovie-slides 検証docsと生成script guardの drift を保護 | PR-IM |
| 308 | `skills/supermovie-slides/SKILL.md` のトーン別見た目 table (`align` / `backgroundColor` / bullet emphasis 比率) が `template/scripts/build_slide_data.py` の `style_for_tone()` 定義と同期することを lint (`test_supermovie_slides_tone_style_docs_match_build_slide_data_lint`、Codex 推奨 PR-IN)。supermovie-slides tone guidance と生成script style default の drift を保護 | PR-IN |
| 309 | `skills/supermovie-slides/SKILL.md` の実行コマンド (`build_slide_data.py` default / `--mode topic` / `generate_slide_plan.py --output` / `build_slide_data.py --plan` / `--strict-plan`) が `template/scripts/build_slide_data.py` と `template/scripts/generate_slide_plan.py` の argparse 定義と同期することを lint (`test_supermovie_slides_command_docs_match_script_cli_lint`、Codex 推奨 PR-IO)。supermovie-slides CLI guidance と生成script entrypoint の drift を保護 | PR-IO |
| 310 | `skills/supermovie-slides/SKILL.md` の frame 計算 / LLM plan 設計根拠 (`cutData` 経由 playback frame、cutData 不在時 `ms / 1000 * FPS`、word index → script 側 frame 変換) が `template/scripts/build_slide_data.py` の `ms_to_playback_frame()` wrapper / deterministic topic・segment・plan 変換 / `playbackEnd` clamp と同期することを lint (`test_supermovie_slides_frame_conversion_docs_match_build_slide_data_lint`、Codex 推奨 PR-IP)。supermovie-slides timing guidance と生成script frame conversion の drift を保護 | PR-IP |
| 311 | `skills/supermovie-slides/SKILL.md` の `slide_plan.json` schema example / validation rules が `template/scripts/generate_slide_plan.py` の prompt template (`version` / `slides` / `id` / `startWordIndex` / `endWordIndex` / `title` / `subtitle` / `bullets` / `align` / `videoLayer`) と `template/scripts/build_slide_data.py` の `PLAN_VERSION` / `MAX_BULLETS_PER_SLIDE` / allowed align・videoLayer / validator rules に同期することを lint (`test_supermovie_slides_plan_schema_docs_match_script_contract_lint`、Codex 推奨 PR-IQ)。supermovie-slides LLM plan schema guidance と生成・検証script contract の drift を保護 | PR-IQ |
| 312 | `skills/supermovie-image-gen/SKILL.md` の Gemini 生成先 `public/images/generated/`、ファイル名規約、`insertImageData.ts` sample の `file: 'generated/*.png'`、検証 table の asset root が `template/src/InsertImage/insertImageData.ts` placeholder と `template/src/InsertImage/InsertImage.tsx` の `staticFile(\`images/${segment.file}\`)` 解決契約に同期することを lint (`test_supermovie_image_gen_asset_path_docs_match_insert_image_contract_lint`、Codex 推奨 PR-IR)。image-gen asset 保存先 docs と renderer static asset path の drift を保護 | PR-IR |
| 313 | `skills/supermovie-se/SKILL.md` の `public/se/` 素材チェック・共通素材 copy command・ファイル存在 validation と `seData.ts` sample の bare `.mp3` filename が `template/src/SoundEffects/SESequence.tsx` の `staticFile(\`se/${se.file}\`)` 解決契約と同期することを lint (`test_supermovie_se_asset_path_docs_match_sequence_contract_lint`、Codex 推奨 PR-IS)。SE asset runbook と renderer static asset path の drift を保護 | PR-IS |
| 314 | `skills/supermovie-se/SKILL.md` の `BTN_ROTATION` / `WSH_ROTATION` / `CUR_ROTATION` round-robin docs がスタイル別SE選択マトリクスと素材カタログに同期し、連続回避 / ラウンドロビン / `index循環` guidance を保持することを lint (`test_supermovie_se_rotation_docs_match_style_matrix_lint`、Codex 推奨 PR-IT)。SE rotation runbook と style matrix / catalog の drift を保護 | PR-IT |
| 315 | `skills/supermovie-narration/SKILL.md` の Phase 4 Remotion 接合 docs (`narrationData > narration.wav > null` 優先順位 / base volume / `<Sequence>`・legacy `<Audio>`・`null` 分岐) が `template/src/Narration/mode.ts` / `template/src/MainVideo.tsx` / `template/src/Narration/NarrationAudio.tsx` の runtime wiring と同期することを lint (`test_supermovie_narration_mode_docs_match_runtime_contract_lint`、Codex 推奨 PR-IU)。narration docs と renderer mode contract の drift を保護 | PR-IU |
| 316 | `skills/supermovie-narration/SKILL.md` の output docs / 連携マップ (`public/narration.wav` / `public/narration/chunk_NNN.wav` / `public/narration/chunk_meta.json` / `src/Narration/narrationData.ts`) が `template/scripts/voicevox_narration.py` の path constants / chunk write / `narrationData.ts`・`chunk_meta.json` atomic write / success summary keys と同期することを lint (`test_supermovie_narration_output_docs_match_voicevox_paths_lint`、Codex 推奨 PR-IV)。narration output runbook と voicevox writer path contract の drift を保護 | PR-IV |
| 317 | `skills/supermovie-narration/SKILL.md` の Phase 2 入力解決 docs (`--script` / `--script-json` / `transcript_fixed.json` 優先順位、`startMs` / `endMs` optional timing、transcript 不在時の迂回) が `template/scripts/voicevox_narration.py` の `collect_chunks()` / transcript missing guard と同期することを lint (`test_supermovie_narration_input_docs_match_collect_chunks_lint`、Codex 推奨 PR-IW)。narration input runbook と chunk collection priority / timing metadata contract の drift を保護 | PR-IW |
| 318 | `skills/supermovie-narration/SKILL.md` の FPS 解決 docs (`--fps` 引数 → `src/videoConfig.ts` の `FPS` → default 30、`--fps <= 0` exit 4 / videoConfig parse failure fallback) が `template/scripts/voicevox_narration.py` の argparse / `read_video_config_fps(PROJ)` fallback / `invalid_fps` emission / `write_narration_data(..., fps, ...)` と同期することを lint (`test_supermovie_narration_fps_docs_match_voicevox_resolution_lint`、Codex 推奨 PR-IX)。narration FPS runbook と voicevox frame conversion source-of-truth の drift を保護 | PR-IX |
| 319 | `skills/supermovie-narration/SKILL.md` の engine availability docs (`/version` healthcheck、engine 不在時 skip exit 0、`--require-engine` 時 exit 4、asset gate による render 継続) が `template/scripts/voicevox_narration.py` の `check_engine()` / argparse `--require-engine` / `engine_skipped`・`engine_unavailable_strict` emission と同期することを lint (`test_supermovie_narration_engine_docs_match_require_engine_contract_lint`、Codex 推奨 PR-IY)。VOICEVOX 起動有無の運用 docs と CI strict mode の drift を保護 | PR-IY |
| 320 | `skills/supermovie-narration/SKILL.md` の synthesis API docs (`POST /audio_query` → `POST /synthesis`、speaker default/override、`--list-speakers`、chunk skip WARN / all chunks exit 5) が `template/scripts/voicevox_narration.py` の `synthesize()` / `list_speakers()` / argparse `--speaker`・`--list-speakers` / synth failure loop と同期することを lint (`test_supermovie_narration_synthesis_docs_match_voicevox_api_contract_lint`、Codex 推奨 PR-IZ)。VOICEVOX API call order と speaker routing runbook の drift を保護 | PR-IZ |
| 321 | `skills/supermovie-narration/SKILL.md` の stale cleanup docs (synthesis 前の stale chunk / 旧 `narration.wav` cleanup、legacy wav 削除失敗 exit 7 / `StaleCleanupError`) が `template/scripts/voicevox_narration.py` の `cleanup_stale_all()` / `reset_narration_data_ts()` / `stale_cleanup_fail` emission と同期することを lint (`test_supermovie_narration_cleanup_docs_match_stale_reset_contract_lint`、Codex 推奨 PR-JA)。stale legacy 再生事故防止 runbook と cleanup implementation の drift を保護 | PR-JA |
| 322 | `skills/supermovie-narration/SKILL.md` の VAD / cut-aware docs (`vad_result.json`、`ms_to_playback_frame` 同型 mapping、cut 範囲外 fallback WARN、VAD 部分破損 exit 8 / synthesis 前 fail-fast) が `template/scripts/voicevox_narration.py` の `project_load_cut_segments(fail_fast=True)` / `ms_to_playback_frame(...)` / `vad_invalid` emission と同期することを lint (`test_supermovie_narration_vad_docs_match_cut_aware_failfast_contract_lint`、Codex 推奨 PR-JB)。cut-aware narration timing と stale legacy 防止 fail-fast contract の drift を保護 | PR-JB |
| 323 | `skills/supermovie-narration/SKILL.md` の overlap docs (前 chunk 終端 > 現 startFrame の WARN、`chunk_meta.json` `overlaps[]` 記録、`<Sequence>` 重複による二重再生 signal、transcript 再分割 / `--allow-partial` 検討) が `template/scripts/voicevox_narration.py` の `overlap_warns` / mitigation warning / chunk_meta `overlaps` field と同期することを lint (`test_supermovie_narration_overlap_docs_match_chunk_meta_warning_lint`、Codex 推奨 PR-JC)。narration overlap runbook と debug metadata の drift を保護 | PR-JC |
| 324 | `skills/supermovie-narration/SKILL.md` の partial chunk failure docs (`--allow-partial` なしでは exit 6、部分 chunk 削除、`narrationData.ts` 空 array / all-or-nothing) が `template/scripts/voicevox_narration.py` の argparse `--allow-partial` / `partial_chunks_disallowed` emission / partial chunk unlink gate と同期することを lint (`test_supermovie_narration_partial_docs_match_allow_partial_contract_lint`、Codex 推奨 PR-JD)。partial synthesis failure runbook と all-or-nothing guard の drift を保護 | PR-JD |
| 325 | `skills/supermovie-narration/SKILL.md` の WAV error docs (`WAV 結合 format mismatch` は WARN + skip、`wave.Error` / `EOFError` は exit 6 rollback) が `template/scripts/voicevox_narration.py` の `concat_wavs_atomic()` format guard / `measure_duration_seconds()` header probe / `write_narration_data_wave_error`・`concat_fail` emission と同期することを lint (`test_supermovie_narration_wav_docs_match_header_concat_contract_lint`、Codex 推奨 PR-JE)。WAV header / concat failure runbook と all-or-nothing rollback の drift を保護 | PR-JE |
| 326 | `template/scripts/voicevox_narration.py` の publish sentinel (`public/narration.ready.json` / schemaVersion 1 / `write_narration_ready()` / `sentinel_write_fail`) が `template/src/Narration/mode.ts` の `NARRATION_READY_FILE` と `template/src/Narration/useNarrationMode.ts` の sentinel watcher / delete-event guard / metadata dedup と同期することを lint (`test_supermovie_narration_ready_sentinel_contract_lint`、Codex 推奨 PR-JF)。writer と runtime の ready signal drift を保護 | PR-JF |
| 327 | `skills/supermovie-subtitles/SKILL.md` の BudouX first docs (`build_telop_data.py` command、`template/scripts/budoux_split.mjs`、`--baseline`、legacy fallback guidance) が `template/scripts/build_telop_data.py` の argparse `--baseline` / `call_budoux()` subprocess / BudouX split default path / legacy fallback と同期することを lint (`test_supermovie_subtitles_budoux_docs_match_build_telop_cli_lint`、Codex 推奨 PR-JG)。subtitles generation runbook と build script CLI の drift を保護 | PR-JG |
| 328 | `skills/supermovie-subtitles/SKILL.md` の Phase 3 BudouX 改行 docs (short 閾値 10、phrase boundary、固有名詞/単語途中保護、閾値以下 no-op) が `template/scripts/build_telop_data.py` の `insert_linebreak()` threshold / preserve・ASCII word guard / phrase-aware tier order / `_candidate_score()` 呼び出しと同期することを lint (`test_supermovie_subtitles_linebreak_docs_match_insert_linebreak_contract_lint`、Codex 推奨 PR-JH)。subtitles linebreak runbook と phrase-aware wrapper の drift を保護 | PR-JH |
| 329 | `skills/supermovie-subtitles/SKILL.md` の KPI gate docs (`hard_word_split_count` / `linebreak_inside_preserve_count` / `single_char_telops` / `two_char_tail_telops` / `frame_overlap_count`) が `template/scripts/compare_telop_split.py` の KPI metric 初期化 / print key order / gate 条件と同期することを lint (`test_supermovie_subtitles_kpi_docs_match_compare_telop_split_contract_lint`、Codex 推奨 PR-JI)。BudouX verify runbook と比較 script の gate drift を保護 | PR-JI |
| 330 | `skills/supermovie-subtitles/SKILL.md` の BudouX helper docs (`template/scripts/budoux_split.mjs`、`scripts/budoux_split.mjs --in input.json --out phrases.json`、`loadDefaultJapaneseParser().parse()`、`npm: budoux`) が `template/scripts/budoux_split.mjs` の CLI parser / `--text` smoke mode / JSON read-write contract と `template/package.json` の `budoux` dependency に同期することを lint (`test_supermovie_subtitles_budoux_helper_and_dependency_contract_lint`、Codex 推奨 PR-JJ)。BudouX helper runbook と Node dependency drift を保護 | PR-JJ |
| 331 | `skills/supermovie-transcribe/SKILL.md` の `transcript.json` 出力スキーマ / runner docs (word timestamp ms 変換、confidence、`transcript_audio.wav` → `transcript.json`) が `CLAUDE.md` の `transcript.json / transcript_fixed.json` 共通 schema とファイルパス規約に同期することを lint (`test_supermovie_transcribe_output_schema_docs_match_claude_transcript_schema_lint`、Codex 推奨 PR-JK)。transcribe runbook と全スキル共通 transcript contract の drift を保護 | PR-JK |
| 332 | `skills/supermovie-transcript-fix/SKILL.md` の `transcript_fixed.json` / `transcript_corrections.json` 出力スキーマ、`typo_dict.json` 構造、transcript 前提条件が `CLAUDE.md` の共通 transcript schema / `fix_meta` 追加規約 / ファイルパス規約 / 用語統一に同期することを lint (`test_supermovie_transcript_fix_output_schema_docs_match_claude_transcript_contract_lint`、Codex 推奨 PR-JL)。transcript-fix runbook と全スキル共通 transcript contract の drift を保護 | PR-JL |
| 333 | `skills/supermovie-cut/SKILL.md` の VAD runner docs (Silero VAD、16kHz、speech/silence ms 変換、0.5 秒 default、`vad_result.json` 出力スキーマ) が `CLAUDE.md` の `transcript_audio.wav` / `vad_result.json` / `cutData.ts` ファイルパス規約に同期することを lint (`test_supermovie_cut_vad_result_schema_docs_match_claude_path_contract_lint`、Codex 推奨 PR-JM)。cut VAD runbook と共通 file path contract の drift を保護 | PR-JM |
| 334 | `skills/supermovie-cut/SKILL.md` の VAD / transcript gap / keep segment / cutData mapping docs が `template/scripts/timeline.py` の `validate_vad_schema()` / `build_cut_segments_from_vad()` / `load_cut_segments()` / `ms_to_playback_frame()` helper contract と `CLAUDE.md` の cutData schema に同期することを lint (`test_supermovie_cut_timeline_helper_docs_match_vad_mapping_contract_lint`、Codex 推奨 PR-JN)。cut docs と共通 timeline helper の VAD mapping drift を保護 | PR-JN |
| 335 | `template/scripts/timeline.py` の `validate_transcript_segment()` / `validate_transcript_segments()` helper contract (`segments[].text/start/end`、任意 timing、`require_timing=True` strict、start <= end) が `CLAUDE.md` の transcript schema と `skills/supermovie-slides/SKILL.md` の `segments[] (start/end ms + text)` 入力 docs に同期することを lint (`test_timeline_transcript_segment_validation_docs_match_schema_contract_lint`、Codex 推奨 PR-JO)。共通 timeline validation と transcript schema docs の drift を保護 | PR-JO |
| 336 | `skills/supermovie-slides/SKILL.md` の `slideData.ts` 出力先 / backup / 完了報告 docs が `template/scripts/build_slide_data.py` の `render_slide_data_ts()` typed import・generated comment・`export const slideData: SlideSegment[]`・optional field render・backup/write flow と `template/src/Slides/slideData.ts` placeholder に同期することを lint (`test_supermovie_slides_render_output_docs_match_build_slide_data_lint`、Codex 推奨 PR-JP)。slideData generated surface と slides runbook の drift を保護 | PR-JP |
| 337 | `skills/supermovie-slides/SKILL.md` の Phase 3-C `generate_slide_plan.py --output <PROJECT>/slide_plan.json` / `build_slide_data.py --plan` handoff docs と `docs/OBSERVABILITY.md` の generate_slide_plan output path redaction / dry-run 2-emission docs が `template/scripts/generate_slide_plan.py` の `slide_plan.json` default output・API key skip・`out_path.write_text()`・safe `wrote:` stdout・`slides:` stdout・success JSON tail (`slides` / `output`) と同期することを lint (`test_supermovie_slides_generate_plan_output_docs_match_script_contract_lint`、Codex 推奨 PR-JQ)。LLM optional plan の write surface と observability runbook の drift を保護 | PR-JQ |
| 338 | `skills/supermovie-slides/SKILL.md` の deterministic topic grouping docs (`1.5 秒以上` silence split / `4-5 segments` fallback / max bullets / segment fallback) が `template/scripts/build_slide_data.py` の `SILENCE_THRESHOLD_MS`・`MAX_SEGMENTS_PER_SLIDE`・`MAX_BULLETS_PER_SLIDE`・`group_topics()` gap split・topic mode title/subtitle/bullets source・segment mode no-bullets contract と同期することを lint (`test_supermovie_slides_topic_grouping_docs_match_build_slide_data_lint`、Codex 推奨 PR-JR)。topic grouping guidance と deterministic slide builder の drift を保護 | PR-JR |
| 339 | `skills/supermovie-init/SKILL.md` の Phase 3-3 videoConfig SSoT 書き換え docs (`FORMAT` / `FPS` / `SOURCE_DURATION_FRAMES` / `VIDEO_FILE` / RESOLUTION_MAP derived resolution / Root・telopData・titleData touch しない) が `template/src/videoConfig.ts` の `VideoFormat` union・supermovie-init comment・FORMAT/FPS/SOURCE_DURATION_FRAMES/VIDEO_FILE exports・RESOLUTION/TELOP_CONFIG derived exports と同期することを lint (`test_supermovie_init_videoconfig_ssot_docs_match_template_contract_lint`、Codex 推奨 PR-JS)。init runbook と template SSoT の drift を保護 | PR-JS |
| 340 | `skills/supermovie-init/SKILL.md` の preflight `project-config.json source.*` nested schema docs (`raw` / `display` / `rotation.*` / `aspect` / `sar` / `dar` / `inferred_format` / `fps.*` / `codec.*` / `color.*` / `streams.*` / `risks`) が `template/scripts/preflight_video.py` の `build_source()` core fields・`detect_vfr()` fps fields・`source["risks"]`・`--write-config` の `cfg["source"]` / display-derived `cfg["resolution"]` / `cfg["format"]` write contract と同期することを lint (`test_supermovie_init_preflight_source_schema_docs_match_script_contract_lint`、Codex 推奨 PR-JT)。init preflight runbook と analyzer output schema の drift を保護 | PR-JT |
| 341 | `skills/supermovie-image-gen/SKILL.md` の Phase 4-2 type 別表示ロジック docs (`infographic` 全画面固定表示 / `photo` 全画面 + Ken Burns 1.0→1.05 / `overlay` 暗背景 0.7 + 中央配置) が `template/src/InsertImage/InsertImage.tsx` の overlay 分岐・rgba(0,0,0,0.7)・centered flex・80% contain bounds・photo Ken Burns interpolate・infographic contain / photo cover・absolute full-screen wrapper と同期することを lint (`test_supermovie_image_gen_display_logic_docs_match_insert_image_renderer_lint`、Codex 推奨 PR-JU)。image-gen runbook と renderer type behavior の drift を保護 | PR-JU |
| 342 | `skills/supermovie-image-gen/SKILL.md` の Phase 5 validation docs (画像ファイル存在 / frame 重複 / テロップ共存 / endFrame 範囲 / 枚数目安) が `template/src/InsertImage/ImageSequence.tsx` の `insertImageData.map()`・`<Sequence from={segment.startFrame} durationInFrames={segment.endFrame - segment.startFrame}>`・`<InsertImage segment={segment} />` と `ImageSegment` / `insertImageData.ts` の frame/file/type placeholder contract に同期することを lint (`test_supermovie_image_gen_validation_docs_match_image_sequence_contract_lint`、Codex 推奨 PR-JV)。image-gen validation runbook と sequence frame-range surface の drift を保護 | PR-JV |
| 343 | `skills/supermovie-init/SKILL.md` の Phase 5/6 setup handoff docs (`npm install` / `npx remotion studio` を skill 内で自動実行しない、Phase 4 後に手動コマンドを提示、連携マップの `npm run dev` preview) が `template/package.json` の `scripts.dev = remotion studio`・`scripts.render`・`scripts.lint`・`scripts.test:timeline`・`private: true` と同期することを lint (`test_supermovie_init_setup_docs_match_package_dev_script_contract_lint`、Codex 推奨 PR-JW)。init handoff runbook と generated project package surface の drift を保護 | PR-JW |
| 344 | `skills/supermovie-se/SKILL.md` の前提条件チェックリスト / Phase 1 入力 docs (`/supermovie-subtitles`、`src/テロップテンプレート/telopData.ts`、`src/Title/titleData.ts`、`project-config.json` tone、`public/se/`、任意の `/supermovie-image-gen` / `src/InsertImage/insertImageData.ts`) が `template/src/テロップテンプレート/telopData.ts`・`template/src/Title/titleData.ts`・`template/src/SoundEffects/seData.ts`・`SEPlayer.ts`・`SESequence.tsx` の data surface と同期することを lint (`test_supermovie_se_input_prereq_docs_match_template_data_surface_lint`、Codex 推奨 PR-JX)。SE 入力 runbook と template data wiring の drift を保護 | PR-JX |
| 345 | `skills/supermovie-se/SKILL.md` の完了報告フォーマット / 連携マップ (`SE数`・テロップ比・POP/BTN/WSH/CUR/BUZZ/他 summary・音量レンジ 0.25〜0.35・`npm run dev` preview・`seData.ts` 手動調整) が SE カタログ / style matrix volume range / `template/package.json` の `scripts.dev = remotion studio` / `template/src/SoundEffects/seData.ts` typed export と同期することを lint (`test_supermovie_se_completion_docs_match_preview_and_catalog_contract_lint`、Codex 推奨 PR-JY)。SE 完了 handoff と preview runbook の drift を保護 | PR-JY |
| 346 | `skills/supermovie-se/SKILL.md` のエラーハンドリング表 (`telopData が空`→`/supermovie-subtitles`、SE素材不足→共通素材コピー、`project-config.json` なし→カジュアル密度 40% fallback) が前提条件チェックリスト / Phase 2 asset copy command / Phase 1 `project-config.json tone` / トーン別密度表 / `template/src/テロップテンプレート/telopData.ts` generator comment / `template/src/SoundEffects/seData.ts` typed export と同期することを lint (`test_supermovie_se_error_handling_docs_match_fallback_contract_lint`、Codex 推奨 PR-JZ)。SE recovery runbook と fallback density の drift を保護 | PR-JZ |
| 347 | `skills/supermovie-se/SKILL.md` のトーン別 SE 密度 table (`プロフェッショナル` 30-40% / `エンタメ` 50-70% / `カジュアル` 40-50% / `教育的` 25-35%) が `skills/supermovie-init/SKILL.md` のヒアリング tone 4択と `project-config.json` の `tone` sample (`プロフェッショナル`) に同期することを lint (`test_supermovie_se_tone_density_docs_match_init_tone_choices_lint`、Codex 推奨 PR-KA)。SE density runbook と init tone vocabulary の drift を保護 | PR-KA |
| 348 | `skills/supermovie-slides/SKILL.md` の完了報告 / 連携マップ (`slideData.ts` 保存先、`SlideSequence layer (Phase 3-A)`、`supermovie-image-gen / se` handoff、`npm run render`) が `template/src/MainVideo.tsx` の `Video → SlideSequence → ImageSequence` layer order、`template/src/Slides/SlideSequence.tsx` の `slideData.map()` / frame-range `<Sequence>`、Slides barrel exports、`template/package.json` の `scripts.render = remotion render MainVideo out/video.mp4` と同期することを lint (`test_supermovie_slides_layer_handoff_docs_match_mainvideo_contract_lint`、Codex 推奨 PR-KB)。slides completion handoff と render layer wiring の drift を保護 | PR-KB |
| 349 | `skills/supermovie-transcribe/SKILL.md` の Phase 4 音声抽出 / Phase 5-2 runner command / Phase 6-3 cleanup docs (`ffprobe -select_streams a` 音声トラック確認、`ffmpeg -vn -acodec pcm_s16le -ar 16000 -ac 1` 16kHz mono WAV、`transcript_audio.wav`→`transcript.json` handoff、`transcribe_runner.py` のみ削除して音声ファイル保持) が `CLAUDE.md` の `transcript.json` / `transcript_audio.wav` / `.venv` ファイルパス規約と `README.md` の ffmpeg / AssemblyAI 任意 prerequisites に同期することを lint (`test_supermovie_transcribe_audio_extract_docs_match_file_path_contract_lint`、Codex 推奨 PR-KC)。transcribe audio extraction runbook と file path / prerequisite contract の drift を保護 | PR-KC |
| 350 | `skills/supermovie-transcribe/SKILL.md` の Phase 1 engine detection / Phase 2 hearing skip / Phase 3 venv install / fallback chain / Phase 6-4 `project-config.json transcribe` docs (`mlx_whisper` / `faster_whisper` import probe、Apple Silicon優先、`speakers >= 2` のみ AssemblyAI、`--speakers N` と既存 `project-config.json` による省略、`.venv` 作成・pip install・検証、GPU/モデル/メモリ fallback) が `CLAUDE.md` の `project-config.json transcribe` sample / `.venv` path / AssemblyAI scope と `README.md` の AssemblyAI 任意 prerequisite に同期することを lint (`test_supermovie_transcribe_engine_config_docs_match_project_config_contract_lint`、Codex 推奨 PR-KD)。transcribe engine selection runbook と project-config persistence の drift を保護 | PR-KD |
| 351 | `skills/supermovie-transcribe/SKILL.md` の完了報告フォーマット / 連携マップ (`文字起こし完了`、環境 summary、text preview、word/segment count、音声長、`<PROJECT>/transcript.json` 保存先、`/supermovie-transcript-fix` next step、`transcript.json`→`transcript_fixed.json` handoff) が `CLAUDE.md` の正規ワークフロー / file path contract と `skills/supermovie-transcript-fix/SKILL.md` の前提条件 (`/supermovie-transcribe` 済み、`transcript.json` + `words` 配列、missing transcript recovery) に同期することを lint (`test_supermovie_transcribe_completion_handoff_docs_match_workflow_contract_lint`、Codex 推奨 PR-KE)。transcribe completion report と transcript-fix handoff の drift を保護 | PR-KE |
| 352 | `skills/supermovie-transcript-fix/SKILL.md` の完了報告フォーマット / 連携マップ (`文字起こし修正完了`、辞書/LLM/フィラー/修正率 summary、`transcript_fixed.json` / `transcript_corrections.json` 保存先、`transcript_fixed.json` 確認、`/supermovie-cut` next step、`transcript_fixed.json`→`cutData.ts` handoff) が `CLAUDE.md` の正規ワークフロー / file path contract と `skills/supermovie-cut/SKILL.md` の前提条件 (`/supermovie-transcript-fix` 済み、`transcript_fixed.json` + `words` 配列、missing fixed transcript recovery) に同期することを lint (`test_supermovie_transcript_fix_completion_handoff_docs_match_cut_handoff_contract_lint`、Codex 推奨 PR-KF)。transcript-fix completion report と cut handoff の drift を保護 | PR-KF |
| 353 | `skills/supermovie-transcript-fix/SKILL.md` の LLM prompt 絶対ルール / Phase 5 validation table / エラーハンドリング表 (`text` のみ編集、word追加/削除/分割/結合禁止、word数一致、timestamp順序、30%超過警告、LLM JSON retry、corrections index 範囲チェック、typo_dict JSON recovery、overlap confidence解決) が同一 skill 内の guard contract として同期することを lint (`test_supermovie_transcript_fix_error_handling_docs_match_validation_contract_lint`、Codex 推奨 PR-KG)。transcript-fix validation/error recovery runbook の drift を保護 | PR-KG |
| 354 | `skills/supermovie-transcript-fix/SKILL.md` の `typo_dict.json` schema (`replace` / `fillers.remove` / `fillers.keep_in_context` / `preserve`) と LLM修正由来の学習フィードバック基準 (`2回以上` は辞書追加候補、`1回のみ` / 複数正解がありうる同音異義語は提案しない、追加後は Phase 2 で即修正) が correction history の `phase: "llm"` / reason 付き出力と同期することを lint (`test_supermovie_transcript_fix_typo_learning_docs_match_dict_guard_lint`、Codex 推奨 PR-KH)。transcript-fix typo_dict learning runbook の drift を保護 | PR-KH |
| 355 | `skills/supermovie-cut/SKILL.md` の完了報告フォーマット / 実行タイミング注意 / 連携マップ (`カット完了`、元/カット後 duration、無音短縮/内容カット/フィラー除去 count、`cutData.ts` / `telopData.ts` 生成、`npm run dev` preview、`/supermovie-subtitles` 再生成、cut は subtitles 前推奨) が `CLAUDE.md` の正規ワークフロー / file path contract と `template/package.json` の `scripts.dev = remotion studio` に同期することを lint (`test_supermovie_cut_completion_preview_docs_match_subtitles_handoff_lint`、Codex 推奨 PR-KI)。cut completion/preview handoff の drift を保護 | PR-KI |
| 356 | `skills/supermovie-cut/SKILL.md` の severity table / ユーザー確認 block / カットモード選択 / Phase 6 validation / エラーハンドリング表 (`high/medium/low`、`auto/aggressive/silence-only/manual`、元動画50%以上、telop frame範囲、crossfade、Silero VAD fallback、missing `transcript_fixed.json`、50%未満時 `silence-only` 提案、16kHz再変換) が同一 skill 内の mode/error guard contract として同期することを lint (`test_supermovie_cut_error_validation_docs_match_mode_contract_lint`、Codex 推奨 PR-KJ)。cut mode/validation/error recovery runbook の drift を保護 | PR-KJ |
| 357 | `skills/supermovie-subtitles/SKILL.md` の完了報告フォーマット / 連携マップ (`テロップ＆タイトル生成完了`、style / line count summary、`telopData.ts` / `titleData.ts` 保存先、`/supermovie-image-gen` next step、`npm run dev` preview、slides / narration / image-gen / se handoff) が `CLAUDE.md` の正規ワークフロー、`template/package.json` の `scripts.dev = remotion studio`、`template/src/テロップテンプレート/telopData.ts` / `template/src/Title/titleData.ts` の generated data surface、`skills/supermovie-image-gen/SKILL.md` の前提条件に同期することを lint (`test_supermovie_subtitles_completion_handoff_docs_match_workflow_contract_lint`、Codex 推奨 PR-KK)。subtitles completion/preview handoff の drift を保護 | PR-KK |
| 358 | `skills/supermovie-subtitles/SKILL.md` の Phase 5 style / animation / legacy template docs (`normal/emphasis/warning/success`、`fadeOnly/slideIn/charByChar/slideFromLeft`、legacy `template` 1..6 fallback、`templateId` registry 優先、tone別 animation比率、強調/ネガティブ/ポジティブ辞書) が `template/src/テロップテンプレート/telopTypes.ts` の `TelopSegment` style / template / animation union と `template/src/テロップテンプレート/TelopPlayer.tsx` の registry優先→legacy fallback runtime に同期することを lint (`test_supermovie_subtitles_style_animation_docs_match_telop_segment_contract_lint`、Codex 推奨 PR-KL)。subtitles style assignment runbook と TelopSegment schema の drift を保護 | PR-KL |
| 359 | `skills/supermovie-subtitles/SKILL.md` の Phase 6 title generation docs (`transcript_fixed.json` の `segments`、5秒以上の無音、話題切り替わり、質問→回答、5〜15セグメント目安、最大15文字、`TitleSegment` 型出力) と Phase 7 `titleData.ts` 保存先が `template/src/Title/Title.tsx` の `TitleSegment` / `TitleSequence` runtime、`template/src/Title/titleData.ts` の typed export / `toFrame` helper、`CLAUDE.md` の titleData schema / filepath / workflow に同期することを lint (`test_supermovie_subtitles_title_generation_docs_match_title_data_contract_lint`、Codex 推奨 PR-KM)。subtitles title generation runbook と Title data surface の drift を保護 | PR-KM |
| 360 | `skills/supermovie-image-gen/SKILL.md` の完了報告フォーマット / 連携マップ (`画像生成・配置完了`、type別生成枚数、`public/images/generated/` 保存先、`insertImageData.ts` 更新、`npm run dev` preview、`/supermovie-se` SE handoff、`insertImageData.ts` handoff) が `template/src/InsertImage/insertImageData.ts` の generated data surface、`template/src/InsertImage/ImageSequence.tsx` の sequence wiring、`template/package.json` の `scripts.dev = remotion studio`、`skills/supermovie-se/SKILL.md` の画像生成/insertImageData 任意前提、`CLAUDE.md` の image-gen→SE workflow に同期することを lint (`test_supermovie_image_gen_completion_handoff_docs_match_sequence_contract_lint`、Codex 推奨 PR-KN)。image-gen completion/preview handoff の drift を保護 | PR-KN |
| 361 | `skills/supermovie-image-gen/SKILL.md` のエラーハンドリング表 (`GEMINI_API_KEY` 未設定、Gemini API rate limit retry/skip、生成品質低下、空 `telopData.ts`、`project-config.json` fallback、`gemini-api-image` スキル不在) が同 skill の前提条件チェックリスト、Gemini生成コマンド、`insertImageData.ts` output docs、`template/src/テロップテンプレート/telopData.ts` / `template/src/Title/titleData.ts` / `template/src/InsertImage/insertImageData.ts` の generated data surface、`CLAUDE.md` の Gemini env / image-gen workflow に同期することを lint (`test_supermovie_image_gen_error_handling_docs_match_prereq_contract_lint`、Codex 推奨 PR-KO)。image-gen recovery runbook と prerequisite/data surface の drift を保護 | PR-KO |
| 362 | `skills/supermovie-image-gen/SKILL.md` の Phase 1 content analysis docs (入力 `telopData.ts` / `titleData.ts` / `project-config.json`、判定表の `infographic` / `photo` / `overlay` / タイトル切り替わり `なし`、候補 JSON `suggestedType`) が `template/src/InsertImage/types.ts` の `ImageSegment.type` union、`template/src/テロップテンプレート/telopData.ts` / `template/src/Title/titleData.ts` の generated data surface、`CLAUDE.md` の `project-config.json` format/tone/notes schema に同期することを lint (`test_supermovie_image_gen_content_analysis_docs_match_image_segment_type_contract_lint`、Codex 推奨 PR-KP)。image-gen analysis runbook と generated image type contract の drift を保護 | PR-KP |
| 363 | `skills/supermovie-image-gen/SKILL.md` の Phase 2 画像計画ヒアリング docs (候補提示の 3 timing ranges、prompt案、追加/削除・prompt修正・type変更・表示タイミング変更、`infographic` / `photo` / `overlay` type adjustment) が Phase 1 候補 JSON (`insertAt.startFrame/endFrame`、`reason`、`suggestedType`、`promptDraft`、`priority`) と `template/src/InsertImage/types.ts` の `ImageSegment` frame/type surface に同期することを lint (`test_supermovie_image_gen_plan_hearing_docs_match_candidate_contract_lint`、Codex 推奨 PR-KQ)。image-gen planning handoff と candidate schema の drift を保護 | PR-KQ |
| 364 | `skills/supermovie-image-gen/SKILL.md` の Phase 3 prompt templates (`infographic` no-text/high-contrast infographic、`photo` photorealistic stock image、`overlay` dark/text-on-top background、全 type の `aspect ratio: [format]`) が `template/src/InsertImage/types.ts` の `ImageSegment.type` union、Phase 3-1 aspect table (`youtube` 16:9 / `short` 9:16 / `square` 1:1)、Gemini command の `--prompt` / `-a <アスペクト比>` handoff に同期することを lint (`test_supermovie_image_gen_prompt_templates_match_type_and_aspect_contract_lint`、Codex 推奨 PR-KR)。image-gen prompt runbook と type/aspect command surface の drift を保護 | PR-KR |
| 365 | `skills/supermovie-slides/SKILL.md` の Phase 1 required input docs (`transcript_fixed.json` の `segments[]` / `words[]`、`project-config.json` の `format` / `tone`) が `template/scripts/build_slide_data.py` の required path load / missing-input guard / `fmt`・`tone` defaults / `segments`・`words` extraction と `CLAUDE.md` の filepath / project-config schema に同期することを lint (`test_supermovie_slides_required_input_docs_match_build_slide_data_lint`、Codex 推奨 PR-KS)。slides input runbook と script entrypoint contract の drift を保護 | PR-KS |
| 366 | `docs/REMOTION_CODEX_PLUGIN_INTEGRATION.md` の SuperMovie / official Remotion Codex plugin 境界 plan (`SuperMovie-owned`、delegated official Remotion rule layer、Codex skill migration、minimal docs-only spike、upstream non-goals) が local skill directory set と公式 plugin concepts (`openai/plugins/plugins/remotion`、`.codex-plugin/plugin.json`、`skills/remotion/SKILL.md`、`remotion-best-practices`、主要 `rules/*.md`) に同期することを lint (`test_remotion_codex_plugin_integration_plan_docs_contract_lint`、Codex 推奨 PR-KT)。official Remotion plugin を SuperMovie 置換ではなく補完 layer として扱う境界を保護 | PR-KT |
| 367 | `skills/supermovie-telop-creator/SKILL.md` の Phase 4 preview / Phase 6 cleanup docs (`TelopPreview` Composition を Root.tsx に一時追加、`npm run dev` で Remotion Studio preview、統合時に Root.tsx から削除) が `template/src/Root.tsx` の production contract (default Composition は `MainVideo` 1 件のみ、`TelopPreview` / `PreviewComposition` を残さない) と同期することを lint (`test_supermovie_telop_creator_preview_cleanup_docs_match_root_contract_lint`、Codex 推奨 PR-KU)。telop-creator preview artifact の残置による Remotion entrypoint drift を保護 | PR-KU |
| 368 | `skills/supermovie-telop-creator/SKILL.md` の Phase 6 registry-first 統合 docs (`telopTemplateRegistry.tsx`、`templateId`、`{ category, displayName, Component }`、`main`/`emphasis`/`negative`、`findTemplateIdByDisplayName()` / `listTemplatesByCategory()`、legacy `telopStyles.ts` 任意) が `template/src/テロップテンプレート/telopTemplateRegistry.tsx` の exported registry API と `TelopPlayer.tsx` の `templateId` registry render path に同期することを lint (`test_supermovie_telop_creator_registry_integration_docs_match_registry_contract_lint`、Codex 推奨 PR-KV)。telop-creator の統合手順が旧 `telopStyles.ts` primary へ戻る drift を保護 | PR-KV |
| 369 | `skills/supermovie-telop-creator/SKILL.md` の Phase 3 component skeleton docs (`SubtitleItem` / `SubtitleData` export、必須 `subtitleData` prop、`subtitleData.subtitles.find(...)`、`currentSubtitle` null fallback、`telopData` import 禁止、`TelopPlayer` から `subtitleData` を受ける bridge note) が `telopTemplateRegistry.tsx` の `TelopComponent` 型と `TelopPlayer.tsx` の registry render path に同期することを lint (`test_supermovie_telop_creator_component_skeleton_docs_match_subtitledata_contract_lint`、Codex 推奨 PR-KW)。新規 telop component 手順が旧 `telopData` 直読み skeleton に戻る drift を保護 | PR-KW |
| 370 | Remotion-facing SuperMovie skills (`supermovie-init` / `subtitles` / `slides` / `image-gen` / `se` / `narration` / `telop-creator`) が official Remotion Codex plugin の `remotion-best-practices` bridge note (Remotion code/component 設計時だけ参照、SuperMovie schema / file path / Japanese workflow / Python lint gate は repo source of truth) を保持することを lint (`test_remotion_bridge_note_in_remotion_facing_skills_lint`、Codex 推奨 PR-KX)。official Remotion plugin を workflow 置換ではなく advisory layer として扱う境界を各 skill に反映 | PR-KX |
| 371 | `skills/supermovie-remotion-bridge/SKILL.md` advisory router が official `remotion-best-practices` を Remotion implementation review layer に限定し、SuperMovie schema / file path / Japanese workflow / Python lint gate を source of truth に保ち、README command table と integration plan に同期することを lint (`test_supermovie_remotion_bridge_skill_docs_contract_lint`、Codex 推奨 PR-KY)。official Remotion plugin 連携の入口を runtime 変更なしで追加し、upstream PR / maintainer contact を optional_later 境界に固定 | PR-KY |
| 372 | `skills/supermovie-skill-creator/SKILL.md` の SKILL.md scaffold docs が current plugin skill contract (`name` / `description` / `argument-hint` / `allowed-tools` / `effort` canonical frontmatter、trigger phrase、`skills: "./skills/"` discovery root、`skills/supermovie-<name>/SKILL.md` 配置、README command table / OBSERVABILITY sync、Python lint gate) に同期することを lint (`test_supermovie_skill_creator_scaffold_docs_match_plugin_skill_lint`、Codex 推奨 PR-KZ)。新規 skill 作成手順が旧 `.claude/skills` 配置や effort 欠落 scaffold に戻る drift を保護 | PR-KZ |
| 373 | Remotion-facing SuperMovie skills (`supermovie-init` / `subtitles` / `slides` / `image-gen` / `se` / `narration` / `telop-creator`) が official `remotion-best-practices` を直参照せず、まず `/supermovie-remotion-bridge` を開き、bridge skill の判定で必要な場合だけ official Remotion guidance を参照する note に同期することを lint (`test_remotion_facing_skills_route_through_bridge_skill_lint`、Codex 推奨 PR-LA)。official Remotion plugin を skill-level router 経由の advisory layer として固定し、SuperMovie source-of-truth 境界の drift を保護 | PR-LA |
| 374 | `skills/supermovie-skill-creator/SKILL.md` の品質チェックリスト 12 行 (`YAML frontmatter` / ロール定義 / ワークフロー図 / 前提条件 / フェーズ分割 / データスキーマ / バリデーション / エラーテーブル / 完了報告 / 次のステップ / テーブル活用 / コード例) と `12項目中10個以上` 合格・`9個以下` 改善 threshold が scaffold contract に同期することを lint (`test_supermovie_skill_creator_quality_checklist_docs_match_scaffold_contract_lint`、Codex 推奨 PR-LB)。新規 skill 生成時の品質 gate が frontmatter / docs sync だけに縮退する drift を保護 | PR-LB |
| 375 | `skills/supermovie-subtitles/SKILL.md` の Phase 2 LLM optional plan docs (`LLM 経路は将来オプション化`、`telop_plan.json` validate / invalid→BudouX fallback 設計、`現時点未実装`) が `template/scripts/build_telop_data.py` の現 runtime (LLM/`--plan`/`telop_plan.json`/Anthropic 経路なし、BudouX default + `--baseline` 比較のみ) と同期することを lint (`test_supermovie_subtitles_llm_plan_docs_mark_unimplemented_lint`、Codex 推奨 PR-LC)。未実装の LLM plan を実装済み手順として誤案内する drift を保護 | PR-LC |
| 376 | `docs/REMOTION_CODEX_PLUGIN_INTEGRATION.md` と `skills/supermovie-remotion-bridge/SKILL.md` が upstream PR 作成・close/reopen・maintainer contact・upstream merge 完了条件化・force push / remote history rewrite を non-goal / optional_later に固定し、fork/local docs-and-lint branch で代替する境界を持つことを lint (`test_remotion_upstream_pr_lifecycle_stays_optional_later_lint`、Codex 推奨 PR-LD)。official Remotion plugin 連携が upstream lifecycle 操作へ drift する事故を防ぎ、fork 内完結の実装/review/test loop 境界を保護 | PR-LD |
| 377 | `docs/OBSERVABILITY.md` の Provider Notes にある Gemini API (image gen) 行 (`$/image`、`GEMINI_API_KEY`、Anthropic rate env とは別系統) が `skills/supermovie-image-gen/SKILL.md` の `gemini-api-image` / `GEMINI_API_KEY` 手順と `CLAUDE.md` の画像生成セクションに同期し、image-gen docs が `ANTHROPIC_API_KEY` / `SUPERMOVIE_RATE_*` / `generate_slide_plan` 経路を参照しないことを lint (`test_supermovie_image_gen_gemini_provider_env_boundary_lint`、Codex 推奨 PR-LE)。Gemini 画像生成の provider/env 境界が Anthropic slide-plan cost/rate 経路へ drift する事故を防ぐ | PR-LE |
| 378 | `docs/OBSERVABILITY.md` の Provider Notes / Rate Env Var Convention が Kling / HeyGen / SadTalker 等の future provider を `provider 個別` / `別 doc 化` / v1 canonical only として扱い、現行 `template/scripts/*.py` (test runner 除外) と active `skills/*/SKILL.md` に future provider 実装語彙・API env (`KLING_API_KEY` 等) が混入していないことを lint (`test_observability_future_provider_runtime_boundary_lint`、Codex 推奨 PR-LF)。将来 provider の検証前実装が既存 SuperMovie runtime / skill 手順へ silent に入り込む drift を防ぐ | PR-LF |
| 379 | `_observability.py` の `SCHEMA_VERSION` / `REDACTION_VERSION` が別定数として残り、payload でも root `schema_version` と nested `redaction.version` に分離され、docs §Open Questions の redaction `version` bump policy (`schema_version とは独立`、redaction rule 変更時のみ bump) と同期することを lint (`test_observability_redaction_version_policy_boundary_lint`、Codex 推奨 PR-LG)。schema bump と redaction rule bump の混同や top-level `redaction_version` drift を防ぐ | PR-LG |
| 380 | `docs/OBSERVABILITY.md` の Provider Notes が現行 local engine を VOICEVOX のみに保ち (`local engine、cost なし` / `telemetry 対象外`)、§Open Questions の `VOICEVOX 以外の local engine` cost policy (`cost null vs 削除`) を未解決のまま保持し、`voicevox_narration.py` に cost/rate 実装語彙が混入しないことを lint (`test_observability_local_engine_cost_policy_stays_open_lint`、Codex 推奨 PR-LH)。非VOICEVOX local engine の未設計 cost 方針を実装済み扱いにする drift を防ぐ | PR-LH |
| 381 | `template/scripts/timeline.py` の VAD / transcript timing validation が Python の `bool` (int subclass) を start/end numeric として受け付けず、`int|float` のみを timing として扱うことを lint (`test_timeline_validation_rejects_bool_timing_values`、Codex 推奨 PR-LI)。`start=True` / `end=False` のような typo が ms/frame mapping に silent に入る drift を防ぐ | PR-LI |
| 382 | `template/scripts/timeline.py` の VAD / transcript timing validation が `NaN` / `Infinity` / `-Infinity` を start/end timing として受け付けず、finite `int|float` のみを timing として扱うことを lint (`test_timeline_validation_rejects_non_finite_timing_values`、Codex 推奨 PR-LJ)。非有限値が playback frame 計算や start/end 順序判定に silent に入る drift を防ぐ | PR-LJ |
| 383 | `template/scripts/build_telop_data.py` の cut mapping wrapper が `template/scripts/timeline.py` の `build_cut_segments_from_vad` へ委譲し、inline `cursor_ms` / playback frame dict 構築を持たないことを wiring lint (`test_build_scripts_wiring`、Codex 推奨 PR-LK)。slide / telop / narration の VAD→cut timeline mapping source が分岐する drift を防ぐ | PR-LK |
| 384 | `template/scripts/build_slide_data.py` の cut mapping wrapper が `template/scripts/timeline.py` の `build_cut_segments_from_vad` へ委譲し、inline `cursor_ms` / playback frame dict 構築を持たないことを wiring lint (`test_build_scripts_wiring`、Codex 推奨 PR-LL)。slide 側の source-level guard を telop 側と対称にし、VAD→cut timeline mapping source の drift を防ぐ | PR-LL |
| 385 | `template/scripts/voicevox_narration.py` が `template/scripts/timeline.py` の `load_cut_segments` / `ms_to_playback_frame` を直接 import し、`project_load_cut_segments()` で `fail_fast=True` を維持することを wiring lint (`test_build_scripts_wiring`、Codex 推奨 PR-LM)。narration の VAD 部分破損が soft fallback に戻り stale narration 経路へ流れる drift を防ぐ | PR-LM |
| 386 | `_observability.py` の `build_cost_payload(currency=...)` が uppercase 3-letter ASCII code のみを受け付け、lowercase / mixed-case / non-ASCII / 3文字以外を reject することを lint (`test_observability_build_cost_payload_currency_canonical_case_lint`、Codex 推奨 PR-LN)。`USD` / `usd` / `Usd` が downstream cost aggregator で別 bucket になる drift を防ぐ | PR-LN |
| 387 | `template/scripts/timeline.py` の `build_cut_segments_from_vad()` / `ms_to_playback_frame()` が `fps` に positive int のみを受け付け、bool / float / str / zero / negative を reject することを lint (`test_timeline_rejects_invalid_fps_values`、Codex 推奨 PR-LO)。`fps=True` や `fps=0` が playback frame 計算に silent に入る drift を防ぐ | PR-LO |
| 388 | `template/scripts/timeline.py` の `ms_to_playback_frame()` が `ms` に finite int|float のみを受け付け、bool / str / non-finite を reject することを lint (`test_ms_to_playback_frame_rejects_invalid_ms_values`、Codex 推奨 PR-LP)。`ms=True` や `ms=NaN` が playback frame 計算に silent に入る drift を防ぐ | PR-LP |
| 389 | `template/scripts/timeline.py` の `ms_to_playback_frame()` が `cut_segments` に list[dict] と必須 key (`originalStartMs` / `originalEndMs` / `playbackStart` / `playbackEnd`) を要求し、空 list の no-cut fallback だけを維持することを lint (`test_ms_to_playback_frame_rejects_invalid_cut_segments_shape`、Codex 推奨 PR-LQ)。空 tuple / key 欠落 dict が direct conversion や KeyError に silent に流れる drift を防ぐ | PR-LQ |
| 390 | `template/scripts/timeline.py` の `ms_to_playback_frame()` が `cut_segments[*].originalStartMs/originalEndMs` を finite int|float、`playbackStart/playbackEnd` を int frame として検査することを lint (`test_ms_to_playback_frame_rejects_invalid_cut_segment_value_types`、Codex 推奨 PR-LR)。`playbackStart=True` / `originalStartMs=NaN` のような値型 drift が frame arithmetic に silent に入る事故を防ぐ | PR-LR |
| 391 | `template/scripts/timeline.py` の `ms_to_playback_frame()` が `cut_segments` の順序・範囲 (`originalStartMs <= originalEndMs`、`playbackStart <= playbackEnd`、playback frame は 0 以上) を検査し、zero-duration segment は保持することを lint (`test_ms_to_playback_frame_rejects_invalid_cut_segment_ranges`、Codex 推奨 PR-LS)。逆順 mapping や負 frame が cut-aware playback へ silent に入る drift を防ぐ | PR-LS |
| 392 | `template/scripts/timeline.py` の `ms_to_playback_frame()` が複数 `cut_segments` 間の monotonicity (`next.originalStartMs >= previous.originalEndMs`、`next.playbackStart >= previous.playbackEnd`) を検査し、adjacent boundary は保持することを lint (`test_ms_to_playback_frame_rejects_non_monotonic_cut_segments`、Codex 推奨 PR-LT)。original overlap / playback overlap が first-match lookup に silent に入る drift を防ぐ | PR-LT |
| 393 | `template/scripts/timeline.py` の `validate_vad_schema()` が `speech_segments` 間の monotonicity (`next.start >= previous.end`) を検査し、gap / adjacent segment は保持することを lint (`test_vad_schema_rejects_non_monotonic_speech_segments`、Codex 推奨 PR-LU)。overlap / out-of-order VAD が `build_cut_segments_from_vad()` に入る前に fail-fast する境界を保護 | PR-LU |
| 394 | `template/scripts/timeline.py` の `validate_transcript_segments()` が list-level timing monotonicity (`next.start >= previous.end`) を検査し、`require_timing=False` の timing 欠落 segment は維持することを lint (`test_transcript_segments_reject_non_monotonic_timing`、Codex 推奨 PR-LV)。overlap / out-of-order transcript が slide / telop / narration timing へ silent に入る drift を防ぐ | PR-LV |
| 395 | `template/scripts/timeline.py` の `build_cut_segments_from_vad()` が direct call でも `validate_vad_schema()` を通し、raw VAD の schema / range / monotonicity 破損を `VadSchemaError` で fail-fast することを lint (`test_build_cut_segments_from_vad_validates_input_schema_directly`、Codex 推奨 PR-LW)。wrapper 経由以外の呼び出しで VAD 破損が `KeyError` / silent mapping に流れる drift を防ぐ | PR-LW |
| 396 | `template/scripts/timeline.py` の `read_video_config_fps(default=...)` が fallback default に positive int のみを受け付け、bool / float / str / zero / negative を reject することを lint (`test_read_video_config_fps_rejects_invalid_default`、Codex 推奨 PR-LX)。`videoConfig.ts` 不在 / malformed / `FPS=0` fallback から invalid fps が後段 timing 計算へ silent に流れる drift を防ぐ | PR-LX |
| 397 | `template/scripts/timeline.py` の `validate_transcript_segment()` / `validate_transcript_segments()` が `require_timing` に explicit bool のみを受け付け、`0` / `1` / `"false"` など truthy/falsy 値を reject することを lint (`test_transcript_validation_rejects_non_bool_require_timing`、Codex 推奨 PR-LY)。flag typo が optional/strict timing validation mode を silent に切り替える drift を防ぐ | PR-LY |
| 398 | `template/scripts/timeline.py` の `load_cut_segments(fail_fast=...)` が explicit bool のみを受け付け、`0` / `1` / `"false"` など truthy/falsy 値を reject することを lint (`test_load_cut_segments_fail_fast`、Codex 推奨 PR-LZ)。VAD 破損時の soft fallback / fail-fast strategy が flag typo で silent に切り替わる drift を防ぐ | PR-LZ |
| 399 | `template/scripts/timeline.py` の `_validate_ms()` が `ms` と `cut_segments[*].originalStartMs/originalEndMs` に non-negative finite int|float のみを受け付け、負値を reject することを lint (`test_ms_to_playback_frame_rejects_invalid_ms_values` / `test_ms_to_playback_frame_rejects_invalid_cut_segment_value_types`、Codex 推奨 PR-MA)。負の millisecond が frame 計算や cut-aware range に silent に入る drift を防ぐ | PR-MA |
| 400 | `template/scripts/timeline.py` の `validate_vad_schema()` / `validate_transcript_segment()` が `start` / `end` timing に non-negative finite int|float のみを受け付け、負値を reject することを lint (`test_timeline_validation_rejects_negative_timing_values`、Codex 推奨 PR-MB)。負 timestamp が VAD / transcript schema validation を通過して後段 mapping に入る drift を防ぐ | PR-MB |
| 401 | `template/scripts/timeline.py` の `validate_transcript_segment(idx=...)` が label index に `-1` または非負 int のみを受け付け、bool / float / str / container / `-2` 以下を reject することを lint (`test_transcript_validation_rejects_invalid_idx_values`、Codex 推奨 PR-MC)。public helper の caller typo が `segment[True]` や比較 TypeError に流れる drift を防ぐ | PR-MC |
| 402 | `template/scripts/build_slide_data.py` の `build_cut_segments_from_vad()` wrapper が VAD 不在 `None` のみを empty cut 扱いし、空 dict/list など falsy な破損 VAD を `validate_vad_schema()` 経由で reject することを lint (`test_build_scripts_wiring`、Codex 推奨 PR-MD)。`vad_result.json` が壊れているのに slide generation が cut 無しへ silent fallback する drift を防ぐ | PR-MD |
| 403 | `template/scripts/voicevox_narration.py` の `collect_chunks()` が transcript root に dict を要求し、list / str など root 破損を `TranscriptSegmentError` で reject することを lint (`test_voicevox_collect_chunks_validation`、Codex 推奨 PR-ME)。壊れた `transcript_fixed.json` root が empty chunks として扱われ、narration 生成が「chunk なし」へ silent fallback する drift を防ぐ | PR-ME |
| 404 | `template/scripts/generate_slide_plan.py` が `transcript_fixed.json` root に dict を要求し、list / str など root 破損を `transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_generate_slide_plan_rejects_non_dict_transcript_root`、Codex 推奨 PR-MF)。壊れた transcript root が prompt construction の `.get()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MF |
| 405 | `template/scripts/generate_slide_plan.py` が prompt preview 用 `transcript.words` / `transcript.segments` に list[dict] shape を要求し、非 list / 非 dict entry を `transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_generate_slide_plan_rejects_malformed_prompt_lists`、Codex 推奨 PR-MG)。`words` / `segments` 破損が prompt construction の `.get()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MG |
| 406 | `template/scripts/generate_slide_plan.py` が `project-config.json` root に dict を要求し、list / str など root 破損を `config_invalid` JSON tail + exit 3 で reject することを lint (`test_generate_slide_plan_rejects_non_dict_project_config_root`、Codex 推奨 PR-MH)。壊れた project config root が `config.get()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MH |
| 407 | `template/scripts/build_slide_data.py` が `transcript_fixed.json` root に dict を要求し、list / str など root 破損を `build_slide_transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_build_slide_data_rejects_non_dict_transcript_root`、Codex 推奨 PR-MI)。壊れた transcript root が `transcript.get()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MI |
| 408 | `template/scripts/build_slide_data.py` が `project-config.json` root に dict を要求し、list / str など root 破損を `config_invalid` JSON tail + exit 3 で reject することを lint (`test_build_slide_data_rejects_non_dict_project_config_root`、Codex 推奨 PR-MJ)。壊れた project config root が `config.get()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MJ |
| 409 | `template/scripts/build_telop_data.py` が `transcript_fixed.json` root に dict を要求し、list / str など root 破損を `build_telop_transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_build_telop_data_rejects_non_dict_transcript_root`、Codex 推奨 PR-MK)。壊れた transcript root が `transcript["words"]` / `transcript["segments"]` TypeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MK |
| 410 | `template/scripts/build_telop_data.py` が optional `typo_dict.json` root に dict を要求し、list / str など root 破損を `typo_dict_invalid` JSON tail + exit 3 で reject することを lint (`test_build_telop_data_rejects_non_dict_typo_dict_root`、Codex 推奨 PR-ML)。壊れた typo_dict root が `typo_dict.get("preserve")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-ML |
| 411 | `template/scripts/compare_telop_split.py` が `transcript_fixed.json` root に dict を要求し、list / str など root 破損を `transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_non_dict_transcript_root`、Codex 推奨 PR-MM)。壊れた transcript root が `transcript.get("words")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MM |
| 412 | `template/scripts/compare_telop_split.py` が optional `typo_dict.json` root に dict を要求し、list / str など root 破損を `typo_dict_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_non_dict_typo_dict_root`、Codex 推奨 PR-MN)。壊れた typo_dict root が `typo_dict.get("preserve")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MN |
| 413 | `template/scripts/preflight_video.py` が既存 `--write-config` JSON root に dict を要求し、list / str など root 破損を `write_config_parse_error` JSON tail + exit 3 で reject することを lint (`test_preflight_video_write_config_rejects_non_dict_root`、Codex 推奨 PR-MO)。壊れた project config root が `cfg.setdefault("source", {})` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MO |
| 414 | `template/scripts/build_slide_data.py` の `validate_slide_plan()` が plan 使用時の `transcript.words` に list[dict] shape を要求し、非 dict entry を `build_slide_plan_invalid` JSON tail + exit 2 で reject することを lint (`test_build_slide_data_plan_rejects_malformed_words`、Codex 推奨 PR-MP)。壊れた words entry が `words[sw].get("start")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MP |
| 415 | `template/scripts/build_slide_data.py` の `validate_slide_plan()` が plan 使用時の `transcript.words[*].start/end` に finite numeric timing を要求し、str / bool / negative など timing 破損を `build_slide_plan_invalid` JSON tail + exit 2 で reject することを lint (`test_build_slide_data_plan_rejects_invalid_word_timing`、Codex 推奨 PR-MQ)。壊れた word timing が `ms_to_playback_frame()` ValueError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MQ |
| 416 | `template/scripts/build_telop_data.py` が optional `typo_dict.preserve` に `list[str]` を要求し、str / 非 str entry を `typo_dict_invalid` JSON tail + exit 3 で reject することを lint (`test_build_telop_data_rejects_malformed_typo_preserve`、Codex 推奨 PR-MR)。壊れた preserve が 1 文字単位 iteration や `text.find()` TypeError に流れ、telop 改行保護が silent に崩れる drift を防ぐ | PR-MR |
| 417 | `template/scripts/compare_telop_split.py` が optional `typo_dict.preserve` に `list[str]` を要求し、str / 非 str entry を `typo_dict_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_malformed_typo_preserve`、Codex 推奨 PR-MS)。壊れた preserve が KPI 計算の linebreak_inside_preserve_count で 1 文字単位 iteration や `find()` TypeError に流れる drift を防ぐ | PR-MS |
| 418 | `template/scripts/compare_telop_split.py` が `transcript.words` に list[dict] shape を要求し、非 list / 非 dict entry を `transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_malformed_transcript_words`、Codex 推奨 PR-MT)。壊れた words entry が KPI 計算の `.get("text")` AttributeError に流れ、`kpi_calc_error` へ誤分類される drift を防ぐ | PR-MT |
| 419 | `template/scripts/compare_telop_split.py` が `transcript.words[*].text` に str を要求し、数値 / null など text 破損を `transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_malformed_transcript_word_text`、Codex 推奨 PR-MU)。壊れた word text が KPI 計算の `"".join(...)` TypeError に流れ、`kpi_calc_error` へ誤分類される drift を防ぐ | PR-MU |
| 420 | `template/scripts/build_telop_data.py` が `transcript.segments` に list shape を要求し、missing / 非 list を `build_telop_transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_build_telop_data_rejects_malformed_transcript_segments`、Codex 推奨 PR-MV)。壊れた transcript segments が `transcript["segments"]` KeyError や後段 iteration に流れ、observability tail なしで落ちる drift を防ぐ | PR-MV |
| 421 | `template/scripts/build_telop_data.py` が `transcript.words` に list[dict] shape を要求し、非 list / 非 dict entry を `build_telop_transcript_invalid` JSON tail + exit 3 で reject することを lint (`test_build_telop_data_rejects_malformed_transcript_words`、Codex 推奨 PR-MW)。壊れた words entry が transcript schema drift として残り、telop input contract が segments だけに偏る drift を防ぐ | PR-MW |
| 422 | `template/scripts/preflight_video.py` が ffprobe JSON root / `streams` / `streams[*]` に dict / list / dict shape を要求し、破損を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_streams`、Codex 推奨 PR-MX)。壊れた ffprobe output が `probe.get()` / `s.get("codec_type")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MX |
| 423 | `template/scripts/preflight_video.py` が ffprobe `format` root に dict shape を要求し、非 dict を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format`、Codex 推奨 PR-MY)。壊れた format metadata が `fmt_meta.get("duration")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MY |
| 424 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG JSON root / `streams` / `streams[0]` / width-height shape を fail-loud に検証し、CLI が validation error を `probe_failed` summary + `env_error` JSON tail + exit 3 に載せることを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json` / `test_visual_smoke_probe_dim_error_emits_tail`、Codex 推奨 PR-MZ)。壊れた ffprobe output が `info["streams"][0]` KeyError / TypeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-MZ |
| 425 | `template/scripts/preflight_video.py` が ffprobe video stream の `width` / `height` に正の int shape を要求し、欠落 / bool / 非数値 / 0 を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_video_dimensions`、Codex 推奨 PR-NA)。壊れた video dimension が `int(video.get(...))` ValueError / TypeError やゼロ寸法 source に流れ、observability tail なしで落ちる drift を防ぐ | PR-NA |
| 426 | `template/scripts/preflight_video.py` が ffprobe `streams[*].side_data_list` に list[dict] shape を要求し、非 list / 非 dict entry を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_side_data_list`、Codex 推奨 PR-NB)。壊れた side data が `parse_side_data()` の `entry.get("side_data_type")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-NB |
| 427 | `template/scripts/preflight_video.py` が ffprobe Display Matrix `rotation` に int shape を要求し、bool / 非整数文字列を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_side_data_rotation`、Codex 推奨 PR-NC)。壊れた rotation 値が `parse_side_data()` の `int(rotation)` ValueError / bool混入に流れ、observability tail なしで落ちる drift を防ぐ | PR-NC |
| 428 | `template/scripts/preflight_video.py` が ffprobe `streams[*].tags` root に dict shape を要求し、非 dict を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_tags`、Codex 推奨 PR-ND)。壊れた tags が `detect_rotation()` の `tags.get("rotate")` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-ND |
| 429 | `template/scripts/preflight_video.py` が ffprobe `format.duration` に有限数 shape を要求し、bool / 非数値 / nan を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration`、Codex 推奨 PR-NE)。壊れた duration が `float(fmt_meta.get("duration"))` ValueError や non-finite frame計算に流れ、observability tail なしで落ちる drift を防ぐ | PR-NE |
| 430 | `template/scripts/preflight_video.py` が ffprobe video stream の `r_frame_rate` / `avg_frame_rate` に finite `num/den` string shape を要求し、非 string / slash なし / nan を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-NF)。壊れた frame rate が `detect_vfr()` / `eval_fps()` の `.split("/")` AttributeError や non-finite render fps計算に流れ、observability tail なしで落ちる drift を防ぐ | PR-NF |
| 431 | `template/scripts/preflight_video.py` が ffprobe video stream の `color_transfer` / `color_primaries` / `color_space` に optional str shape を要求し、list / int / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_color_metadata`、Codex 推奨 PR-NG)。壊れた color metadata が `detect_hdr()` の `.lower()` / `.startswith()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-NG |
| 432 | `template/scripts/preflight_video.py` が ffprobe video stream の `pix_fmt` に optional str shape を要求し、list / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_pix_fmt`、Codex 推奨 PR-NH)。壊れた pix_fmt が `is_10bit_codec()` の `.lower()` AttributeError に流れ、observability tail なしで落ちる drift を防ぐ | PR-NH |
| 433 | `template/scripts/preflight_video.py` が ffprobe video stream の `tags.rotate` / root `rotation` に int shape を要求し、bool / 非整数文字列 / list を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_rotation_fields`、Codex 推奨 PR-NI)。壊れた rotation field が `detect_rotation()` で silent default / bool由来の非 canonical rotation に流れ、preflight risk 判定が drift することを防ぐ | PR-NI |
| 434 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG の width / height に正の int shape を要求し、0 / 負数を ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-NJ)。壊れた PNG dimension が impossible actual size として visual smoke mismatch / summary に流れ、dimension regression 判定が drift することを防ぐ | PR-NJ |
| 435 | `template/scripts/preflight_video.py` が ffprobe video stream の `sample_aspect_ratio` / `display_aspect_ratio` に optional str shape を要求し、list / float を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_aspect_ratio_metadata`、Codex 推奨 PR-NK)。壊れた aspect metadata が `infer_format()` の SAR 判定や source schema に流れ、unknown-aspect / non-square-sar risk 判定が drift することを防ぐ | PR-NK |
| 436 | `template/scripts/preflight_video.py` が ffprobe video stream の `field_order` に optional str shape を要求し、list / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_field_order`、Codex 推奨 PR-NL)。壊れた field_order が interlace risk 判定や source.codec metadata に流れ、risk 判定が drift することを防ぐ | PR-NL |
| 437 | `template/scripts/preflight_video.py` が ffprobe video stream の `codec_name` / `profile` / `color_range` に optional str shape を要求し、list / int / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_string_metadata`、Codex 推奨 PR-NM)。壊れた codec / color string metadata が source schema に流れ、downstream の source metadata contract が drift することを防ぐ | PR-NM |
| 438 | `template/scripts/preflight_video.py` が ffprobe `streams[*].codec_type` に optional str shape を要求し、list / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_codec_type`、Codex 推奨 PR-NN)。壊れた codec_type が video/audio/subtitle/data stream 分類や no-video 判定に流れ、stream count / source classification が drift することを防ぐ | PR-NN |
| 439 | `template/scripts/preflight_video.py` が ffprobe `streams[*].side_data_list[*].side_data_type` に optional str shape を要求し、list / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_side_data_type`、Codex 推奨 PR-NO)。壊れた side_data_type が Display Matrix 判定や rotation extraction に流れ、rotation/source side-data classification が drift することを防ぐ | PR-NO |
| 440 | `template/scripts/preflight_video.py` が ffprobe DOVI side-data の `dv_profile` / `dv_level` / `rpu_present_flag` / `el_present_flag` / `bl_present_flag` に optional int shape を要求し、非整数文字列 / bool を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_dovi_fields`、Codex 推奨 PR-NP)。壊れた DOVI metadata が `source.color.dovi` と HDR risk 判定に流れ、source color contract が drift することを防ぐ | PR-NP |
| 441 | `template/scripts/preflight_video.py` が ffprobe `format.duration` に非負の finite number shape を要求し、負数を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration`、Codex 推奨 PR-NQ)。壊れた duration が `source.duration_sec` / `duration_frames` に負値として流れ、timeline duration metadata が drift することを防ぐ | PR-NQ |
| 442 | `template/scripts/preflight_video.py` が ffprobe `streams` の `null` を空配列に丸めず list shape として reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_streams`、Codex 推奨 PR-NR)。壊れた streams root が `no_video_stream` に誤分類され、ffprobe validation failure の JSON tail から外れる drift を防ぐ | PR-NR |
| 443 | `template/scripts/preflight_video.py` が ffprobe `format` の `null` を空 dict に丸めず dict shape として reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format`、Codex 推奨 PR-NS)。壊れた format root が downstream write/config failure に流れ、ffprobe validation failure の JSON tail から外れる drift を防ぐ | PR-NS |
| 444 | `template/scripts/preflight_video.py` が ffprobe `streams[*].side_data_list` の `null` を missing扱いにせず list shape として reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_side_data_list`、Codex 推奨 PR-NT)。壊れた side-data root が downstream write/config failure に流れ、ffprobe validation failure の JSON tail から外れる drift を防ぐ | PR-NT |
| 445 | `template/scripts/preflight_video.py` が ffprobe `streams[*].tags` の `null` を missing扱いにせず dict shape として reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_tags`、Codex 推奨 PR-NU)。壊れた tags root が rotation/source metadata 判定の silent default に流れ、ffprobe validation failure の JSON tail から外れる drift を防ぐ | PR-NU |
| 446 | `template/scripts/preflight_video.py` が ffprobe video stream の `r_frame_rate` / `avg_frame_rate` で明示された負 numerator / 0 denominator を `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-NV)。壊れた frame rate が `render_fps` / `duration_frames` に 0 または負値として流れ、timeline fps metadata が drift することを防ぐ | PR-NV |
| 447 | `template/scripts/preflight_video.py` が ffprobe `streams[*].codec_type` の `null` を missing扱いにせず str shape として reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_codec_type`、Codex 推奨 PR-NW)。壊れた codec_type が `no_video_stream` に誤分類され、ffprobe validation failure の JSON tail から外れる drift を防ぐ | PR-NW |
| 448 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG の `width` / `height` で非整数 float を `int()` で丸めず ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-NX)。壊れた PNG dimension が truncation で positive int に見せかけられ、visual smoke の実寸判定が drift することを防ぐ | PR-NX |
| 449 | `template/scripts/visual_smoke.py` が `--frames` の underscore 区切り数値 (`3_0` など) を Python `int()` の緩い解釈に任せず `usage_error_frames_invalid` JSON tail + exit 4 で reject することを lint (`test_visual_smoke_cli_rejects_underscore_frame_value`、Codex 推奨 PR-NY)。壊れた frame CLI 入力が別 frame として silent normalize され、visual smoke の検証対象 frame が drift することを防ぐ | PR-NY |
| 450 | `template/scripts/generate_slide_plan.py` の `_resolve_int()` が `SUPERMOVIE_MAX_TOKENS` などの env integer で underscore 区切り数値 (`4_096` など) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_generate_slide_plan_resolve_int_rejects_underscore_env_value`、Codex 推奨 PR-NZ)。壊れた cost/input cap env が silent normalize され、LLM request guard の上限値が drift することを防ぐ | PR-NZ |
| 451 | `template/scripts/generate_slide_plan.py` が `SUPERMOVIE_MAX_INPUT_SEGMENTS` の env integer で underscore 区切り数値 (`1_0` など) を Python `int()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_max_input_segments_env_rejects_underscore_value`、Codex 推奨 PR-OA)。壊れた segment cap env が silent normalize され、slide plan prompt の segment 上限が drift することを防ぐ | PR-OA |
| 452 | `template/scripts/generate_slide_plan.py` が `--max-input-segments` の CLI integer で underscore 区切り数値 (`1_0` など) を argparse `type=int` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_max_input_segments_cli_rejects_underscore_value`、Codex 推奨 PR-OB)。壊れた segment cap CLI 入力が silent normalize され、slide plan prompt の segment 上限が drift することを防ぐ | PR-OB |
| 453 | `template/scripts/generate_slide_plan.py` が `--max-input-words` の CLI integer で underscore 区切り数値 (`1_000` など) を argparse `type=int` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_max_input_words_cli_rejects_underscore_value`、Codex 推奨 PR-OC)。壊れた word cap CLI 入力が silent normalize され、slide plan prompt の word 上限が drift することを防ぐ | PR-OC |
| 454 | `template/scripts/generate_slide_plan.py` が `--max-tokens` の CLI integer で underscore 区切り数値 (`4_096` など) を argparse `type=int` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_max_tokens_cli_rejects_underscore_value`、Codex 推奨 PR-OD)。壊れた max_tokens CLI 入力が silent normalize され、LLM request guard の出力 token 上限が drift することを防ぐ | PR-OD |
| 455 | `template/scripts/generate_slide_plan.py` が `--rate-input` の CLI decimal で underscore 区切り数値 (`1_000.5` など) を argparse `type=float` / Python `float()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_rate_input_cli_rejects_underscore_value`、Codex 推奨 PR-OE)。壊れた input rate CLI 入力が silent normalize され、dry-run / cost guard estimate が drift することを防ぐ | PR-OE |
| 456 | `template/scripts/generate_slide_plan.py` が `--rate-output` の CLI decimal で underscore 区切り数値 (`1_000.5` など) を argparse `type=float` / Python `float()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_rate_output_cli_rejects_underscore_value`、Codex 推奨 PR-OF)。壊れた output rate CLI 入力が silent normalize され、dry-run / cost guard estimate が drift することを防ぐ | PR-OF |
| 457 | `template/scripts/generate_slide_plan.py` が `--cost-abort-at` の CLI decimal で underscore 区切り数値 (`1_000.5` など) を argparse `type=float` / Python `float()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_cost_abort_cli_rejects_underscore_value`、Codex 推奨 PR-OG)。壊れた cost abort threshold CLI 入力が silent normalize され、pre-API cost abort guard の閾値が drift することを防ぐ | PR-OG |
| 458 | `template/scripts/voicevox_narration.py` が `--speaker` の CLI integer で underscore 区切り数値 (`1_000` など) を argparse `type=int` の緩い解釈に任せず argparse parse error + exit 2 で reject することを lint (`test_voicevox_speaker_cli_rejects_underscore_value`、Codex 推奨 PR-OH)。壊れた VOICEVOX speaker id CLI 入力が silent normalize され、意図しない speaker id で synthesis / list-speakers 前段へ進むことを防ぐ | PR-OH |
| 459 | `template/scripts/voicevox_narration.py` が `--fps` の CLI integer で underscore 区切り数値 (`3_0` など) を argparse `type=int` の緩い解釈に任せず argparse parse error + exit 2 で reject することを lint (`test_voicevox_fps_cli_rejects_underscore_value`、Codex 推奨 PR-OI)。壊れた narration FPS CLI 入力が silent normalize され、frame conversion / narrationData.ts timing が drift することを防ぐ | PR-OI |
| 460 | `template/scripts/preflight_video.py` が ffprobe `format.duration` の underscore decimal (`1_000.5` など) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration`、Codex 推奨 PR-OJ)。壊れた source duration が silent normalize され、`source.duration_sec` / `duration_frames` が drift することを防ぐ | PR-OJ |
| 461 | `template/scripts/preflight_video.py` が ffprobe video stream の `r_frame_rate` / `avg_frame_rate` で underscore decimal component (`3_0/1` など) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-OK)。壊れた frame rate が silent normalize され、`render_fps` / `duration_frames` が drift することを防ぐ | PR-OK |
| 462 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG の `width` / `height` で underscore integer string (`1_000` など) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-OL)。壊れた PNG dimension が silent normalize され、visual smoke の実寸判定が drift することを防ぐ | PR-OL |
| 463 | `template/scripts/generate_slide_plan.py` が `--rate-input` の leading plus decimal (`+1.0`) を Python `float()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_rate_input_cli_rejects_plus_value`、Codex 推奨 PR-OM)。壊れた cost rate CLI token が canonical form へ silent normalize され、dry-run / cost guard estimate の入力契約が drift することを防ぐ | PR-OM |
| 464 | `template/scripts/voicevox_narration.py` の `parse_cli_int_token()` が signed token (`+3` / `-1`) と Unicode digit (`００3` / `٣`) を Python `int()` の緩い解釈に任せず argparse `invalid integer token` で reject することを lint (`test_voicevox_cli_int_token_rejects_signed_and_unicode_digits`、Codex 推奨 PR-ON)。壊れた speaker / fps CLI token が canonical ASCII decimal へ silent normalize され、VOICEVOX speaker id や narration frame conversion が drift することを防ぐ | PR-ON |
| 465 | `template/scripts/preflight_video.py` の integer validators が ffprobe dimension / rotation string の Unicode digit (`３２０` / `９０`) を Python `int()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_video_dimensions` / `test_preflight_video_rejects_malformed_ffprobe_rotation_fields`、Codex 推奨 PR-OO)。壊れた ffprobe integer token が canonical ASCII integer へ silent normalize され、source dimension / rotation metadata が drift することを防ぐ | PR-OO |
| 466 | `template/scripts/generate_slide_plan.py` の `_resolve_int()` が env / CLI integer の Unicode digit (`４０９６`) を Python `int()` の緩い解釈に任せず `invalid token` で reject することを lint (`test_generate_slide_plan_resolve_int_rejects_unicode_digit_tokens`、Codex 推奨 PR-OP)。壊れた LLM cost / prompt cap integer token が canonical ASCII integer へ silent normalize され、request guard 上限が drift することを防ぐ | PR-OP |
| 467 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG dimension string の Unicode digit (`３２０` / `２４０`) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-OQ)。壊れた PNG dimension が canonical ASCII integer へ silent normalize され、visual smoke の実寸判定が drift することを防ぐ | PR-OQ |
| 468 | `template/scripts/visual_smoke.py` の `--frames` parser が Unicode digit (`３０`) を regex `\d` / Python `int()` の緩い解釈に任せず `usage_error_frames_invalid` JSON tail + exit 4 で reject することを lint (`test_visual_smoke_cli_rejects_unicode_frame_value`、Codex 推奨 PR-OR)。壊れた frame CLI 入力が canonical ASCII integer へ silent normalize され、visual smoke の検証対象 frame が drift することを防ぐ | PR-OR |
| 469 | `template/scripts/compare_telop_split.py` の `parse_telop_data_ts()` が telopData.ts の `id` / `startFrame` / `endFrame` に含まれる Unicode digit (`１` / `０` / `６０`) を regex `\d` / Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_compare_telop_split_rejects_unicode_telop_numeric_tokens`、Codex 推奨 PR-OS)。壊れた telop frame token が canonical ASCII integer へ silent normalize され、KPI comparison の frame metrics が drift することを防ぐ | PR-OS |
| 470 | `template/scripts/compare_telop_split.py` の telopData.ts numeric token guard が fullwidth / Arabic-Indic / circled / underscore token を parser helper と CLI tail の両方で `telop_ts_invalid` として fail-loud することを lint (`test_compare_telop_split_rejects_unicode_telop_numeric_tokens` 拡張 + `test_compare_telop_split_invalid_telop_numeric_token_emits_tail`、Codex consult 推奨 PR-OT)。壊れた telop numeric token が parse skip や canonical ASCII integer への silent normalize で KPI gate を通過することを防ぐ | PR-OT |
| 471 | `template/scripts/timeline.py` の `read_video_config_fps()` が videoConfig.ts の `export const FPS = ...;` で Unicode digit (`６０` / `٦٠`) を regex `\d` / Python `int()` の緩い解釈に任せず default fallback に倒すことを lint (`test_read_video_config_fps_rejects_unicode_digit_token`、Codex 推奨 PR-OU)。壊れた FPS token が canonical ASCII integer へ silent normalize され、slide / telop / narration の frame conversion が drift することを防ぐ | PR-OU |
| 472 | `template/scripts/generate_slide_plan.py` の `_resolve_decimal()` が cost rate / abort decimal token の Unicode digit (`１.０` / `١.٠`) を Python `float()` の緩い解釈に任せず `cost_guard_arg_invalid` JSON tail + exit 4 で reject することを lint (`test_generate_slide_plan_decimal_tokens_reject_unicode_digits`、Codex 推奨 PR-OV)。壊れた cost rate token が canonical ASCII decimal へ silent normalize され、dry-run / cost guard estimate が drift することを防ぐ | PR-OV |
| 473 | `template/scripts/preflight_video.py` の ffprobe `format.duration` と `r_frame_rate` / `avg_frame_rate` component が Unicode digit (`１０００.５` / `３０/1`) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration` / `test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-OW)。壊れた source duration / frame-rate token が canonical ASCII decimal へ silent normalize され、`source.duration_sec` / `render_fps` / `duration_frames` が drift することを防ぐ | PR-OW |
| 474 | `template/scripts/preflight_video.py` の ffprobe `format.duration` と `r_frame_rate` / `avg_frame_rate` component が leading plus decimal (`+1000.5` / `+30/1` / `30/+1`) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration` / `test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-OX)。壊れた source duration / frame-rate token が canonical form へ silent normalize され、`source.duration_sec` / `render_fps` / `duration_frames` が drift することを防ぐ | PR-OX |
| 475 | `_observability.py` の `build_cost_payload(rate_source=...)` が `env:` 後の env name に whitespace / control / non-ASCII を含む token を ValueError で reject することを lint (`test_observability_build_cost_payload_rate_source_contract`、Codex 推奨 PR-OY)。壊れた rate source token が canonical env name として payload に silent 通過し、cost telemetry の rate provenance が drift することを防ぐ | PR-OY |
| 476 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG dimension string の leading plus integer (`+320` / `+240`) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-OZ)。壊れた PNG dimension token が canonical form へ silent normalize され、visual smoke の実寸判定が drift することを防ぐ | PR-OZ |
| 477 | `template/scripts/visual_smoke.py` の `probe_dim()` が ffprobe PNG dimension string の leading/trailing whitespace token (` 320` / `240 `) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_visual_smoke_probe_dim_rejects_malformed_ffprobe_json`、Codex 推奨 PR-PA)。壊れた PNG dimension token が canonical form へ silent normalize され、visual smoke の実寸判定が drift することを防ぐ | PR-PA |
| 478 | `template/scripts/generate_slide_plan.py` の `_resolve_int()` が env / CLI integer の leading/trailing whitespace token (` 4096` / `4096 `) を Python `int()` の緩い解釈に任せず ValueError で reject することを lint (`test_generate_slide_plan_resolve_int_rejects_whitespace_tokens`、Codex 推奨 PR-PB)。壊れた cost/input cap token が canonical form へ silent normalize され、LLM request guard の上限値が drift することを防ぐ | PR-PB |
| 479 | `template/scripts/generate_slide_plan.py` の `_resolve_decimal()` が env / CLI decimal の leading/trailing whitespace token (` 1.0` / `1.0 `) を Python `float()` の緩い解釈に任せず ValueError で reject することを lint (`test_generate_slide_plan_decimal_tokens_reject_whitespace`、Codex 推奨 PR-PC)。壊れた cost rate token が canonical form へ silent normalize され、dry-run / cost guard estimate が drift することを防ぐ | PR-PC |
| 480 | `template/scripts/preflight_video.py` の ffprobe video `width` / `height` string が leading/trailing whitespace token (` 320` / `240 `) を Python `int()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_video_dimensions`、Codex 推奨 PR-PD)。壊れた source dimension token が canonical form へ silent normalize され、`source.width` / `source.height` metadata が drift することを防ぐ | PR-PD |
| 481 | `template/scripts/preflight_video.py` の ffprobe rotation string (`tags.rotate` / `rotation` / Display Matrix `rotation`) が leading/trailing whitespace token (` 90` / `90 `) を Python `int()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_rotation_fields` / `test_preflight_video_rejects_malformed_ffprobe_side_data_rotation`、Codex 推奨 PR-PE)。壊れた rotation token が canonical form へ silent normalize され、`source.rotation.*` metadata が drift することを防ぐ | PR-PE |
| 482 | `template/scripts/preflight_video.py` の ffprobe `format.duration` decimal string が leading/trailing whitespace token (` 1000.5` / `1000.5 `) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_format_duration`、Codex 推奨 PR-PF)。壊れた source duration token が canonical form へ silent normalize され、`source.duration_sec` / `duration_frames` metadata が drift することを防ぐ | PR-PF |
| 483 | `template/scripts/preflight_video.py` の ffprobe `r_frame_rate` / `avg_frame_rate` string が leading/trailing whitespace token (` 30/1` / `30/1 `) と component 周辺 whitespace token (`30 /1` / `30/ 1`) を Python `float()` の緩い解釈に任せず `ffprobe_failed` JSON tail + exit 3 で reject することを lint (`test_preflight_video_rejects_malformed_ffprobe_frame_rates`、Codex 推奨 PR-PG)。壊れた source frame-rate token が canonical form へ silent normalize され、`fps.*` / `render_fps` / `duration_frames` metadata が drift することを防ぐ | PR-PG |
| 484 | `template/scripts/voicevox_narration.py` の `parse_cli_int_token()` が `--speaker` / `--fps` integer string の leading/trailing whitespace token (` 12` / `12 `) を Python `int()` の緩い解釈に任せず argparse `invalid integer token` で reject することを lint (`test_voicevox_cli_int_token_rejects_signed_and_unicode_digits`、Codex 推奨 PR-PH)。壊れた narration CLI token が canonical ASCII integer へ silent normalize され、VOICEVOX speaker id や narration frame conversion が drift することを防ぐ | PR-PH |
| 485 | `template/scripts/visual_smoke.py` の `--frames` comma-list integer token が component whitespace (`30, 90`) を Python `int()` の緩い解釈に任せず `usage_error_frames_invalid` JSON tail + exit 4 で reject することを lint (`test_visual_smoke_cli_rejects_whitespace_frame_token`、Codex 推奨 PR-PI)。壊れた visual smoke frame CLI token が canonical ASCII integer へ silent normalize され、検証対象 frame が drift することを防ぐ | PR-PI |
| 486 | `template/scripts/visual_smoke.py` の `--frames` comma-list integer token が empty comma component (`30,,90`) を parser で drop せず `usage_error_frames_invalid` JSON tail + exit 4 で reject し、完全空 token (`--frames ""`) は既存 `usage_error_frames_empty` path を保つことを lint (`test_visual_smoke_cli_rejects_empty_frame_token` / `test_visual_smoke_cli_preserves_empty_frames_status`、Codex review P2 PR-PJ)。壊れた visual smoke frame CLI token が検証対象 frame set を silent shrink して通過することを防ぐ | PR-PJ |
| 487 | `template/scripts/visual_smoke.py` の `--formats` comma-list enum token が component whitespace (`youtube, short`) や empty comma component (`youtube,,short`) を parser で strip/drop せず `usage_error_unknown_format` JSON tail + exit 4 で reject し、完全空 token (`--formats ""`) は既存 `usage_error_formats_empty` path を保つことを lint (`test_visual_smoke_cli_rejects_whitespace_format_token` / `test_visual_smoke_cli_rejects_empty_format_token` / `test_visual_smoke_cli_preserves_empty_formats_status`、Codex 推奨 PR-PK)。壊れた visual smoke format CLI token が検証対象 format set を silent normalize / shrink して通過することを防ぐ | PR-PK |
| 488 | `template/scripts/preflight_video.py` の `--allow-risk` comma-list enum token が component / whitespace-only token (`unknown-aspect, multiple-or-missing-audio` / `"   "`) や empty comma component / comma-only token (`unknown-aspect,,multiple-or-missing-audio` / `","`) を parser で strip/drop せず `usage_error_allow_risk_invalid` JSON tail + exit 4 で reject し、完全空 token (`--allow-risk ""`) は既存 `risks_not_allowed` path を保つことを lint (`test_preflight_video_rejects_whitespace_allow_risk_token` / `test_preflight_video_rejects_empty_allow_risk_token` / `test_preflight_video_preserves_empty_allow_risk_status`、Codex 推奨 PR-PL)。壊れた preflight allowlist token が risk gate を silent bypass / shrink して通過することを防ぐ | PR-PL |
| 489 | `template/scripts/compare_telop_split.py` の `parse_telop_data_ts()` が telopData.ts `text:` の malformed / non-string JSON token (`hello` / `'hello'` / `"hello` / `123`) を strip fallback で silent 通過させず `telop_ts_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_malformed_telop_text_json` / `test_compare_telop_split_invalid_telop_text_token_emits_tail`、Codex 推奨 PR-PM)。壊れた telop text token が KPI 比較の文字列・境界計算へ混入することを防ぐ | PR-PM |
| 490 | `template/scripts/compare_telop_split.py` の `parse_telop_data_ts()` が telopData.ts `style:` を `TelopSegment.style` union 外 (`danger` / `normal2` / `_normal` 等) で `telop_ts_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_malformed_telop_style_token` / `test_compare_telop_split_invalid_telop_style_token_emits_tail`、Codex 推奨 PR-PN)。union 外の style が KPI 比較を通過してレンダラーの型契約を破ることを防ぐ | PR-PN |
| 491 | `template/scripts/compare_telop_split.py` の `parse_telop_data_ts()` が telopData.ts の `endFrame <= startFrame` を `telop_ts_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_non_positive_telop_frame_range` / `test_compare_telop_split_invalid_telop_frame_range_emits_tail`、Codex 推奨 PR-PO)。zero/reversed duration な telop が KPI 比較やレンダラーの frame range 判定を silent pass することを防ぐ | PR-PO |
| 492 | `template/scripts/compare_telop_split.py` の `parse_telop_data_ts()` が string/comment-aware な top-level object counter (`_count_telop_array_items()`) で array item 数と regex parse 件数を照合し、field 名 typo / field-order drift で prefix 不一致な item を `telop_ts_invalid` JSON tail + exit 3 で reject することを lint (`test_compare_telop_split_rejects_unparsed_telop_item_shape` / `test_compare_telop_split_unparsed_telop_item_shape_emits_tail`、Codex 推奨 PR-PP)。regex prefix に合わない malformed item が silent skip して KPI 比較の件数・gate 判定に影響することを防ぐ | PR-PP |
| 493 | `template/scripts/build_telop_data.py` が `vad_result.json` の FileNotFoundError / OSError / json.JSONDecodeError / VadSchemaError を `_emit_error("vad_invalid", 8, error=redact_error_message(str(e)))` で捕捉し、未捕捉例外伝播を防ぐ (`test_build_telop_data_rejects_invalid_vad_result` 4 case loop、Codex 推奨 PR-PQ)。vad_result.json 欠損・破損が uncaught exception で終了し observability tail が出ない問題を修正 | PR-PQ |
| 494 | `template/scripts/build_slide_data.py` が `load_json(transcript_path)` / `load_json(config_path)` の OSError / json.JSONDecodeError を `_emit_error("build_slide_transcript_invalid", 3)` / `_emit_error("config_invalid", 3)` で捕捉し、malformed JSON 入力が uncaught exception で終了する問題を修正 (`test_build_slide_data_rejects_malformed_json_inputs` 2 case、Codex 推奨 PR-PR)。redact_error_message で path 漏れも防止 | PR-PR |
| 495 | `template/scripts/build_slide_data.py` のオプション vad_result.json ロード (OSError / json.JSONDecodeError → `_emit_error("vad_invalid", 8)`) および VadSchemaError → `_emit_error("vad_invalid", 8)`、`--plan` load_json (OSError / json.JSONDecodeError → `_emit_error("build_slide_plan_invalid", 2)`) を捕捉し uncaught 例外終了を防ぐ (`test_build_slide_data_rejects_invalid_vad_and_plan_load` 2 case: malformed_vad / malformed_plan、Codex 推奨 PR-PS)。redact_error_message で abs path 漏れも防止 | PR-PS |
| 496 | `template/scripts/build_telop_data.py` の `transcript_fixed.json` load (FileNotFoundError / OSError / json.JSONDecodeError → `_emit_error("build_telop_transcript_invalid", 3, error=redact_error_message(str(e)))`) を捕捉し uncaught 例外終了を防ぐ (`test_build_telop_data_rejects_invalid_transcript_load` 3 case: missing / directory / malformed_json、Codex 推奨 PR-PT)。category=transcript-invalid, exit 3 で STATUS_MAP 整合 | PR-PT |
| 497 | `template/scripts/build_telop_data.py` の optional `typo_dict.json` load (OSError / json.JSONDecodeError → `_emit_error("typo_dict_invalid", 3, error=redact_error_message(str(e)))`) を捕捉し uncaught 例外終了を防ぐ (`test_build_telop_data_rejects_invalid_typo_dict_load` 2 case: directory / malformed_json、Codex 推奨 PR-PU)。typo.exists() でガード済みのため FileNotFoundError は不要、category=typo_dict_invalid, exit 3 で STATUS_MAP 整合 | PR-PU |
| 498 | `template/scripts/generate_slide_plan.py` の `transcript_fixed.json` / `project-config.json` load (OSError / json.JSONDecodeError → `return emit_json("transcript_invalid"/"config_invalid", 3, error=redact_error_message(str(e)))`) を捕捉し uncaught 例外終了を防ぐ (`test_generate_slide_plan_rejects_malformed_json_inputs` 2 case: malformed_transcript / malformed_config)。exists() ガード済みのため FileNotFoundError は不要、emit_json closure (return int) パターンで sys.exit 不要 | PR-PU |
| 499 | `template/scripts/voicevox_narration.py` の `transcript_fixed.json` load (OSError / json.JSONDecodeError → `return emit_json("transcript_invalid", 3, error=...)`) と `--script-json` load in `collect_chunks()` (OSError / json.JSONDecodeError → `raise TranscriptSegmentError(...)` — 既存ハンドラで吸収) を捕捉し uncaught 例外終了を防ぐ (`test_voicevox_narration_rejects_malformed_json_inputs` 2 case: malformed_transcript / malformed_script_json) | PR-PU |
| 500 | `template/scripts/build_slide_data.py` の backup read + `slideData.ts` write_text ブロック (OSError → `_emit_error("build_slide_write_error", 3, error=redact_error_message(str(e)))` + STATUS_MAP 追加) を捕捉し uncaught 例外終了を防ぐ (`test_build_slide_data_rejects_write_error` 1 case: read-only output path、Codex 推奨 PR-PV)。write 失敗が uncaught で終了し observability tail が出ない問題を修正、category=slide-write-error | PR-PV |
| 501 | `template/scripts/build_telop_data.py` の `telopData.ts` write_text (OSError → `_emit_error("build_telop_write_error", 3, error=redact_error_message(str(e)))` + STATUS_MAP 追加) を捕捉し uncaught 例外終了を防ぐ (`test_build_telop_data_rejects_write_error` 1 case: read-only output path)。category=telop-write-error | PR-PW |
| 502 | `template/scripts/generate_slide_plan.py` の `slide_plan.json` write_text (OSError → `return emit_json("generate_slide_plan_write_error", 3, error=redact_error_message(str(e)))` + STATUS_MAP 追加) を捕捉し uncaught 例外終了を防ぐ (`test_generate_slide_plan_rejects_write_error` 1 case: read-only output path、API mock使用)。category=slide-plan-write-error | PR-PX |
| 503 | `template/scripts/generate_slide_plan.py` の `urllib.request.urlopen` ブロックに `except (urllib.error.URLError, OSError)` 節追加 (socket timeout / DNS / SSL/TLS / connection refused → `return emit_json("api_network_error", 4, error=err)` + STATUS_MAP 追加)。HTTPError のみ捕捉で URLError が raw traceback になる問題を修正 (`test_generate_slide_plan_rejects_network_error` 1 case: mock_urlopen raising URLError)。category=api-network-error | PR-PM |
| 504 | `template/scripts/preflight_video.py` の `run_ffprobe()` に `timeout=30` + `except subprocess.TimeoutExpired` 追加。hung ffprobe が uncaught で観測尾部をスキップする問題を修正 (`test_preflight_video_rejects_ffprobe_timeout` 1 case: mock_run raising TimeoutExpired → FFprobeError → main() ffprobe_failed exit 3)。category=ffprobe-failed | PR-PM |
| 505 | `template/scripts/build_telop_data.py` の `call_budoux()` subprocess.run に `timeout=30` + `except subprocess.TimeoutExpired` 追加。hung Node.js が無限 block するのを防ぎ、TimeoutExpired を `RuntimeError('budoux_split.mjs timed out: ...')` に変換。呼び出し元の `except Exception` fallback (line 408) がこれを捕捉し legacy splitting で継続 (`test_build_telop_data_budoux_timeout_raises_runtime_error` 1 case: mock subprocess.run raising TimeoutExpired → RuntimeError with 'timed out')。pipeline は hang せず続行 | PR-PM |
| 506 | `template/scripts/visual_smoke.py` の全 subprocess 呼び出し 4 箇所に timeout 追加 + `TimeoutExpired` 捕捉: `probe_dim()` check_output に `timeout=30`、`render_still()` check_call に `timeout=120`、`has_drawtext_filter()` check_output に `timeout=30`、`make_grid()` check_call に `timeout=60`。各 call site の except 節に `subprocess.TimeoutExpired` を追加 (`render_still` call site → `env_error="still_failed"`、`probe_dim` call site → `env_error="probe_failed"`、`make_grid` call site → `grid_status="failed"`)。has_drawtext_filter は既存 except に TimeoutExpired を追加し `False` を返す (`test_visual_smoke_render_still_timeout_exits_3`、`test_visual_smoke_probe_dim_timeout_exits_3`、`test_visual_smoke_has_drawtext_filter_timeout_returns_false`、`test_visual_smoke_make_grid_timeout_exits_3` 4 tests 追加)。hung remotion / ffprobe / ffmpeg が uncaught TimeoutExpired で observability tail をスキップする問題を修正 | PR-PM |
| 507 | `template/scripts/voicevox_narration.py` の `write_narration_data()` call site (line ~764) の except 節に `OSError` を追加。`write_narration_data()` は `atomic_write_text()` を 2 回呼ぶ (`chunk_meta.json` / `narrationData.ts`) が、`atomic_write_text()` が raise する `OSError` (mkdir / write_text / os.replace 失敗) が捕捉されずに chunk 残置 + rollback スキップが起きていた。fix: `except (wave.Error, EOFError, OSError) as e:` に拡張、`isinstance(e, OSError)` で分岐し `write_narration_data_write_error` (exit 6) を emit。secondly: synthesize ループの except に `OSError` を追加 (`URLError` と同系統の `urlopen` 失敗経路)。`_observability.py` に `write_narration_data_write_error` エントリ追加 (`test_vn_write_narration_data_oserror_propagates_and_caller_catches` 追加) | PR-PM |
| 508 | `template/scripts/voicevox_narration.py` の synth loop (line ~708) の except 節に `json.JSONDecodeError` を追加。`synthesize()` は `/audio_query` レスポンスを `json.loads()` するが (line ~187)、VOICEVOX engine が invalid JSON を返した場合に `JSONDecodeError` が uncaught で `__main__` まで伝播し structured status emit をスキップしていた (P1)。fix: `except (HTTPError, URLError, OSError, json.JSONDecodeError)` に拡張し既存 synth failure と同扱いで WARN + continue (`test_vn_synthesize_json_decode_error_is_caught_as_synth_failure` 追加) | PR-PM |
| 509 | `template/scripts/voicevox_narration.py` の `--list-speakers` path (line ~632) で `list_speakers()` 呼び出しを `try/except (HTTPError, URLError, OSError, json.JSONDecodeError)` で囲む。`check_engine()` 成功後も `/speakers` endpoint が失敗/invalid JSON を返すと uncaught で `__main__` まで伝播し structured status emit をスキップしていた。fix: 捕捉時に `list_speakers_error` (exit 4) を emit。`_observability.py` に `list_speakers_error` エントリ追加 (`test_vn_list_speakers_error_guard` 追加) | PR-PM |
| 510 | `template/scripts/voicevox_narration.py` の `collect_chunks()` `--script` path (line ~454) で `read_text()` を `try/except (OSError, UnicodeDecodeError)` で囲み `TranscriptSegmentError` に変換。`--script-json` path は既に同パターン (`try/except (OSError, json.JSONDecodeError)` → `TranscriptSegmentError`) を持つが `--script` path が未ガードだった。caller の `except TranscriptSegmentError` → `emit_json("transcript_invalid", 3)` が正しく発火する (`test_vn_collect_chunks_script_read_oserror_raises_transcript_error` 追加) | PR-PM |
| 511 | `template/scripts/build_telop_data.py` の `main()` 内 3 箇所 (`transcript_fixed.json` / `vad_result.json` / `typo_dict.json` の `read_text(encoding="utf-8")`) の `except` tuple に `UnicodeDecodeError` を追加。corrupted (non-UTF-8) JSON ファイルで `read_text()` が raise する `UnicodeDecodeError` が `except (OSError, json.JSONDecodeError)` を素通りし top-level に uncaught で伝播し structured status emit をスキップしていた。fix: 各 except tuple に `UnicodeDecodeError` を追加 (`test_build_telop_data_rejects_unicode_decode_error` 追加、3 sub-case: transcript/vad/typo_dict) | PR-PM |
| 512 | `template/scripts/build_slide_data.py` の `main()` 内 4 箇所 (`transcript_fixed.json` / `project-config.json` / `vad_result.json` / `--plan` の `load_json()`) の `except` tuple に `UnicodeDecodeError` を追加。step 511 と同型の gap — corrupted JSON ファイルで `UnicodeDecodeError` が uncaught で top-level に伝播し structured status emit をスキップしていた。fix: 各 except tuple に `UnicodeDecodeError` を追加 (`test_build_slide_data_rejects_unicode_decode_error` 追加、4 sub-case: transcript exit3/config exit3/vad exit8/plan exit2) | PR-PM |
| 513 | `template/scripts/compare_telop_split.py` 2 箇所 + `generate_slide_plan.py` 2 箇所の `except` tuple に `UnicodeDecodeError` を追加。step 511-512 と同型の gap — corrupted (non-UTF-8) JSON ファイルで `read_text()` が raise する `UnicodeDecodeError` が `except (OSError, json.JSONDecodeError)` を素通りし uncaught で伝播し structured status emit をスキップしていた。fix: 各 except tuple に `UnicodeDecodeError` を追加 (`test_compare_telop_split_rejects_unicode_decode_error` 2 sub-case + `test_generate_slide_plan_rejects_unicode_decode_error` 2 sub-case 追加) | PR-PM |
| 514 | `template/scripts/voicevox_narration.py` 3 箇所の `except` tuple に `UnicodeDecodeError` を追加。`--script-json` path load / `transcript_fixed.json` load / `vad_result.json` load の各 `except` が `UnicodeDecodeError` を捕捉できず uncaught で伝播し structured status emit をスキップしていた gap を修正。fix: 各 except tuple に `UnicodeDecodeError` を追加 + contract lint test の vad exception tuple 文字列を新 tuple に更新 (`test_vn_unicode_decode_error_guard` 追加) | PR-PM |
| 515 | `template/scripts/timeline.py:266` + `preflight_video.py:683` の `except` tuple に `UnicodeDecodeError` を追加。`timeline.load_cut_segments()` の `vad_result.json` load (行 266) と `preflight_video.py` の `--write-config` 既存 config parse (行 683) が non-UTF-8 ファイルで raise する `UnicodeDecodeError` を捕捉できず uncaught で伝播していた gap を修正。fix: 各 except tuple に `UnicodeDecodeError` を追加 (`test_timeline_load_cut_segments_rejects_unicode_decode_error` + `test_preflight_write_config_unicode_guard_contract_lint` 追加) | PR-PM |
| 516 | `template/scripts/timeline.py:49` + `build_slide_data.py:481` + `visual_smoke.py:414` の `except OSError` に `UnicodeDecodeError` を追加。`timeline.read_video_config_fps()` の `videoConfig.ts` read / `build_slide_data` の `slideData.ts` backup read / `visual_smoke` の `videoConfig.ts` read が非 UTF-8 ファイルで `UnicodeDecodeError` を捕捉できず uncaught で伝播していた gap を修正。fix: 各 except に `UnicodeDecodeError` を追加 (`test_step516_unicode_guard_contract_lint` + `test_timeline_read_video_config_fps_swallows_unicode_error` 追加) | PR-PM |
| 517 | `template/scripts/voicevox_narration.py:845` の `out_path.stat().st_size` が concat_wavs_atomic 成功後の diagnostic print で unguarded だった gap を修正。stat() 失敗 (race condition / permission error) が uncaught で main() を終了させ sentinel write をスキップする恐れ。fix: stat() を try/except OSError でラップし失敗時は "unknown" を出力 (`test_vn_stat_oserror_guard_contract_lint` 追加) | PR-PM |
| 518 | `template/scripts/voicevox_narration.py` の 2 箇所 — (1) `synthesize()` 呼び出しサイト (chunk ループ内 except 句): `aq_body.decode("utf-8")` が非 UTF-8 VOICEVOX レスポンスで `UnicodeDecodeError` を送出すると既存の `except (HTTPError, URLError, OSError, JSONDecodeError)` が捕捉できず sentinel emit をスキップする gap。(2) `check_engine()`: `/version` レスポンスの `body.decode("utf-8")` が `UnicodeDecodeError` を送出すると `except (URLError, OSError, HTTPError)` に捕捉されず `main()` の emit を経ずに伝播する gap。fix: 両 except 句に `UnicodeDecodeError` を追加 (`test_vn_synthesize_unicode_guard_contract_lint` 追加、2 snippet を検証) | PR-PM |
| 519 | `template/scripts/generate_slide_plan.py:589` の Anthropic API レスポンス読み取り `resp.read().decode("utf-8")` が非 UTF-8 バイト列で `UnicodeDecodeError` を送出した場合、既存の `except (urllib.error.URLError, OSError)` が捕捉できず `main()` を `emit_json` なしに終了させる gap。fix: `except (urllib.error.URLError, OSError, UnicodeDecodeError)` に追加 (`test_gsp_api_unicode_guard_contract_lint` 追加) | PR-PM |

## Test Requirements

- `test_timeline_integration.py` に redaction regression test を実装済:
  - abs_path: `test_observability_safe_artifact_path_redacts` (PR #3)、`test_compare_telop_split_error_message_redacted` (PR-G)、`test_build_slide_data_human_stdout_path_redacted_by_default` (PR-I)、`test_voicevox_narration_summary_path_redacted_by_default` (PR-I fix iter)、`test_generate_slide_plan_stderr_proj_path_redacted` (PR-J)
  - user_content: `test_observability_user_content_meta_no_raw` (PR #3)
  - provider_response_body: `test_observability_redact_provider_body_default_strict` + `test_observability_provider_body_stderr_default_redact` (PR #3)
  - secret: `test_observability_redact_secret_long_value_keeps_last_4` / `_short_value_full_mask` / `_non_string_passthrough` / `_custom_last_n_and_mask_char` / `_last_n_zero_or_negative_full_mask` (PR-H + fix iter、helper-level の last-4 mask + boundary fail-closed を直接 verify)
  - error_message redact: `test_observability_redact_error_message_strips_abs_path` (PR-G fix iter 2)、`_windows_path` / `_ipv6_and_data_uri_safe` / `_multiple_paths_in_one_msg` (PR-K)
- 各 script の `--json-log` smoke test (parse + schema_version 確認) は `test_timeline_integration.py` 内 v1 schema test (`test_observability_build_status_v1_schema` 等) でカバー済。
- cost telemetry の missing rate behavior は `test_generate_slide_plan_skip_preserves_with_bad_env` / `test_generate_slide_plan_rate_rejects_nan_inf` / `test_generate_slide_plan_rate_v0_v1_alias_precedence` (PR-D) で網羅済。

## Open Questions

- (resolved by PR-E、`Trace Context Convention` section 参照: `run_id` は env 優先 + 未設定時 auto-generate、`parent_run_id` / `step_id` は env 優先 + 未設定時 None で auto-generate しない、cap 128 char、cap 超過は error。)
- (resolved by PR-V、artifact path の root 解決優先順序: `§Path Policy` に `project_root` 優先 → `repo_root` fallback → lexical_redact placeholder の 3 段解決順序を正式仕様化済)
- redaction `version` の bump policy (schema_version とは独立、redaction rule 変更時のみ bump)。
- VOICEVOX 以外の local engine が増えた場合の cost 取り扱い (cost null vs 削除)。
